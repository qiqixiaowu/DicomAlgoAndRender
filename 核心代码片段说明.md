# 核心代码片段详细说明

## 1. 射线-AABB包围盒求交算法

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L70-L86)

### 算法原理
```glsl
bool intersectRayAABB(vec3 rayOrigin, vec3 rayDir, 
                     vec3 boxMin, vec3 boxMax,
                     out float tEnter, out float tExit)
{
    // 1. 计算射线方向的倒数（优化除法为乘法）
    vec3 invRayDir = 1.0 / rayDir;
    
    // 2. 计算射线与包围盒6个面的交点参数t
    //    对于每个轴: t = (plane - rayOrigin) / rayDir
    vec3 t1 = (boxMin - rayOrigin) * invRayDir;  // 与最小面的交点
    vec3 t2 = (boxMax - rayOrigin) * invRayDir;  // 与最大面的交点
    
    // 3. 确保t1是近交点，t2是远交点
    vec3 tMin = min(t1, t2);
    vec3 tMax = max(t1, t2);
    
    // 4. 找到三个轴中最远的近交点（进入点）
    float tmin = max(max(tMin.x, tMin.y), tMin.z);
    
    // 5. 找到三个轴中最近的远交点（退出点）
    float tmax = min(min(tMax.x, tMax.y), tMax.z);
    
    // 6. 判断是否相交
    //    - tmax < tmin: 射线未击中包围盒
    //    - tmax < 0: 包围盒在射线后方
    if (tmax < max(tmin, 0.0)) return false;
    
    tEnter = tmin;
    tExit = tmax;
    return true;
}
```

### 可视化示意
```
        射线方向 →
           ╱
          ╱
    相机 ●╱
        ╱
       ╱  tEnter
      ╱   ↓
     ┌────●────┐
     │    ╱    │ ← 体数据包围盒
     │   ╱     │   [0,0,0] to [1,1,1]
     │  ╱      │
     │ ●       │
     └─↑───────┘
       tExit
       
步骤：
1. 计算进入点: entryPoint = rayOrigin + rayDir * tEnter
2. 计算退出点: exitPoint = rayOrigin + rayDir * tExit
3. 在这两点之间进行采样
```

---

## 2. 蓝噪声抖动算法

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L20-L110)

### 为什么需要抖动？

**问题：**步进采样会产生"木纹伪影"（Wood Grain Artifact）
```
无抖动采样:
射线1: ●---●---●---●---●  固定步长
射线2: ●---●---●---●---●  
射线3: ●---●---●---●---●
      ↑ 相同位置产生条纹

有抖动采样:
射线1: ●--●----●-●-----●  随机偏移
射线2: ●----●-●-----●--●
射线3: ●-●-----●--●----●
      ↑ 位置打乱，条纹消失
```

### 哈希函数（伪随机数生成）
```glsl
// 2D输入 → 1D输出 [0,1]
float hash12(vec2 p)
{
    // 1. 将输入映射到3D空间（增加随机性）
    vec3 p3 = fract(vec3(p.xyx) * 0.1031);
    
    // 2. 使用点积混合分量
    p3 += dot(p3, p3.yzx + 19.19);
    
    // 3. 计算最终哈希值
    return fract((p3.x + p3.y) * p3.z);
}

// 示例输出:
// hash12(vec2(1.0, 2.0)) → 0.7234
// hash12(vec2(1.1, 2.0)) → 0.1892  (微小变化导致完全不同的输出)
```

### 梯度噪声（类似Perlin噪声）
```glsl
float gradientNoise(vec2 p)
{
    // 1. 分解为整数和小数部分
    vec2 i = floor(p);  // 网格坐标
    vec2 f = fract(p);  // 单元格内位置 [0,1]
    
    // 2. 获取四个角点的随机值
    //    c ---- d
    //    |      |
    //    |  f   |  ← 插值位置
    //    |      |
    //    a ---- b
    float a = hash12(i);
    float b = hash12(i + vec2(1.0, 0.0));
    float c = hash12(i + vec2(0.0, 1.0));
    float d = hash12(i + vec2(1.0, 1.0));
    
    // 3. 平滑插值函数（Smoothstep）
    //    普通线性插值: u = f
    //    Smoothstep:   u = 3f² - 2f³
    vec2 u = f * f * (3.0 - 2.0 * f);
    
    // 4. 双线性插值
    //    先在x方向插值: ab = lerp(a, b, u.x)
    //                  cd = lerp(c, d, u.x)
    //    再在y方向插值: result = lerp(ab, cd, u.y)
    return mix(mix(a, b, u.x), 
               mix(c, d, u.x), u.y);
}
```

### 屏幕空间蓝噪声
```glsl
float screenSpaceBlueNoise(vec2 pixelCoord)
{
    // 1. 归一化像素坐标到纹理空间
    vec2 uv = pixelCoord / screenSize * noiseScale;
    
    // 2. 添加像素级偏移（避免重复模式）
    uv += vec2(hash12(floor(pixelCoord * 0.1)), 
               hash12(floor(pixelCoord * 0.1 + 0.5)));
    
    // 3. 多层噪声叠加（类似Fractional Brownian Motion）
    float noise1 = gradientNoise(uv * 1.0);       // 低频 (50%权重)
    float noise2 = gradientNoise(uv * 2.0 + 100.0); // 中频 (25%权重)
    float noise3 = gradientNoise(uv * 4.0 + 200.0); // 高频 (12.5%权重)
    
    // 4. 加权组合
    return noise1 * 0.5 + noise2 * 0.25 + noise3 * 0.125;
}
```

### 在主循环中使用
```glsl
for (int i = 0; i < numSteps; ++i)
{
    // 1. 生成噪声值 [0,1]
    float noiseValue = screenSpaceBlueNoise(gl_FragCoord.xy + vec2(float(i) * 0.1));
    
    // 2. 转换为抖动偏移 [-jitterStrength*stepSize, +jitterStrength*stepSize]
    //    (noiseValue - 0.5) → [-0.5, 0.5]
    //    * jitterStrength   → [-0.5*strength, 0.5*strength]
    //    * stepSize         → 与步长成比例
    float jitter = (noiseValue - 0.5) * jitterStrength * stepSize;
    
    // 3. 应用抖动
    vec3 samplePos = basePos + rayDir * jitter;
    
    // 采样...
}
```

**效果对比：**
- 无抖动：规则条纹，类似木纹
- 白噪声抖动：高频噪点明显
- 蓝噪声抖动：平滑，噪点分布均匀 ✓

---

## 3. 窗宽窗位算法

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L205-L213)

### 医学背景
在CT扫描中，不同组织的密度以**Hounsfield Unit (HU)** 表示：
```
HU值范围:
  -1000         -100    0     40    80    300         3000
    |-----------|------|-----|-----|-----|------------|
   空气        肺  脂肪 水  软组织 骨骼      金属

窗宽窗位作用：
- 选择感兴趣的密度范围
- 增强特定组织的对比度
- 抑制无关组织的显示
```

### 算法实现
```glsl
float applyWindow(float density, float level, float width) 
{
    // 1. 计算窗口边界
    //    level: 窗口中心
    //    width: 窗口宽度
    float windowMin = level - width / 2.0;
    float windowMax = level + width / 2.0;
    
    // 2. 分段映射
    //    density < windowMin → 0.0 (黑色，完全透明)
    //    density > windowMax → 1.0 (白色，不透明)
    //    中间区域 → 线性映射到 [0, 1]
    if (density <= windowMin) return 0.0;
    if (density >= windowMax) return 1.0;
    return (density - windowMin) / width;
}
```

### 可视化
```
原始密度值:
     0.0                0.5                1.0
      |------------------|------------------|
              ↓ 应用窗口

窗口映射 (level=0.5, width=0.4):
     windowMin=0.3      level=0.5      windowMax=0.7
         |                 |                 |
         0.0              0.5               1.0
         ↓                 ↓                 ↓
    输出: 0.0            0.5               1.0

区域外的值被截断:
- density < 0.3 → 0.0 (完全透明，不显示)
- density > 0.7 → 1.0 (完全不透明)
- 0.3 < density < 0.7 → 线性映射
```

### 预设窗口
```cpp
// 1. 软组织窗（观察肌肉、脏器）
windowLevel = 0.26f;  // 对应 HU ≈ 40
windowWidth = 0.1f;   // 窗宽 ≈ 400 HU

// 2. 骨窗（观察骨骼结构）
windowLevel = 0.35f;  // 对应 HU ≈ 400
windowWidth = 0.5f;   // 窗宽 ≈ 2000 HU

// 3. 肺窗（观察肺部病变）
windowLevel = 0.3f;   // 对应 HU ≈ 200
windowWidth = 0.08f;  // 窗宽 ≈ 80 HU
```

---

## 4. 梯度计算（法线提取）

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L215-L228)

### 为什么需要梯度？
```
体数据是密度场:
     高密度(骨骼)
          ↑
          |  ← 密度变化最快的方向
          |     = 梯度方向
     低密度(软组织)
     
梯度方向 = 表面法线
```

### 中心差分法
```glsl
vec3 computeSimpleGradient(vec3 pos)
{
    const float eps = 0.01;  // 采样间距
    vec3 grad;
    
    // X方向梯度
    //   前向采样 - 后向采样
    //   ───────────────────
    //         2 * eps
    grad.x = texture(volume, pos + vec3(eps, 0.0, 0.0)).r 
           - texture(volume, pos - vec3(eps, 0.0, 0.0)).r;
    
    // Y方向梯度
    grad.y = texture(volume, pos + vec3(0.0, eps, 0.0)).r 
           - texture(volume, pos - vec3(0.0, eps, 0.0)).r;
    
    // Z方向梯度
    grad.z = texture(volume, pos + vec3(0.0, 0.0, eps)).r 
           - texture(volume, pos - vec3(0.0, 0.0, eps)).r;
    
    // 归一化得到单位法向量
    return normalize(grad);
}
```

### 采样图示
```
        Z
        |
        ●  pos + (0,0,eps)
        |
        |
  ●-----●-----●  pos层
  |     |     |
 -X   原点   +X
        |
        |
        ●  pos - (0,0,eps)

总共6次纹理采样:
  [+X, -X, +Y, -Y, +Z, -Z]
  
梯度向量:
  ∇density = (∂ρ/∂x, ∂ρ/∂y, ∂ρ/∂z)
```

### 替代方案（性能优化）
```glsl
// 索贝尔算子（Sobel Operator）- 更准确但需要26次采样
vec3 computeSobelGradient(vec3 pos) {
    float dx = 
        -texture(volume, pos + vec3(-eps,-eps,-eps)).r +
         texture(volume, pos + vec3( eps,-eps,-eps)).r +
        // ... (共26个采样点)
    // ...
}
```

---

## 5. Phong光照模型

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L230-L253)

### 三个分量
```glsl
vec3 applyPhongLighting(vec3 color, vec3 position, 
                       vec3 normal, vec3 viewDir)
{
    vec3 lightDir = normalize(lightPos - position);
    
    // ===== 1. 环境光 (Ambient) =====
    //   模拟间接光照，无方向性
    vec3 ambient = ambientStrength * color;
    
    // ===== 2. 漫反射 (Diffuse) =====
    //   基于Lambert余弦定律
    //   I = I₀ * max(N·L, 0)
    //
    //        N (法线)
    //        ↑
    //        |  θ
    //        | /
    //        |/_____ L (光线方向)
    //      表面
    //
    //   当θ=0°(正面): cos(0)=1.0 (最亮)
    //   当θ=90°(侧面): cos(90)=0.0 (不照亮)
    float diff = max(dot(normal, lightDir), 0.0);
    vec3 diffuse = diff * diffuseStrength * color * lightColor;
    
    // ===== 3. 镜面反射 (Specular) =====
    //   模拟高光，基于Phong反射模型
    //   I = I₀ * max(R·V, 0)^shininess
    //
    //        N
    //        |    R (反射)
    //        |   /
    //        |  / α
    //        | /_____ V (视线)
    //      表面  \
    //             \ L (入射)
    //
    //   R = reflect(-L, N) = 2(N·L)N - L
    vec3 reflectDir = reflect(-lightDir, normal);
    float spec = pow(max(dot(viewDir, reflectDir), 0.0), shininess);
    vec3 specular = spec * specularStrength * lightColor;
    
    // ===== 4. 距离衰减 =====
    //   物理上: I ∝ 1/d²
    //   实际使用: I = 1/(a₀ + a₁d + a₂d²)
    //   避免d→0时的奇异性
    float distance = length(lightPos - position);
    float attenuation = 1.0 / (1.0 + 0.1*distance + 0.01*distance*distance);
    
    // ===== 5. 组合三个分量 =====
    //   环境光不受衰减影响（来自四面八方）
    //   漫反射和镜面反射受衰减影响
    return ambient + (diffuse + specular) * attenuation;
}
```

### 参数调整效果
| 参数 | 值 | 视觉效果 |
|------|-----|---------|
| `ambient` | 0.1 | 很暗，阴影区域几乎黑色 |
| `ambient` | 0.5 | 明亮，细节清晰但对比度降低 |
| `diffuse` | 0.0 | 无方向感，像卡通渲染 |
| `diffuse` | 1.0 | 强烈的明暗对比 |
| `specular` | 0.0 | 哑光表面 |
| `specular` | 1.0 | 光泽表面，高光明显 |
| `shininess` | 2 | 高光范围大（塑料感） |
| `shininess` | 128 | 高光范围小（金属感） |

### 可视化
```
明暗分布示例 (从光源看向物体):

shininess = 8:          shininess = 32:        shininess = 128:
    ████                    ██                      ▓
   ██████                  ████                    ███
  ████████                ██████                  █████
 ██████████              ████████                ███████
   高光                    高光                    高光
   范围大                  中等                    范围小

环境光=0.0:             环境光=0.3:            环境光=0.7:
█████░░░░░             █████▓▓▓▓▓             █████████
█████░░░░░             █████▓▓▓▓▓             █████████
█████░░░░░             █████▓▓▓▓▓             █████████
 受光面  背光面          受光面  背光面          几乎无对比
```

---

## 6. Alpha混合算法

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L276-L293)

### 前向混合公式
```glsl
// 在步进循环中累积颜色
for (int i = 0; i < numSteps; ++i)
{
    // ... 采样、光照计算 ...
    
    // 当前采样点的透明度
    float alpha = clamp(colorSample.a * windowedDensity * 1.5, 0.0, 1.0);
    
    // ===== 前向Alpha混合公式 =====
    // 新颜色 = 已累积颜色 + (1 - 已累积α) × 当前α × 当前颜色
    accumulatedColor.rgb += (1.0 - accumulatedAlpha) * alpha * litColor;
    
    // 新透明度 = 已累积α + (1 - 已累积α) × 当前α
    accumulatedAlpha += (1.0 - accumulatedAlpha) * alpha;
}
```

### 数学推导
基于体渲染方程的离散化：
$$
\begin{aligned}
C_n &= C_{n-1} + T_{n-1} \cdot \alpha_n \cdot c_n \\
T_n &= T_{n-1} \cdot (1 - \alpha_n) \\
\alpha_{acc,n} &= 1 - T_n
\end{aligned}
$$

其中：
- $C_n$ = 累积颜色
- $T_n$ = 透射率（剩余穿透能力）
- $\alpha_n$ = 当前采样点透明度
- $c_n$ = 当前采样点颜色

### 逐步示例
```
假设沿射线有4个采样点:

初始状态:
  accColor = (0, 0, 0)
  accAlpha = 0.0

采样点1: color=(1.0, 0.0, 0.0, 0.3)  红色，α=0.3
  accColor = (0,0,0) + (1-0.0)*0.3*(1,0,0) = (0.3, 0.0, 0.0)
  accAlpha = 0.0 + (1-0.0)*0.3 = 0.3
  剩余穿透率 = 1 - 0.3 = 0.7

采样点2: color=(0.0, 1.0, 0.0, 0.5)  绿色，α=0.5
  accColor = (0.3,0,0) + (1-0.3)*0.5*(0,1,0) = (0.3, 0.35, 0.0)
  accAlpha = 0.3 + (1-0.3)*0.5 = 0.65
  剩余穿透率 = 1 - 0.65 = 0.35

采样点3: color=(0.0, 0.0, 1.0, 0.4)  蓝色，α=0.4
  accColor = (0.3,0.35,0) + (1-0.65)*0.4*(0,0,1) = (0.3, 0.35, 0.14)
  accAlpha = 0.65 + (1-0.65)*0.4 = 0.79
  剩余穿透率 = 1 - 0.79 = 0.21

采样点4: color=(1.0, 1.0, 1.0, 0.8)  白色，α=0.8
  accColor = (0.3,0.35,0.14) + (1-0.79)*0.8*(1,1,1) = (0.47, 0.52, 0.31)
  accAlpha = 0.79 + (1-0.79)*0.8 = 0.96
  剩余穿透率 = 1 - 0.96 = 0.04  ← 几乎不透明

最终输出: rgba(0.47, 0.52, 0.31, 0.96)
```

### 早期终止优化
```glsl
if (accumulatedAlpha > 0.99) break;
```

**原理：**
- 当累积透明度接近1.0（完全不透明）时
- 后面的采样点对最终颜色贡献 < 1%
- 提前终止可节省大量计算

**效果：**
```
无早期终止:
射线穿过骨骼后继续采样空气
  ●-●-●-█-█-░-░-░  (8次采样)
      骨骼  空气
        ↑
     已不透明，但继续采样(浪费)

有早期终止:
  ●-●-●-█-█  (5次采样，节省37.5%)
           ↑
        α>0.99，终止
```

---

## 7. 后处理技术

### Gamma校正
```glsl
// 线性空间 → sRGB空间
accumulatedColor.rgb = pow(accumulatedColor.rgb, vec3(1.0/2.2));
```

**为什么需要？**
- OpenGL默认使用线性光照计算
- 显示器使用sRGB色彩空间（非线性）
- 不校正会导致图像过暗

**公式：**
$$
\text{sRGB} = \text{Linear}^{1/2.2}
$$

**对比：**
```
线性值:    0.0   0.25   0.5   0.75   1.0
           |-----|-----|-----|-----|
           
无Gamma校正 (显示器解释为sRGB):
           ▓▓▓░░░░░░░░░░░░░░░░░░  ← 暗部丢失细节

有Gamma校正:
           ▓▓▓▓▓▓░░░░░░░░░░░░░░  ← 亮度正常
```

### 边缘增强
```glsl
if (accumulatedAlpha > 0.1)
{
    float edgeNoise = screenSpaceBlueNoise(gl_FragCoord.xy * 2.0);
    accumulatedColor.rgb *= 1.0 + 0.2 * edgeNoise;
}
```

**作用：**
- 减少几何边缘的锯齿
- 使用噪声打破规则边缘
- 类似抖动抗锯齿（Dithering）

---

## 8. 完整采样循环代码注释版

### 代码位置
[volume_frag_glsl.frag](shader/volume_frag_glsl.frag#L255-L310)

```glsl
void main()
{
    // ========== 第一步：射线设置 ==========
    const vec3 boxMin = vec3(0.0);
    const vec3 boxMax = vec3(1.0);
    
    vec3 rayOrigin = cameraPos.xyz;        // 相机位置（模型空间）
    vec3 rayEnd = vertexPos.xyz;           // 立方体顶点
    vec3 rayDir = normalize(rayEnd - rayOrigin);  // 射线方向
    
    // ========== 第二步：求交测试 ==========
    float tEnter, tExit;
    if (!intersectRayAABB(rayOrigin, rayDir, boxMin, boxMax, tEnter, tExit))
    {
        discard;  // 射线未击中，丢弃片元
    }
    
    // ========== 第三步：计算步进参数 ==========
    tEnter = max(tEnter, 0.0);  // 确保起点在相机前方
    vec3 entryPoint = rayOrigin + rayDir * tEnter;
    vec3 exitPoint = rayOrigin + rayDir * tExit;
    
    float rayLength = distance(entryPoint, exitPoint);  // 射线长度
    int numSteps = int(rayLength / stepSize) + 1;      // 步数
    
    // ========== 第四步：初始化累积器 ==========
    vec4 accumulatedColor = vec4(0.0);
    float accumulatedAlpha = 0.0;
    
    vec3 viewDir = normalize(-rayDir);  // 视线方向（光照用）
    vec2 pixelCoord = gl_FragCoord.xy;  // 像素坐标（抖动用）
    
    // ========== 第五步：步进循环 ==========
    for (int i = 0; i < numSteps; ++i)
    {
        // 早期终止优化
        if (accumulatedAlpha > 0.99) break;
        
        // --- 5.1 计算抖动 ---
        float noiseValue = screenSpaceBlueNoise(
            pixelCoord + vec2(float(i) * 0.1)
        );
        float jitter = (noiseValue - 0.5) * jitterStrength * stepSize;
        
        // --- 5.2 计算采样位置 ---
        float t = float(i) / float(numSteps - 1);  // 归一化位置 [0,1]
        vec3 basePos = mix(entryPoint, exitPoint, t);  // 基础位置
        vec3 samplePos = basePos + rayDir * jitter;    // 应用抖动
        
        // --- 5.3 边界检查 ---
        if (any(lessThan(samplePos, boxMin - 0.001)) || 
            any(greaterThan(samplePos, boxMax + 0.001)))
        {
            break;  // 超出边界，终止
        }
        
        // --- 5.4 密度采样 ---
        float density = texture(volume, samplePos).r;  // 3D纹理采样
        
        // --- 5.5 窗宽窗位调整 ---
        float windowedDensity = applyWindow(density, windowLevel, windowWidth);
        
        // 空区域跳过
        if (windowedDensity < 0.01) continue;
        
        // --- 5.6 传输函数 ---
        vec4 colorSample = texture(transferFunc, windowedDensity);
        
        // --- 5.7 梯度计算 ---
        vec3 normal = computeSimpleGradient(samplePos);
        
        // --- 5.8 光照计算 ---
        vec3 litColor = applyPhongLighting(
            colorSample.rgb,  // 基础颜色
            samplePos,        // 位置（衰减计算）
            normal,           // 法线
            viewDir           // 视线方向
        );
        
        // --- 5.9 阴影因子 ---
        vec3 lightDir = normalize(lightPos - samplePos);
        float shadowFactor = max(dot(normal, lightDir), 0.0);
        litColor *= mix(0.5, 1.0, shadowFactor);  // 暗部至少保留50%亮度
        
        // --- 5.10 透明度调整 ---
        float alpha = colorSample.a * windowedDensity * 1.5;
        alpha = clamp(alpha, 0.0, 1.0);
        
        // --- 5.11 Alpha混合 ---
        accumulatedColor.rgb += (1.0 - accumulatedAlpha) * alpha * litColor;
        accumulatedAlpha += (1.0 - accumulatedAlpha) * alpha;
    }
    
    // ========== 第六步：后处理 ==========
    
    // 边缘增强
    if (accumulatedAlpha > 0.1)
    {
        float edgeNoise = screenSpaceBlueNoise(pixelCoord * 2.0);
        accumulatedColor.rgb *= 1.0 + 0.2 * edgeNoise;
    }
    
    // 亮度调整
    accumulatedColor.rgb *= brightness;
    
    // Gamma校正
    accumulatedColor.rgb = pow(accumulatedColor.rgb, vec3(1.0/2.2));
    
    // ========== 第七步：输出 ==========
    if (accumulatedAlpha < 0.001)
    {
        discard;  // 完全透明，丢弃
    }
    
    outColor = vec4(accumulatedColor.rgb, accumulatedAlpha);
}
```

---

## 性能分析

### 主要开销来源
```
GPU时间分布（估算）:

纹理采样: ████████████████ 60%
  - 体数据采样: 1次/步 × numSteps
  - 梯度计算: 6次/步 × numSteps
  - 传输函数: 1次/步 × numSteps

光照计算: ████████ 20%
  - 向量归一化、点积、反射

噪声计算: ████ 10%
  - 哈希函数、梯度噪声

其他: ████ 10%
  - 分支、混合等
```

### 优化建议

1. **减少采样次数**
```glsl
// 优化前：每步7次纹理采样
float density = texture(volume, pos).r;         // 1次
vec3 normal = computeGradient(pos);             // 6次
vec4 color = texture(transferFunc, density);    // 1次

// 优化后：预计算梯度到另一个纹理
vec4 densityAndGradient = texture(precomputedVolume, pos);  // 1次
vec3 normal = densityAndGradient.gba;
float density = densityAndGradient.r;
```

2. **自适应步长**
```glsl
// 低密度区域大步长，高密度区域小步长
float adaptiveStepSize = baseStepSize * (1.0 + 2.0 * (1.0 - density));
```

3. **LOD（Level of Detail）**
```glsl
// 远处物体使用粗糙采样
float lodFactor = length(samplePos - cameraPos);
int steps = max(minSteps, int(baseSteps / (1.0 + lodFactor)));
```

---

## 总结

这个医学图像光线投射系统的核心技术点：

✅ **射线-AABB求交** - 确定采样范围  
✅ **蓝噪声抖动** - 消除条纹伪影  
✅ **窗宽窗位** - 医学图像标准调整  
✅ **梯度计算** - 提取表面法线  
✅ **Phong光照** - 真实感渲染  
✅ **Alpha混合** - 半透明体绘制  
✅ **早期终止** - 性能优化  
✅ **Gamma校正** - 色彩空间转换  

每个技术都经过精心调优，实现了高质量的医学图像可视化。

---

## 相关文档

- [README_光线投射技术总览.md](README_光线投射技术总览.md) - 项目总览和快速开始
- [光线投射渲染流程详解.md](光线投射渲染流程详解.md) - 完整渲染流程12阶段
- [坐标系统与光线生成详解.md](坐标系统与光线生成详解.md) - 坐标空间转换与光线发射原理 ⭐新增
