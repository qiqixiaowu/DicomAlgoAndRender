# 坐标系统与光线生成详解

## 目录
1. [五大坐标空间](#五大坐标空间)
2. [坐标变换矩阵](#坐标变换矩阵)
3. [光线从哪里发射](#光线从哪里发射)
4. [代理几何体原理](#代理几何体原理)
5. [从像素到射线的完整过程](#从像素到射线的完整过程)
6. [实际代码解析](#实际代码解析)

---

## 五大坐标空间

### 1. 模型空间（Model Space / Object Space）

**定义：** 物体自身的局部坐标系统

**在本项目中：**
- 立方体的8个顶点定义在 `[0,0,0]` 到 `[1,1,1]` 范围内
- 体数据的纹理坐标也是 `[0,0,0]` 到 `[1,1,1]`
- 这个空间中，立方体的中心在 `(0.5, 0.5, 0.5)`

```cpp
// 立方体顶点（模型空间）
float vertices[] = {
    0.0f, 0.0f, 0.0f,  // v0: 左下后
    1.0f, 0.0f, 0.0f,  // v1: 右下后
    1.0f, 1.0f, 0.0f,  // v2: 右上后
    0.0f, 1.0f, 0.0f,  // v3: 左上后
    0.0f, 0.0f, 1.0f,  // v4: 左下前
    1.0f, 0.0f, 1.0f,  // v5: 右下前
    1.0f, 1.0f, 1.0f,  // v6: 右上前
    0.0f, 1.0f, 1.0f   // v7: 左上前
};
```

**可视化：**
```
模型空间坐标系:
      Z (前)
      ↑   Y (上)
      |  ↗
      | /
      |/_____ X (右)
     原点(0,0,0)

立方体:
      v7────────v6
      /|       /|
     / |      / |
    v4────────v5|
    |  v3────|──v2
    | /      | /
    |/       |/
    v0───────v1
```

---

### 2. 世界空间（World Space）

**定义：** 场景的全局坐标系统

**在本项目中：**
- 模型矩阵（Model Matrix）= 单位矩阵（无变换）
- 立方体直接放置在世界坐标原点
- 相机围绕立方体中心 `(0.5, 0.5, 0.5)` 旋转

```cpp
// main.cpp
glm::mat4 model = glm::mat4(1.0f);  // 单位矩阵，无变换
```

**作用：**
- 在这个项目中，世界空间和模型空间重合
- 如果有多个物体，世界空间用于统一管理它们的相对位置

---

### 3. 观察空间（View Space / Camera Space / Eye Space）

**定义：** 以相机为原点的坐标系统

**在本项目中：**
- 相机通过轨道控制器（OrbitCamera）围绕目标旋转
- 目标点：`(0.5, 0.5, 0.5)` （立方体中心）
- 距离：可调整（默认3.0单位）
- 方向：由Yaw和Pitch角度控制

```cpp
// camera.cpp - 计算相机位置
void OrbitCamera::updateCamera()
{
    // 根据Yaw和Pitch计算相机在球面上的位置
    float yawRad = glm::radians(yaw);
    float pitchRad = glm::radians(pitch);
    
    // 球坐标转笛卡尔坐标
    position.x = target.x + distance * cos(pitchRad) * sin(yawRad);
    position.y = target.y + distance * sin(pitchRad);
    position.z = target.z + distance * cos(pitchRad) * cos(yawRad);
}
```

**视图矩阵（View Matrix）：**
```cpp
glm::mat4 getViewMatrix() const
{
    // LookAt矩阵：从世界空间变换到观察空间
    return glm::lookAt(
        position,        // 相机位置
        target,          // 观察目标（立方体中心）
        glm::vec3(0,0,1) // 上方向（Z轴向上）
    );
}
```

**可视化：**
```
观察空间 (相机视角):
       Y (上)
       ↑
       |
       |_____ X (右)
      /
     ↙
    Z (观察方向，指向屏幕内)
    
相机位置 = 原点(0,0,0)
物体在相机前方（负Z方向）
```

---

### 4. 裁剪空间（Clip Space）

**定义：** 透视投影后的坐标系统

**投影矩阵：**
```cpp
glm::mat4 projection = glm::perspective(
    glm::radians(45.0f),  // FOV (视野角度)
    800.0f / 600.0f,      // 宽高比
    0.1f,                 // 近裁剪面
    100.0f                // 远裁剪面
);
```

**投影原理：**
```
透视投影 (Perspective Projection):
            近裁剪面
             (near)
    视锥体     |
    (Frustum)  |  物体
        \      |  /
         \     | /
          \    |/
           \   *   ← 相机位置
            \ /|
             / |
               |
             远裁剪面
             (far)

投影后:
- X, Y 被压缩到 [-w, +w] 范围
- Z 被映射到 [0, w] (OpenGL) 或 [-w, w] (其他API)
- W 分量 = 原始的 -Z 值（用于透视除法）
```

**数学公式：**
$$
\begin{bmatrix}
x_{clip} \\
y_{clip} \\
z_{clip} \\
w_{clip}
\end{bmatrix}
=
\begin{bmatrix}
\frac{f}{aspect} & 0 & 0 & 0 \\
0 & f & 0 & 0 \\
0 & 0 & \frac{far+near}{near-far} & \frac{2 \cdot far \cdot near}{near-far} \\
0 & 0 & -1 & 0
\end{bmatrix}
\begin{bmatrix}
x_{view} \\
y_{view} \\
z_{view} \\
1
\end{bmatrix}
$$

其中：$f = \frac{1}{\tan(FOV/2)}$

---

### 5. 规范化设备坐标（NDC - Normalized Device Coordinates）

**定义：** 透视除法后的归一化坐标

**计算：**
```glsl
vec3 ndc;
ndc.x = clip.x / clip.w;  // 范围: [-1, +1]
ndc.y = clip.y / clip.w;  // 范围: [-1, +1]
ndc.z = clip.z / clip.w;  // 范围: [-1, +1] (OpenGL)
```

**坐标范围：**
```
NDC空间（标准立方体）:
        +Y (上)
         |
    (-1,1)────(1,1)
         |\ /  |
         | X   |
         |/ \  |
    (-1,-1)───(1,-1)─── +X (右)
        /
       /
      +Z (屏幕外)

中心点: (0, 0, 0)
可见范围: x,y,z ∈ [-1, +1]
```

---

### 6. 屏幕空间（Screen Space）

**定义：** 最终的像素坐标

**视口变换：**
```cpp
// OpenGL自动执行
x_screen = (ndc.x * 0.5 + 0.5) * width;   // [0, 800]
y_screen = (ndc.y * 0.5 + 0.5) * height;  // [0, 600]
```

**片元着色器中的坐标：**
```glsl
gl_FragCoord.xy  // 当前像素的屏幕坐标
// 例如: (400.5, 300.5) 表示屏幕中心像素
```

---

## 坐标变换矩阵

### 完整的变换流程

```cpp
// 顶点着色器中的变换
vec4 position_clip = projection * modelview * vec4(inPosition, 1.0);
//                       ↑            ↑              ↑
//                    投影矩阵     视图×模型矩阵    模型空间顶点
```

**分步详解：**

#### 步骤1：模型变换（Model Transform）
```cpp
vec4 position_world = model * vec4(inPosition, 1.0);
```
- **输入：** 模型空间顶点 `(x_model, y_model, z_model)`
- **输出：** 世界空间顶点 `(x_world, y_world, z_world)`
- **本项目：** `model = I`（单位矩阵），所以 `world == model`

#### 步骤2：观察变换（View Transform）
```cpp
vec4 position_view = view * position_world;
```
- **输入：** 世界空间顶点
- **输出：** 观察空间顶点
- **矩阵：** LookAt矩阵

**LookAt矩阵构造：**
```cpp
vec3 forward = normalize(target - position);  // 观察方向
vec3 right = normalize(cross(forward, up));   // 右方向
vec3 up_actual = cross(right, forward);       // 实际上方向

// LookAt矩阵
mat4 view = mat4(
    right.x,    up_actual.x,    -forward.x,    0,
    right.y,    up_actual.y,    -forward.y,    0,
    right.z,    up_actual.z,    -forward.z,    0,
    -dot(right,position), -dot(up_actual,position), dot(forward,position), 1
);
```

#### 步骤3：投影变换（Projection Transform）
```cpp
vec4 position_clip = projection * position_view;
```
- **输入：** 观察空间顶点
- **输出：** 裁剪空间顶点（透视除法前）

#### 步骤4：透视除法（Perspective Division）
```glsl
// GPU自动执行
vec3 ndc = position_clip.xyz / position_clip.w;
```

### ModelView矩阵优化

```cpp
// main.cpp - 合并模型和视图矩阵
glm::mat4 view = orbitCamera.getViewMatrix();
glm::mat4 modelview = view * model;

// 传递给着色器
glUniformMatrix4fv(mvLoc, 1, GL_FALSE, glm::value_ptr(modelview));
```

**为什么合并？**
- 减少着色器中的矩阵乘法
- `modelview * vertex` 比 `view * (model * vertex)` 更快
- 节省uniform传输带宽

---

## 光线从哪里发射

### 核心原理：代理几何体方法

**传统方法的问题：**
```
如果为每个像素直接计算射线:
  for each pixel (x, y):
      ray = compute_ray_from_pixel(x, y)  // 需要复杂计算
      raycasting(ray)
      
问题: 需要手动计算每个像素对应的3D射线
```

**本项目的解决方案：代理几何体（Proxy Geometry）**
```
渲染一个立方体:
  Draw立方体 → 顶点着色器处理8个顶点 → 光栅化自动生成片元
  
优势: 光栅化自动为每个像素计算射线参数
```

---

### 光线发射的完整过程

#### 阶段1：顶点着色器 - 计算射线参数

```glsl
// volume_vert_glsl.vert
void main() 
{
    // 1️⃣ 计算裁剪空间坐标（正常的顶点处理）
    gl_Position = projection * modelview * vec4(inPosition, 1.0);
    
    // 2️⃣ 【关键】计算相机位置（在模型空间）
    //    为什么要在模型空间？因为体数据纹理坐标也是模型空间 [0,1]³
    cameraPos = inverse(modelview) * vec4(0.0, 0.0, 0.0, 1.0);
    //           ↑                    ↑
    //      观察空间→模型空间        观察空间的相机位置
    
    // 3️⃣ 保存立方体顶点位置（模型空间）
    vertexPos = vec4(inPosition, 1.0);
    
    // 后续在片元着色器中:
    //   射线起点 = cameraPos (模型空间)
    //   射线终点 = vertexPos (模型空间，每个顶点不同)
    //   射线方向 = normalize(vertexPos - cameraPos)
}
```

**为什么`cameraPos = inverse(modelview) * vec4(0,0,0,1)`？**

逐步推导：
```
目标: 求相机在模型空间的位置

已知:
- 模型空间顶点 → modelview变换 → 观察空间顶点
- 观察空间的相机位置 = (0,0,0)

推导:
观察空间: P_view = modelview × P_model
求 P_model 当 P_view = (0,0,0,1):
  (0,0,0,1) = modelview × P_camera_model
  P_camera_model = inverse(modelview) × (0,0,0,1)

结论: 相机在模型空间的位置就是 ModelView矩阵逆变换的平移部分
```

**图解：**
```
观察空间（相机视角）:          模型空间（立方体视角）:
                              
   相机●                           立方体
   (0,0,0)                        ┌────┐
     |                            │    │
     | 看向                        │ ● │ ← 相机在这里
     ↓                            │    │  (经过inverse变换)
   立方体                          └────┘
   (某个位置)                     
```

#### 阶段2：光栅化 - 自动插值

```
立方体的8个顶点经过光栅化后:

顶点v0: cameraPos=C, vertexPos=(0,0,0)
顶点v1: cameraPos=C, vertexPos=(1,0,0)
顶点v2: cameraPos=C, vertexPos=(1,1,0)
...

光栅化插值后，某个像素P:
  cameraPos = C (所有片元相同)
  vertexPos = 插值(v0, v1, v2, ...) (每个片元不同！)
  
例如立方体表面某点:
  vertexPos = (0.3, 0.7, 0.2)  // 三角形内插值得到
```

**关键洞察：**
- `cameraPos` 在所有片元中都相同（相机位置不变）
- `vertexPos` 在每个片元中不同（立方体表面不同位置）
- 因此每个像素自动得到不同的射线方向！

#### 阶段3：片元着色器 - 计算射线

```glsl
// volume_frag_glsl.frag
void main()
{
    // 1️⃣ 计算射线起点和方向（全部在模型空间）
    vec3 rayOrigin = cameraPos.xyz;              // 相机位置（模型空间）
    vec3 rayEnd = vertexPos.xyz;                 // 立方体表面点（插值得到）
    vec3 rayDir = normalize(rayEnd - rayOrigin); // 射线方向
    
    // 2️⃣ 射线-立方体求交（确定采样范围）
    float tEnter, tExit;
    bool hit = intersectRayAABB(
        rayOrigin, rayDir,
        vec3(0,0,0),   // 立方体最小点（模型空间）
        vec3(1,1,1),   // 立方体最大点（模型空间）
        tEnter, tExit
    );
    
    if (!hit) {
        discard;  // 射线未击中，丢弃片元
    }
    
    // 3️⃣ 计算实际的采样起点和终点
    vec3 entryPoint = rayOrigin + rayDir * tEnter;  // 射线进入立方体的点
    vec3 exitPoint = rayOrigin + rayDir * tExit;    // 射线离开立方体的点
    
    // 4️⃣ 沿着射线采样体数据
    // entryPoint 和 exitPoint 同时也是纹理坐标（因为都在[0,1]³范围）
    for (int i = 0; i < numSteps; ++i) {
        vec3 samplePos = mix(entryPoint, exitPoint, t);
        float density = texture(volume, samplePos).r;  // 直接用作纹理坐标
        // ...
    }
}
```

---

### 为什么这个方法有效？

#### 1. 自动处理视角变化

```
不同的相机位置 → 不同的cameraPos → 不同的射线方向

相机从正面看:               相机从侧面看:
   ●相机                        相机●
   ↓                              ↘
  ┌────┐                         ┌────┐
  │    │                         │    │
  └────┘                         └────┘
  
射线垂直进入立方体           射线斜着进入立方体
```

#### 2. 自动处理视锥剔除

```
只有可见的立方体部分会被光栅化:

从远处看（立方体小）:     从近处看（立方体大，部分在视野外）:
    ┌─┐                       ████████
    │ │ ← 整个立方体          ████████  ← 只有这部分
    └─┘    在屏幕上            可见      被光栅化
```

#### 3. 自动插值生成连续射线

```
立方体三角形面片:

v0────────v1
 │＼      │
 │  ＼ P  │   P点的vertexPos = 插值(v0, v1, v2)
 │    ＼  │   因此相邻像素有连续的射线方向
 │      ＼│
v2────────v3
```

---

## 代理几何体原理深入

### 为什么用立方体？

#### 作用1：定义采样范围

```
体数据的有效范围是 [0,0,0] 到 [1,1,1]:

立方体顶点正好覆盖这个范围:
    v7(0,1,1)────v6(1,1,1)
       /|           /|
      / |          / |
  v4(0,0,1)────v5(1,0,1)
     |  v3(0,1,0)|──v2(1,1,0)
     | /          | /
     |/           |/
  v0(0,0,0)────v1(1,0,0)
  
任何击中立方体表面的射线都会穿过有效的体数据区域
```

#### 作用2：提供射线终点

```
射线计算:
  起点: cameraPos (相机位置，可能在立方体外)
  终点: vertexPos (立方体表面的某个点)
  方向: normalize(vertexPos - cameraPos)

不同像素看到立方体的不同表面 → 不同的vertexPos → 不同的射线
```

#### 作用3：利用GPU并行

```
传统CPU方法:
  for y in range(height):
      for x in range(width):
          ray = compute_ray(x, y)
          raycasting(ray)
  
串行执行，慢

GPU方法（本项目）:
  Draw Cube (8个顶点)
    ↓
  光栅化（自动并行）
    ↓
  片元着色器（每个像素并行执行）
  
完全并行，快！
```

---

### 立方体的索引顺序

```cpp
unsigned int indices[] = {
    // 前面 (Z=0)
    0, 1, 2,  0, 2, 3,  // 两个三角形
    // 右面 (X=1)
    1, 5, 6,  1, 6, 2,
    // 后面 (Z=1)
    5, 4, 7,  5, 7, 6,
    // 左面 (X=0)
    4, 0, 3,  4, 3, 7,
    // 上面 (Y=1)
    3, 2, 6,  3, 6, 7,
    // 下面 (Y=0)
    4, 5, 1,  4, 1, 0
};
```

**为什么是12个三角形？**
- 立方体有6个面
- 每个面由2个三角形组成
- 总共 6 × 2 = 12 个三角形

**顶点顺序的重要性：**
```
正确的逆时针顺序（从外向内看）:
v0────v1       v2────v6
│  ＼  │   或  │  ＼  │
│    ＼│       │    ＼│
v2────v3       v3────v7

错误的顺序会导致面剔除出问题
```

---

## 从像素到射线的完整过程

### 时间线展示

```
T1: CPU设置绘制命令
    └─> glBindVertexArray(cube.VAO)
    └─> glDrawElements(GL_TRIANGLES, 36, ...)

T2: GPU - 顶点着色器阶段
    └─> 处理8个顶点
        └─> 每个顶点:
            ├─> 计算 gl_Position (屏幕位置)
            ├─> 计算 cameraPos (模型空间相机位置)
            └─> 传递 vertexPos (模型空间顶点位置)

T3: GPU - 图元装配
    └─> 将顶点组装成12个三角形

T4: GPU - 光栅化
    └─> 为每个三角形生成覆盖的像素
    └─> 插值顶点属性（cameraPos, vertexPos等）
    └─> 示例:
        像素(400, 300): vertexPos = (0.5, 0.5, 0.2)
        像素(401, 300): vertexPos = (0.51, 0.5, 0.2)
        像素(400, 301): vertexPos = (0.5, 0.51, 0.2)

T5: GPU - 片元着色器阶段（每个像素并行）
    └─> 当前像素获得插值后的 cameraPos, vertexPos
    └─> 计算射线:
        rayOrigin = cameraPos
        rayDir = normalize(vertexPos - cameraPos)
    └─> 执行光线投射算法
    └─> 输出最终颜色

T6: GPU - 深度测试、混合
    └─> 写入帧缓冲

T7: CPU - 交换缓冲
    └─> glfwSwapBuffers(window)
    └─> 显示到屏幕
```

---

## 实际代码解析

### 完整的射线生成代码（带注释）

#### 1. 顶点着色器

```glsl
#version 430 core
layout (location = 0) in vec3 inPosition;  // 输入：模型空间顶点

uniform mat4 projection;   // 投影矩阵
uniform mat4 modelview;    // 模型视图矩阵（View × Model）

out vec4 cameraPos;  // 输出：相机位置（模型空间）
out vec4 vertexPos;  // 输出：顶点位置（模型空间）

void main() 
{
    // ===== 第一步：计算屏幕位置 =====
    // 标准的顶点变换流程
    gl_Position = projection * modelview * vec4(inPosition, 1.0);
    //            └───┬───┘   └───┬───┘   └───────┬───────┘
    //            投影变换    观察变换      模型空间→齐次坐标
    
    // ===== 第二步：计算相机在模型空间的位置 =====
    // 
    // 数学推导:
    //   观察空间中相机位置 = (0, 0, 0, 1)
    //   P_view = modelview × P_model
    //   当 P_view = (0,0,0,1) 时:
    //   (0,0,0,1) = modelview × P_camera_model
    //   P_camera_model = inverse(modelview) × (0,0,0,1)
    //
    cameraPos = inverse(modelview) * vec4(0.0, 0.0, 0.0, 1.0);
    //          └─────┬──────┘      └─────────┬─────────┘
    //        观察→模型变换         观察空间相机位置
    
    // ===== 第三步：保存顶点位置（模型空间）=====
    // 这个值会在光栅化时被插值
    // 不同像素获得不同的 vertexPos（立方体表面不同位置）
    vertexPos = vec4(inPosition, 1.0);
    
    // 注意：cameraPos 在所有片元中相同
    //      vertexPos 在不同片元中不同（插值结果）
}
```

#### 2. 片元着色器 - 射线计算部分

```glsl
#version 430 core

in vec4 cameraPos;  // 从顶点着色器传入（所有片元相同）
in vec4 vertexPos;  // 从顶点着色器传入（每个片元不同，插值）

out vec4 outColor;

void main()
{
    // ===== 第一步：构造射线（模型空间）=====
    vec3 rayOrigin = cameraPos.xyz;  // 射线起点 = 相机位置
    vec3 rayEnd = vertexPos.xyz;     // 射线"目标" = 立方体表面点
    vec3 rayDir = normalize(rayEnd - rayOrigin);  // 射线方向（归一化）
    
    // 示例值（假设相机在 (2,2,2)，看向某个像素击中的立方体表面点）:
    //   rayOrigin = (2.0, 2.0, 2.0)          // 相机位置
    //   rayEnd = (0.3, 0.7, 0.2)             // 立方体表面（插值得到）
    //   rayDir = normalize((-1.7, -1.3, -1.8)) = (-0.63, -0.48, -0.61)
    
    // ===== 第二步：射线-AABB求交 =====
    // 立方体边界框（模型空间）
    const vec3 boxMin = vec3(0.0, 0.0, 0.0);
    const vec3 boxMax = vec3(1.0, 1.0, 1.0);
    
    float tEnter, tExit;
    if (!intersectRayAABB(rayOrigin, rayDir, boxMin, boxMax, tEnter, tExit))
    {
        discard;  // 射线未击中立方体（不应该发生，但作为保护）
    }
    
    // tEnter: 射线进入立方体的参数（射线方程: P = Origin + t * Dir）
    // tExit:  射线离开立方体的参数
    
    // 确保起点在相机前方（tEnter可能为负，说明相机在立方体内部）
    tEnter = max(tEnter, 0.0);
    
    // ===== 第三步：计算采样起点和终点 =====
    vec3 entryPoint = rayOrigin + rayDir * tEnter;  // 进入点（模型空间）
    vec3 exitPoint = rayOrigin + rayDir * tExit;    // 退出点（模型空间）
    
    // 由于体数据纹理坐标也是 [0,1]³，这些点可以直接用作纹理坐标！
    
    // 示例值:
    //   entryPoint = (0.1, 0.2, 0.15)  // 射线从这里进入立方体
    //   exitPoint = (0.9, 0.8, 0.7)    // 射线从这里离开立方体
    
    // ===== 第四步：沿射线采样 =====
    float rayLength = distance(entryPoint, exitPoint);
    int numSteps = int(rayLength / stepSize) + 1;
    
    for (int i = 0; i < numSteps; ++i)
    {
        float t = float(i) / float(numSteps - 1);  // [0, 1]
        vec3 samplePos = mix(entryPoint, exitPoint, t);  // 线性插值
        
        // samplePos 同时是:
        //   1. 模型空间的3D坐标
        //   2. 体数据纹理的3D纹理坐标（因为都在[0,1]³）
        
        float density = texture(volume, samplePos).r;  // 采样体数据
        
        // ... 后续的光照、混合等处理 ...
    }
}
```

---

### 坐标一致性的关键

**为什么要在模型空间计算射线？**

```
原因1: 体数据纹理坐标就是模型空间 [0,1]³
  ✓ 采样点可以直接用作纹理坐标
  ✗ 如果在观察空间，还需要变换回模型空间

原因2: 简化计算
  ✓ 立方体边界框固定为 [0,0,0] to [1,1,1]
  ✗ 如果在观察空间，边界框会随相机变化

原因3: 数值稳定性
  ✓ 模型空间范围小 [0,1]，精度高
  ✗ 观察空间范围可能很大，精度损失
```

---

## 不同视角的射线示例

### 示例1：从正面观察（Yaw=0°, Pitch=0°）

```
相机设置:
  位置: (0.5, 0.5, 3.0)  // 在立方体前方
  目标: (0.5, 0.5, 0.5)  // 看向立方体中心
  距离: 3.0

射线特征:
  方向: 大致沿着 -Z 方向
  
┌─────────────────┐
│  屏幕/立方体     │
│   (正面视图)     │
│                 │
│   ┌────────┐    │
│   │        │    │
│   │  ●     │    │ ← 射线几乎垂直穿过立方体
│   │        │    │
│   └────────┘    │
│                 │
└─────────────────┘
      ↑
   相机在这里 ●
  (0.5, 0.5, 3.0)
```

### 示例2：从侧面观察（Yaw=90°）

```
相机设置:
  位置: (3.5, 0.5, 0.5)  // 在立方体右侧
  目标: (0.5, 0.5, 0.5)
  距离: 3.0

射线特征:
  方向: 大致沿着 -X 方向

                相机 ●
               (3.5, 0.5, 0.5)
                   ↓
                   射线斜着击中立方体侧面
                   ↓
┌─────────────────────────┐
│         立方体            │
│        ┌────┐            │
│        │    │←───射线    │
│        │    │            │
│        └────┘            │
└─────────────────────────┘
```

### 示例3：从内部观察（相机在立方体内）

```
相机设置:
  位置: (0.5, 0.5, 0.5)  // 在立方体中心
  距离: 0.3 (缩小到内部)

射线特征:
  tEnter < 0 (相机在立方体内部)
  需要用 tEnter = max(tEnter, 0.0) 修正

      立方体
    ┌─────────┐
    │    ↗    │
    │  ● ↑    │ ← 射线从中心向四周发射
    │    ↖    │
    └─────────┘

从中心看，可以看到所有6个面
```

---

## 常见问题与调试

### Q1: 为什么有时候看不到任何东西？

**检查清单：**

1. **相机是否在立方体外？**
```cpp
// 在main.cpp中检查
float distance = orbitCamera.getDistance();
vec3 position = orbitCamera.getPosition();

// 应该大于立方体半对角线长度
float safeDistance = sqrt(3.0f) * 0.5f;  // ≈ 0.866
if (distance < safeDistance) {
    // 相机可能在立方体内部
}
```

2. **ModelView矩阵是否正确？**
```glsl
// 在片元着色器中调试
outColor = vec4(cameraPos.xyz, 1.0);  // 显示相机位置
// 应该看到合理的值，如 (2.0, 2.0, 2.0)
```

3. **射线方向是否正确？**
```glsl
outColor = vec4(rayDir * 0.5 + 0.5, 1.0);  // 方向可视化
// 应该看到平滑的颜色渐变（表示方向连续变化）
```

### Q2: 为什么边缘有锯齿？

**原因分析：**
- 立方体的三角形边缘被光栅化
- 抗锯齿未启用

**解决方案：**
```cpp
// main.cpp - 启用MSAA
glfwWindowHint(GLFW_SAMPLES, 4);
glEnable(GL_MULTISAMPLE);
```

### Q3: 性能问题排查

**着色器性能分析：**
```glsl
// 在片元着色器中添加计时
float startTime = float(gl_FragCoord.x); // 伪计时
// ... 执行光线投射 ...
float endTime = float(numSteps);

// 可视化采样次数
outColor = vec4(vec3(float(numSteps) / 1000.0), 1.0);
// 黑色=少量采样，白色=大量采样
```

---

## 总结

### 核心要点

1. **坐标空间转换链：**
   ```
   模型空间 → 世界空间 → 观察空间 → 裁剪空间 → NDC → 屏幕空间
   ```

2. **光线在模型空间计算：**
   - 起点：`inverse(modelview) × (0,0,0,1)` （相机位置）
   - 方向：`normalize(vertexPos - cameraPos)`
   - 优势：直接匹配体数据纹理坐标

3. **代理几何体方法：**
   - 渲染立方体 → GPU自动光栅化 → 每个像素获得射线参数
   - 无需手动计算每个像素的射线
   - 完全并行，性能高

4. **关键洞察：**
   - `cameraPos` 所有片元相同（相机固定）
   - `vertexPos` 每个片元不同（插值结果）
   - 因此每个像素自动获得不同的射线

### 数据流总结

```
CPU:
  设置相机参数 → 构建ModelView矩阵 → 绘制立方体命令

GPU顶点着色器:
  处理8个顶点 → 计算cameraPos和vertexPos → 传递给光栅化

GPU光栅化:
  生成覆盖的像素 → 插值cameraPos和vertexPos → 传递给片元着色器

GPU片元着色器:
  计算射线 → 射线投射采样 → 输出颜色

帧缓冲:
  存储最终图像 → 交换到屏幕
```

### 与传统光线追踪的对比

| 特性 | 传统CPU光线追踪 | 本项目（GPU光线投射） |
|------|----------------|---------------------|
| 射线生成 | 手动计算每个像素 | GPU自动插值 |
| 并行性 | 需要多线程 | 完全并行（硬件级） |
| 坐标系统 | 通常在世界空间 | 模型空间（与纹理匹配） |
| 几何 | 不需要代理几何 | 依赖立方体代理 |
| 灵活性 | 任意射线 | 射线从相机出发 |
| 性能 | 较慢 | 实时（60+ FPS） |

---

**相关文档：**
- [光线投射渲染流程详解.md](光线投射渲染流程详解.md) - 完整算法流程
- [核心代码片段说明.md](核心代码片段说明.md) - 代码级详解
- [README_光线投射技术总览.md](README_光线投射技术总览.md) - 项目总览

---

**最后更新：** 2026年2月12日
