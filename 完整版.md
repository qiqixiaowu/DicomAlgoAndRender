# 医学影像渲染与重建项目 · 面试准备完整版

> 涵盖：项目自述 → 模块详解 → 算法原理推导 → 高频问答 → 项目难点与亮点 → 一句话速答清单
> 技术栈：C++17 / OpenGL 4.3 / CUDA 12.4 / TensorRT / PyTorch / DCMTK

---

## 目录

1. [一分钟项目自述](#1-一分钟项目自述)
2. [整体架构与模块清单](#2-整体架构与模块清单)
3. [CT 重建原理与推导](#3-ct-重建原理与推导)
4. [PET 重建原理与推导](#4-pet-重建原理与推导)
5. [CUDA 加速重建](#5-cuda-加速重建)
6. [体绘制（光线投射）与 MPR](#6-体绘制光线投射与-mpr)
7. [图像配准](#7-图像配准)
8. [分割算法（传统 + 深度学习）](#8-分割算法传统--深度学习)
9. [深度学习与部署（TensorRT）](#9-深度学习与部署tensorrt)
10. [高频面试问答](#10-高频面试问答)
11. [项目亮点与难点话术](#11-项目亮点与难点话术)
12. [一句话速答清单](#12-一句话速答清单)

---

## 1. 一分钟项目自述

> 「我做了一个**医学影像可视化与重建平台**，用 C++17 + OpenGL 实现。它覆盖三条主线：
> **① 重建**——CT 的 FBP/SIRT/ART、PET 的 MLEM/OSEM/FBP，并用 CUDA 做了 GPU 加速；
> **② 渲染**——基于光线投射的体绘制（Ray Casting）和 MPR 三正交切面，配传输函数和窗宽窗位；
> **③ AI 算法**——传统分割（区域生长、Chan-Vese、GraphCut）、3D 配准（Rigid/Demons/FFD），以及深度学习分割/配准/血管分割，最终通过 ONNX→TensorRT 部署回 C++。
> 数据全程走 DICOM（DCMTK 读取，堆叠成 3D 体）。整个项目的价值在于把**物理原理、数值算法、GPU 工程、AI 部署**串成完整闭环。」

---

## 2. 整体架构与模块清单

### 数据流总览

```
DICOM 序列 ──> 体数据构建(3D体) ──> ┬─ 体绘制/MPR 可视化
   (DCMTK)        (床板去除)        ├─ CT/PET 重建 (CPU/CUDA)
                                    ├─ 配准 (Rigid/Demons/FFD)
                                    └─ 分割 (传统 + DL/TensorRT)
```

### 模块表

| 模块 | 功能 | 关键技术 |
|------|------|----------|
| SharedLib | 公共静态库（相机/DICOM/重建/Shader） | C++17 |
| VolumeRender | 基础体绘制 + MPR | OpenGL Ray Casting |
| VolumeRenderOptimized | 高级体绘制 + 分割叠加 + TRT | AO/裁剪/合成模式 |
| CTReconstruction | CT 重建演示 | FBP/SIRT/ART |
| PETReconstruction | PET 重建演示（2D/3D） | MLEM/OSEM/FBP |
| CudaReconstruction | GPU 加速重建 + CPU 对比 | CUDA/cuFFT |
| RegistrationDemo | 配准可视化 | Demons/FFD/LCC |
| OncologyAlgorithms | 肿瘤测量/病灶/结节 | C++17 + OpenMP |
| MedicalSegmentation | header-only 分割库 | Chan-Vese/GraphCut/CRF |
| RTAutoContour | 放疗自动勾画 | Live-Wire/形态学 |
| dl_medical | DL 训练/推理/导出 | PyTorch/ONNX/TensorRT |

---

## 3. CT 重建原理与推导

### 3.1 物理模型

CT 测量的是 X 射线穿过人体的**衰减线积分**（朗伯-比尔定律）：

$$I = I_0 \, e^{-\int_L \mu(x,y)\,dl} \;\Rightarrow\; p = \ln\frac{I_0}{I} = \int_L \mu(x,y)\,dl$$

所以投影值 = 衰减系数 $\mu$ 沿射线的线积分。所有角度的投影 = **Radon 变换**：

$$p(\theta, t) = \iint f(x,y)\,\delta(x\cos\theta + y\sin\theta - t)\,dx\,dy$$

正弦图就是 $p(\theta, t)$，对应 `Sinogram{numAngles, numDetectors}`。

### 3.2 正向投影实现细节

代码中 `forwardProject` 的核心循环：

```cpp
// 射线参数化: x = t·cosθ - s·sinθ, y = t·sinθ + s·cosθ
// t = 径向偏移(探测器位置), s = 沿射线积分方向
for (float s = -sMax; s <= sMax; s += ds) {
    float ix = t * cosTheta - s * sinTheta + halfImage - 0.5f;
    float iy = t * sinTheta + s * cosTheta + halfImage - 0.5f;
    sum += bilinearSample(image, imageSize, ix, iy) * ds;
}
```

关键设计：
- **探测器间距** = `imageSize × √2 / numDetectors`，保证对角线全覆盖
- **双线性插值** `bilinearSample`：避免最近邻采样导致的条纹伪影
- **步长 ds=1**：像素级步进，与图像分辨率匹配

### 3.3 FBP（滤波反投影）完整推导

#### 3.3.1 中心切片定理

投影的 1D 傅里叶变换 = 图像 2D 傅里叶变换的一条切片：

$$P_\theta(\omega) = F(\omega\cos\theta, \omega\sin\theta)$$

**直觉**：在角度 $\theta$ 方向投影，相当于把 2D 频域中垂直于 $\theta$ 的所有频率分量叠加到 1D 频域上。

#### 3.3.2 从中心切片定理到 FBP

2D 逆傅里叶在极坐标下：

$$f(x,y) = \int_0^\pi \!\!\int_{-\infty}^{\infty} F(\omega\cos\theta, \omega\sin\theta)\,e^{j2\pi\omega t}\,|\omega|\,d\omega\,d\theta$$

极坐标 Jacobian 引出 $|\omega|$（Ramp 滤波器）——补偿低频过采样。代入中心切片定理：

$$f(x,y) = \int_0^\pi \left[\mathcal{F}^{-1}\{|\omega| \cdot \mathcal{F}\{p(\theta,\cdot)\}\}\right](x\cos\theta + y\sin\theta)\,d\theta$$

这就是 FBP 的数学本质：**滤波后的投影 → 反投影累加**。

#### 3.3.3 代码中的 FBP 流程

```
步骤1: 每角度提取投影 → 零填充到 2N → FFT(Cooley-Tukey 基2)
步骤2: 频域乘滤波器 → IFFT → 取实部
步骤3: 反投影: 每像素(x,y)算 t = x·cosθ + y·sinθ → 线性插值取滤波值 → 累加×Δθ
```

反投影核心：
```cpp
float t = px * cosTheta + py * sinTheta;           // 像素→探测器坐标
float detIdx = t / detectorSpacing + halfDet - 0.5f; // →探测器索引
val = (1-frac) * filteredProjections[a][d0] + frac * filteredProjections[a][d1]; // 线性插值
result.image[y * outputSize + x] += val * angleStep; // 累加×角度步长
```

#### 3.3.4 五种滤波器详解

| 滤波器 | 频域公式 | 特性 |
|--------|----------|------|
| **Ram-Lak** | $H(\omega)=|\omega|$ | 纯 Ramp，高频不衰减→分辨率最高但噪声大 |
| **Shepp-Logan** | $H=|\omega|\cdot\frac{\sin(\pi\omega/2)}{\pi\omega/2}$ | sinc 窗，$\omega=0$ 处平滑过渡，抑噪适中 |
| **Cosine** | $H=|\omega|\cdot\cos(\pi\omega/2)$ | 余弦窗，高频衰减更强 |
| **Hamming** | $H=|\omega|\cdot(0.54+0.46\cos(\pi\omega))$ | Hamming 窗，主瓣窄旁瓣低 |
| **Hann** | $H=|\omega|\cdot 0.5(1+\cos(\pi\omega))$ | 最强高频抑制，最平滑但分辨率损失最大 |

代码实现：
```cpp
float freq = std::abs(i - halfSize) / halfSize;  // 归一化频率 [0,1]
float ramp = freq;                                // Ram-Lak 基底
switch (type) {
case SHEPP_LOGAN: filter[i] = ramp * sinc(π·freq/2) / (π·freq/2); break;
case COSINE:      filter[i] = ramp * cos(π·freq/2); break;
case HAMMING:     filter[i] = ramp * (0.54 + 0.46·cos(π·freq)); break;
case HANN:        filter[i] = ramp * 0.5·(1 + cos(π·freq)); break;
}
```

**面试要点**：滤波器选择是分辨率与噪声的权衡。Ram-Lak 最锐利但噪声最大；Hann 最平滑。临床常用 Shepp-Logan 或 Cosine。

#### 3.3.5 FFT 实现细节（Cooley-Tukey 基2）

```cpp
// 位逆序置换 (bit-reversal)
for (int i=1, j=0; i<N; ++i) {
    int bit = N >> 1;
    for (; j & bit; bit >>= 1) j ^= bit;
    j ^= bit;
    if (i < j) std::swap(data[i], data[j]);
}
// 蝶形运算
for (int len=2; len<=N; len<<=1) {
    float angle = 2π/len * (inverse ? -1 : 1);
    complex wn(cos(angle), sin(angle));
    for (int i=0; i<N; i+=len) {
        complex w(1,0);
        for (int j=0; j<len/2; ++j) {
            auto u = data[i+j], v = data[i+j+len/2]*w;
            data[i+j] = u+v; data[i+j+len/2] = u-v;
            w *= wn;
        }
    }
}
```

**面试要点**：
- 位逆序保证蝶形运算的正确数据对齐
- 零填充到 `nextPowerOf2(2n)` 防止循环卷积混叠
- IFFT 只需翻转旋转因子 + 除以 N

### 3.4 SIRT 迭代重建详解

SIRT 把重建写成线性方程 $A\mathbf{x}=\mathbf{b}$，迭代更新：

$$x^{(k+1)} = x^{(k)} + \lambda \cdot \frac{A^T(b - Ax^{(k)})}{\text{weight}}$$

代码流程：
```
1. 正向投影当前估计 → simSino（模拟正弦图）
2. 残差 = measured - simulated
3. 反投影残差 → correction（每像素累加）
4. weight = 每像素被多少射线穿过
5. 更新: image += relaxation × correction / weight
6. 非负约束: image < 0 → 0
```

**关键设计**：
- `weight[]` 记录每像素被射线覆盖次数，避免中心过亮（与 PET 灵敏度图同理）
- `relaxation` 参数控制步长（默认 0.1），太大→振荡，太小→收敛慢
- 收敛判据：`totalError / sinogram.data.size()`（MSE）

### 3.5 ART 迭代重建详解

ART 是 Kaczmarz 投影法的医学影像版本，**逐条射线更新**：

$$x^{(k)} = x^{(k-1)} + \lambda \cdot \frac{p_i - A_i x^{(k-1)}}{\|A_i\|^2} \cdot A_i^T$$

代码中记录每条射线经过的像素 `rayPixels[]`，只更新这些像素：
```cpp
for (auto& rp : rayPixels) {
    result.image[rp.idx] += relaxation * residual * rp.w / rayWeight;
}
```

**SIRT vs ART**：
| | SIRT | ART |
|---|---|---|
| 更新频率 | 全射线一起 | 逐射线 |
| 收敛速度 | 慢 | 快 |
| 噪声特性 | 平滑 | 条纹/噪声 |
| 内存 | 需存全残差 | 只存当前射线 |
| 适用 | 低剂量/稀疏角度 | 高质量数据快速重建 |

### 3.6 Shepp-Logan 体模

经典 10 椭圆体模，模拟头部截面：
```cpp
{ 2.0f,   0, 0,     0.69, 0.92, 0   },   // 外椭圆(颅骨)
{ -0.98f, 0, -0.02, 0.66, 0.87, 0   },   // 内部
{ -0.02f, 0.22, 0,  0.11, 0.31, -18 },   // 右侧结构
...
```

椭圆判定：旋转坐标后检查 $(rx/a)^2+(ry/b)^2 \le 1$，叠加 intensity。

### 3.7 面试高频追问

**Q: 为什么探测器数要 ≥ 图像对角线？**
→ 保证所有角度的射线都能完整穿过图像，否则边缘信息丢失。

**Q: 双线性插值 vs 最近邻？**
→ 最近邻在投影时产生条纹伪影（aliasing），双线性平滑但略模糊。Siddon 方法（精确射线-像素交集面积）更精确但计算量大。

**Q: 零填充为什么是 2n 而不是 n？**
→ 防止循环卷积：滤波是频域乘法=时域循环卷积，零填充到 2n 使循环卷积≈线性卷积。

**Q: FBP 能做低剂量（少角度）吗？**
→ 不能。少角度→频域切片稀疏→重建严重条纹。迭代法（SIRT/ART+TV正则）可以。

**Q: CT 的 HU 值怎么来的？**
→ $\text{HU} = 1000 \times (\mu - \mu_{\text{水}})/\mu_{\text{水}}$。水=0，空气=-1000，骨≈+1000。

---

## 4. PET 重建原理与推导

### 4.1 物理：符合探测

示踪剂衰变发正电子，与电子湮灭产生**一对方向相反的 511 keV γ 光子**。两探测器在纳秒窗内同时触发 → 一条 **LOR**。LOR 按 $(\theta, r)$ 组织成正弦图，点源轨迹为正弦曲线（名称由来）。

$$r = x_0\cos\theta + y_0\sin\theta$$

PET 角度范围 $[0°, 180°)$（环形探测器，对称 LOR 只需半角），CT 则是 $[0°, 360°)$。

### 4.2 正向投影实现细节

PET 的 LOR 投影与 CT 的 Radon 投影数学形式相同，但物理含义不同：
- CT：衰减系数 $\mu$ 的线积分（确定性）
- PET：活度分布 $\lambda$ 的线积分（统计性，泊松噪声）

代码 `projectLOR`：
```cpp
float x = radialOffset * cosA - s * sinA + halfSize - 0.5f;
float y = radialOffset * sinA + s * cosA + halfSize - 0.5f;
sum += bilinearSample(image, size, x, y) * ds;
```

与 CT 的区别：
- PET 用 `radialOffset`（径向偏移）而非 `t`（探测器位置），但数学等价
- PET 角度步进 = π/numAngles，CT = π/numAngles（平行束）或 2π（扇束）

### 4.3 MLEM（最大似然 EM）完整推导

#### 4.3.1 泊松统计模型

PET 每条 LOR 的计数 $y_i$ 服从泊松分布：

$$P(y_i|\bar{y}_i) = \frac{\bar{y}_i^{y_i} e^{-\bar{y}_i}}{y_i!}$$

期望计数 $\bar{y}_i = \sum_j H_{ij}\lambda_j + s_i + r_i$，其中：
- $H_{ij}$：系统矩阵（像素 $j$ 对 LOR $i$ 的贡献概率）
- $\lambda_j$：像素 $j$ 的活度（待求）
- $s_i$：散射符合估计
- $r_i$：随机符合估计

#### 4.3.2 对数似然与 EM 推导

对数似然（忽略常数项）：

$$\mathcal L(\lambda) = \sum_i \left[ y_i\ln\bar{y}_i - \bar{y}_i \right]$$

EM 算法分 E 步和 M 步：

**E 步**：计算隐变量的期望分配比例
$$c_{ij} = \frac{H_{ij}\lambda_j^{(k)}}{\sum_{j'}H_{ij'}\lambda_{j'}^{(k)} + s_i + r_i}$$

**M 步**：最大化似然下界，得到乘法更新：

$$\lambda_j^{(k+1)} = \frac{\lambda_j^{(k)}}{\sum_i H_{ij}} \sum_i H_{ij} \frac{y_i}{\sum_{j'}H_{ij'}\lambda_{j'}^{(k)} + s_i + r_i}$$

#### 4.3.3 代码中的四步实现

```
步骤1: 正向投影 → estimatedSino[i] = Σ_j H_ij·λ_j + additive[i]
步骤2: 比值 → ratio[i] = measured[i] / estimated[i]
步骤3: 反投影比值 → correction[j] = Σ_i H_ij·ratio[i]
步骤4: 乘法更新 → λ_j *= correction[j] / sensitivity[j]
```

灵敏度图预计算（只做一次）：
```cpp
// sensitivity[j] = Σ_i H_ij，即所有 LOR 对像素 j 的贡献之和
for (int a=0; a<numAngles; ++a)
    for (int r=0; r<numRadialBins; ++r)
        backprojectLOR(sensitivity, ..., 1.0f);  // 值=1的反投影
```

**为什么天然非负？**
- 初值 $\lambda_j^{(0)} = 1 > 0$
- 乘法更新：正数×正数=正数
- 即使 ratio 有负值（不可能，因为泊松计数≥0），代码也加了 `if < 0 → 0` 约束

#### 4.3.4 散射/随机校正的加性项

```cpp
if (corrections.scatterCorrection)
    additive[i] += scatterFraction × meanSino;
if (corrections.randomsCorrection)
    additive[i] += randomsRate × meanSino;
```

加性项进入分母：`estimatedSino = proj + additive`，使比值更准确。不加的话，低计数区域比值偏大→重建过亮。

### 4.4 OSEM 详解

#### 4.4.1 子集划分策略

代码采用**交错采样**：
```cpp
for (int a = subset; a < numAngles; a += numSubsets)
    subsetAngles.push_back(a);
// subset=0: 角度 0, 12, 24, ...  (均匀覆盖)
// subset=1: 角度 1, 13, 25, ...
```

为什么不用连续块？→ 连续块角度相近，子集内信息冗余，收敛不稳定。交错保证每个子集角度均匀分布。

#### 4.4.2 子集灵敏度

每个子集有自己的灵敏度图（只含该子集角度的贡献）：
```cpp
for (int a : subsetAngles)
    for (int r=0; r<numRadialBins; ++r)
        backprojectLOR(subSensitivity, ..., 1.0f);
```

更新时除以子集灵敏度而非全局灵敏度，否则会欠校正。

#### 4.4.3 OSEM 收敛特性

| 性质 | 说明 |
|------|------|
| 加速比 | 1次OSEM(S子集) ≈ S次MLEM |
| 收敛极限 | 不严格收敛到全局最大似然，在极限附近振荡 |
| 子集数选择 | 通常 8–16，太多→振荡加剧，太少→加速不够 |
| 临床标准 | 2–4次OSEM(16子集) ≈ 32–64次MLEM，足够临床质量 |

### 4.5 衰减校正详解

511 keV γ 光子穿越人体时被吸收（康普顿散射+光电效应），衰减因子：

$$A(\theta, r) = \exp\left(-\int_{\text{LOR}} \mu(x,y)\,ds\right)$$

代码实现：
```cpp
float sumMu = 0.0f;
for (float s = -sMax; s <= sMax; s += 1.0f)
    sumMu += bilinearSample(attenMap, size, x, y);  // 沿 LOR 积分 μ
return std::exp(-sumMu);  // e^{-∫μds}
```

衰减图 $\mu$ 通常由 CT 提供（CT 值→μ 转换：$\mu = \mu_{\text{水}} \times (1 + \text{HU}/1000)$）。

**不校正的后果**：深部结构（如肝脏）活度被低估，因为 γ 光子穿过的组织更多→衰减更大。

### 4.6 PSF 建模（分辨率恢复)

探测器有限空间分辨率→点扩散函数（PSF）≈高斯。在 MLEM 中对当前估计做高斯平滑：

```cpp
if (corrections.psfModeling) {
    float sigma = corrections.psfFWHM / 2.355f;  // FWHM → σ
    applyGaussianSmooth(result.image, outputSize, outputSize, pixelSigma);
}
```

高斯平滑实现：可分离（X→Y 两通道），核大小 = `ceil(3σ)×2+1`。

**效果**：边缘更锐利（部分补偿分辨率损失），但过度 PSF 建模→边缘过增强（"热边缘"伪影）。

### 4.7 体模设计原理

| 体模 | 目的 | 设计 |
|------|------|------|
| **Derenzo** | 评估空间分辨率 | 6组不同直径热点(0.14→0.04)，间距=2×直径，能分辨的最小热点=分辨率极限 |
| **NEMA IEC** | 评估对比度恢复 | 4热球(高摄取肿瘤)+2冷球(坏死区)，背景椭圆扁率0.8 |
| **均匀圆柱** | 评估噪声/均匀性 | R=0.8均匀圆，用于计算%噪声和均匀性指数 |

Derenzo 六角排列：奇数行偏移 `spacing×0.5`，模拟真实热点排列。

### 4.8 本工程 3D PET 重建

#### 4.8.1 逐切片策略

```cpp
for (int z = 0; z < numSlices; ++z) {
    auto sliceResult = reconstruct(sinogram3D.slices[z], ...);
    sliceResult.normalize();
    std::copy(sliceResult.image.begin(), ..., volume.begin() + z*sizeXY*sizeXY);
}
```

每个切片独立重建→堆叠成 3D 体积。这是 **2D slice-by-slice** 模式。

#### 4.8.2 3D 体模：球截面计算

球形热点在切片 $z$ 处的截面半径：

$$r_{2d} = \sqrt{r^2 - \Delta z^2}$$

代码：
```cpp
float dz = pz - s.cz;
if (std::abs(dz) >= s.radius) continue;  // 该切片不在球内
float r2d = std::sqrt(s.radius*s.radius - dz*dz);
```

背景用 3D 椭球：$px^2/R^2 + py^2/(0.8R)^2 + pz^2 \le 1$。

#### 4.8.3 与真 3D PET 的差距

| | 本工程(逐切片) | 真 3D PET |
|---|---|---|
| LOR | 仅平面内 | 含斜 LOR（跨切片） |
| 正弦图 | 每切片独立 | 3D 正弦图(oblique sinogram) |
| 灵敏度 | 2D 灵敏度图 | 3D 灵敏度体积 |
| 优势 | 实现简单、可复用 2D 代码 | 跨切片信息利用→更高信噪比 |

### 4.9 泊松噪声模拟

```cpp
if (lambda < 50.0f) {
    std::poisson_distribution<int> poisson(lambda);  // 精确泊松
    v = poisson(rng) / scaleFactor;
} else {
    std::normal_distribution<float> normal(lambda, sqrt(lambda));  // 正态近似
    v = max(normal(rng), 0.0f) / scaleFactor;
}
```

**为什么 lambda>50 用正态近似？**
→ 泊松分布在大 λ 时趋近正态（中心极限定理），且 `std::poisson_distribution` 在大 λ 时计算开销大。

### 4.10 PET 面试高频追问

**Q: PET 和 CT 正弦图的区别？**
→ PET 半角[0,π)（环形探测器对称），CT 全角[0,2π)（旋转源）；PET 统计噪声（泊松），CT 电子噪声（近似高斯）。

**Q: MLEM 为什么比 FBP 好？**
→ MLEM 建模泊松统计→低计数区域更准确；可融入衰减/散射/PSF；天然非负。FBP 放大高频噪声。

**Q: OSEM 的极限问题？**
→ OSEM 不收敛到全局最大似然解，在极限附近振荡。解决方案：最后几次用 MLEM "收尾"，或降低子集数。

**Q: 衰减校正为什么必须？**
→ 不校正→深部活度低估（γ 被更多组织吸收），定量分析（SUV）完全不可靠。

**Q: SUV 是什么？**
→ Standardized Uptake Value = 组织活度/(注射剂量/体重)。用于肿瘤良恶性判断，SUV>2.5 通常怀疑恶性。

---

## 5. CUDA 加速重建

### 5.1 GPU 并行策略

#### 5.1.1 反投影并行化

反投影是"每个像素独立累加所有角度的贡献"——天然并行：

```
CPU: for a→for y→for x:  image[y*size+x] += val × Δθ   (串行)
GPU: 每像素一个线程:      image[tid] += val × Δθ         (并行)
```

代码接口：
```cpp
static void gpuBackProject(
    const float* sinogram, int numAngles, int numDetectors,
    float* image, int imageSize,
    float angleStart, float angleEnd);
```

每个 CUDA block 处理 16×16 像素区域，线程映射 `(x, y) → threadIdx`。

#### 5.1.2 正向投影并行化

正向投影是"每条射线独立积分"——也可并行，但需要原子操作写回正弦图：

```
GPU: 每条射线一个线程 → 沿 s 方向累加 → atomicAdd 写入 sino[a*numDet+d]
```

原子操作是瓶颈：多条射线可能写同一正弦图位置。优化策略：
- **共享内存**：block 内射线共享中间结果
- **纹理缓存**：图像数据绑定到 CUDA texture→自动缓存+硬件插值

#### 5.1.3 cuFFT 做 Ramp 滤波

```cpp
static void gpuApplyRampFilter(
    float* sinogram, int numAngles, int numDetectors,
    int filterType);
```

批量 FFT：所有角度的投影一次性送入 cuFFT，比 CPU Cooley-Tukey 快 10–50×。

#### 5.1.4 PET MLEM/OSEM GPU 实现

每次迭代需要正投影+反投影各一次：

```
MLEM GPU 迭代:
  1. gpuForwardProject(image → estimatedSino)     // 正投影 kernel
  2. GPU kernel: ratio = measured / estimated      // 逐元素 kernel
  3. gpuBackProject(ratio → correction)            // 反投影 kernel
  4. GPU kernel: image *= correction / sensitivity  // 逐元素 kernel
```

OSEM 增加子集角度索引数组，每次只处理子集角度。

### 5.2 性能对比与瓶颈分析

| 操作 | CPU 时间 | GPU 时间 | 加速比 |
|------|----------|----------|--------|
| CT FBP 256² 360角 | ~2s | ~50ms | ~40× |
| PET MLEM 128² 10迭代 | ~30s | ~500ms | ~60× |
| FFT 滤波 360行 | ~200ms | ~5ms | ~40× |

**瓶颈排序**：
1. **访存带宽**：正/反投影需要读整个图像/正弦图，GPU 内存带宽是限制
2. **原子操作冲突**：正投影写正弦图时多线程竞争同一地址
3. **主机-设备数据传输**：PCIe 带宽 ~12GB/s，大正弦图传输耗时

**优化手段**：
- 纹理缓存（图像绑定 texture object）
- 共享内存（block 内共享射线参数）
- CUDA event 计时（排除 memcpy）
- 流式并行（正投影+滤波 overlap）

### 5.3 CUDA 面试高频追问

**Q: 为什么反投影比正投影更容易并行？**
→ 反投影是"读正弦图→写图像"，每像素独立写；正投影是"读图像→写正弦图"，多射线写同一位置需原子操作。

**Q: 原子操作怎么优化？**
→ (1) 共享内存聚合 block 内写；(2) 每线程写独立位置再合并；(3) 用 float atomicAdd（CUDA 内建）。

**Q: cuFFT 和手写 FFT 的区别？**
→ cuFFT 是 NVIDIA 优化的库，自动选择最优蝶形策略、利用共享内存和 warp 级并行。手写 Cooley-Tukey 基2 只适合教学/小规模。

**Q: GPU 重建精度和 CPU 一致吗？**
→ 单精度 float 有 ~7 位有效数字，对医学影像足够（HU 精度±1）。但迭代法累积误差可能略不同，临床需验证。

**Q: 多 GPU 怎么做？**
→ 角度分到不同 GPU（数据并行），或切片分到不同 GPU（PET 3D）。需要跨 GPU 同步和通信。

---

## 6. 体绘制（光线投射）与 MPR

### Ray Casting
相机发射光线穿过 3D 纹理，等距采样，传输函数 TF 映射 (R,G,B,A)，**前到后合成**：
$$C=C+(1-A)\,\alpha_i c_i,\;A=A+(1-A)\alpha_i$$
优化：早停、抖动去环、梯度光照、AO、裁剪面。后处理 FXAA 抗锯齿。

### MPR
3D 纹理切轴向/冠状/矢状面，四视口、十字线联动、独立窗宽窗位。

---

## 7. 图像配准

### 7.1 配准问题定义

配准 = 找空间变换 $T$ 使浮动图像 $F$ 与参考图像 $R$ 对齐：

$$T^* = \arg\min_T \mathcal D(R, T(F)) + \mathcal S(T)$$

- $\mathcal D$：相似性度量（MSE/MI/NCC）
- $\mathcal S$：正则化（平滑约束/拓扑约束）

### 7.2 刚性配准（6 参数）

#### 7.2.1 变换模型

6 个参数：3 旋转 $(\alpha,\beta,\gamma)$ + 3 平移 $(t_x,t_y,t_z)$：

$$T(\mathbf{x}) = R(\alpha,\beta,\gamma)\mathbf{x} + \mathbf{t}$$

旋转矩阵 $R$ 用欧拉角（ZYX 顺序）分解。

#### 7.2.2 有限差分梯度下降

代码不解析求导，而是用**中心差分**估计梯度：

```cpp
float eps = 0.01f;
for (int p=0; p<6; ++p) {
    RigidParam3D paramPlus = current; paramPlus.params[p] += eps;
    RigidParam3D paramMinus = current; paramMinus.params[p] -= eps;
    float costPlus = computeMSE(R, applyRigid(F, paramPlus));
    float costMinus = computeMSE(R, applyRigid(F, paramMinus));
    gradient[p] = (costPlus - costMinus) / (2*eps);
}
```

更新：`params[p] -= step × gradient[p]`，步长 `step` 逐迭代递减（步长松弛）。

#### 7.2.3 多分辨率金字塔

```
Level 3 (64³):  大步长、粗搜索 → 快速逼近全局最优
Level 2 (128³): 中步长、精搜索
Level 1 (256³): 小步长、微调 → 精确对齐
```

每层先对图像做 3D 高斯平滑再下采样。高斯核 $\sigma$ 随层数增大，抑制高频细节避免局部极值。

**面试要点**：多分辨率是配准的核心策略——先粗后细，避免陷入局部极值，同时加速收敛。

### 7.3 Demons 配准（Thirion 光学流）

#### 7.3.1 力公式推导

Thirion 从光学流方程 $I(x,t+\Delta t)=I(x+\mathbf{u},t)$ 出发，假设亮度恒定：

$$\nabla I \cdot \mathbf{u} + \frac{\partial I}{\partial t} = 0$$

令 $\frac{\partial I}{\partial t} = R(x) - F(x)$（参考-浮动差），得位移：

$$\mathbf{u} = \frac{(R-F)\nabla F}{|\nabla F|^2 + (R-F)^2}$$

分母加 $(R-F)^2$ 是 Thirion 的稳定化：当梯度为零时避免除零，同时限制位移大小。

代码实现：
```cpp
float diff = R[idx] - F_warped[idx];
float gx = gradFx[idx], gy = gradFy[idx], gz = gradFz[idx];
float gradSq = gx*gx + gy*gy + gz*gz;
float denom = gradSq + diff*diff;
if (denom > 1e-10f) {
    deformX[idx] += diff * gx / denom;
    deformY[idx] += diff * gy / denom;
    deformZ[idx] += diff * gz / denom;
}
```

#### 7.3.2 流场平滑

每步迭代后对变形场做高斯平滑（正则化 $\mathcal S$）：

```cpp
gaussianSmooth3D(deformX, w, h, d, sigma);
gaussianSmooth3D(deformY, w, h, d, sigma);
gaussianSmooth3D(deformZ, w, h, d, sigma);
```

$\sigma$ 控制变形场平滑度：大→全局变形（粗配准），小→局部变形（精配准）。

### 7.4 LCC-Demons（局部互相关力）

#### 7.4.1 为什么需要 LCC？

Demons 力假设亮度恒定，但多模态配准（CT-MRI）亮度关系非线性。LCC 用**局部互相关**替代亮度差：

$$\text{LCC}(x) = \frac{\sum_{\Omega_x}(R-\bar R)(F_w-\bar F_w)}{\sqrt{\sum_{\Omega_x}(R-\bar R)^2 \cdot \sum_{\Omega_x}(F_w-\bar F_w)^2}}$$

$\Omega_x$ 是以 $x$ 为中心的局部窗口。

#### 7.4.2 LCC 力推导

最大化 LCC → 求 $\frac{\partial \text{LCC}}{\partial \mathbf{u}}$：

$$\mathbf{f}_{\text{LCC}} = \frac{1}{\sigma_R^2 \sigma_F^2} \left[ (F_w - \bar F_w)\nabla R - \frac{\text{LCC}}{\sigma_R^2}(R-\bar R)\nabla F_w \right]$$

代码中分两步计算：
1. 局部均值/方差（box filter 快速计算）
2. 组合梯度得到力向量

**面试要点**：LCC-Demons 是多模态配准的标准方法，比互信息(MI)更高效（无需联合直方图），且力场更平滑。

### 7.5 多网格 Demons（MG-Demons）

递归多网格策略加速收敛：

```
Level 3 (粗): 大 σ, 大步 → 快速逼近全局变形
Level 2 (中): 中 σ, 中步
Level 1 (细): 小 σ, 小步 → 精细局部变形
```

每层继承上一层的变形场作为初始值（coarse-to-fine）。

### 7.6 FFD（自由变形，B 样条）

#### 7.6.1 B 样条基函数

三次 B 样条由 4 个基函数构成：

$$B_0(u)=(1-u)^3/6,\quad B_1(u)=(3u^3-6u^2+4)/6$$
$$B_2(u)=(-3u^3+3u^2+3u+1)/6,\quad B_3(u)=u^3/6$$

变形由控制点网格 $\mathbf{c}_{i,j,k}$ 驱动：

$$T(x,y,z) = \sum_{i,j,k} B_i(u)B_j(v)B_k(w) \cdot \mathbf{c}_{i+l,j+m,k+n}$$

$(l,m,n)$ 是 $(x,y,z)$ 所在的控制点网格单元索引，$(u,v,w)$ 是单元内局部坐标。

#### 7.6.2 解析梯度推导

MSE 目标函数对控制点的梯度：

$$\frac{\partial \mathcal D}{\partial \mathbf{c}_{i,j,k}} = 2 \sum_{\mathbf{x}} (F_w(\mathbf{x})-R(\mathbf{x})) \cdot \nabla F_w(\mathbf{x}) \cdot B_i(u)B_j(v)B_k(w)$$

代码实现：
```cpp
// 对每个像素，计算其对哪些控制点有贡献
float residual = warpedF[idx] - R[idx];
float gx = gradWarpedFx[idx], gy = gradWarpedFy[idx], gz = gradWarpedFz[idx];
for (int di=0; di<4; ++di)
    for (int dj=0; dj<4; ++dj)
        for (int dk=0; dk<4; ++dk) {
            float w = B[di](u) * B[dj](v) * B[dk](w);
            gradCx[ci+di] += residual * gx * w;
            gradCy[cj+dj] += residual * gy * w;
            gradCz[ck+dk] += residual * gz * w;
        }
```

**面试要点**：解析梯度比有限差分快 ~100×（6 参数→3×N_control 点），且更精确。

#### 7.6.3 折叠防止

变形场可能产生拓扑破坏（折叠），通过监控**Jacobian 行列式**：

$$J_T(\mathbf{x}) = \det\begin{pmatrix} \partial T_x/\partial x & \partial T_x/\partial y & \partial T_x/\partial z \\ \partial T_y/\partial x & \cdots & \cdots \\ \partial T_z/\partial x & \cdots & \cdots \end{pmatrix}$$

$J>0$ → 拓扑保持；$J<0$ → 折叠。代码中检测 $J<0$ 时惩罚或回退。

### 7.7 配准面试高频追问

**Q: 刚性 vs 非刚性配准的选择？**
→ 同一患者同模态→刚性（6参数足够）；不同模态/不同患者/术前术后→非刚性（Demons/FFD）。

**Q: Demons 和 FFD 的区别？**
→ Demons 是密集变形场（每像素一个位移），FFD 是稀疏控制点驱动（参数少→更平滑→不易折叠）。Demons 快但可能拓扑破坏，FFD 慢但拓扑可控。

**Q: 多模态配准为什么不能用 MSE？**
→ CT 和 MRI 亮度关系非线性，MSE 假设线性关系→失效。用 MI（统计相关）或 LCC（局部线性近似）。

**Q: 配准验证怎么做？**
→ (1) 手动标记点对→计算 TRE；(2) 逆向一致性检验（$T_{AB} \circ T_{BA} \approx I$）；(3) Jacobian 行列式检查拓扑。

**Q: MI 的原理？**
→ 互信息 $I(A,B) = H(A)+H(B)-H(A,B)$，最大化→两图像联合分布最集中→最相关。计算需联合直方图，开销大。

---

## 8. 分割算法（传统 + 深度学习）

### 8.1 Chan-Vese（水平集能量最小化）

#### 8.1.1 能量函数推导

Chan-Vese 模型（Mumford-Shah 的特例）将图像分为内外两区域，各自用常数 $c_1, c_2$ 近似：

$$E(c_1,c_2,\phi) = \mu\cdot\text{Length}(C) + \nu\cdot\text{Area}(\text{inside}) + \lambda_1\int_{\text{inside}}(I-c_1)^2 + \lambda_2\int_{\text{outside}}(I-c_2)^2$$

水平集 $\phi$：$\phi>0$ = 内部，$\phi<0$ = 外部，$\phi=0$ = 边界。

#### 8.1.2 演化方程

对 $\phi$ 求变分导数，得到水平集演化 PDE：

$$\frac{\partial \phi}{\partial t} = \delta(\phi)\left[\mu\cdot\text{div}\left(\frac{\nabla\phi}{|\nabla\phi|}\right) - \nu - \lambda_1(I-c_1)^2 + \lambda_2(I-c_2)^2\right]$$

代码实现：
```cpp
float c1 = meanInside, c2 = meanOutside;  // 区域均值
float curvature = divNormalPhi / absGradPhi;  // 曲率 = div(n̂)
float dphi = deltaEpsilon(phi[idx]) * (
    mu * curvature - nu - lambda1*(I[idx]-c1)*(I[idx]-c1) + lambda2*(I[idx]-c2)*(I[idx]-c2)
);
phi[idx] += dt * dphi;
```

#### 8.1.3 窄带法 + Sussman 重初始化

- **窄带法**：只更新 $|\phi|<\text{bandWidth}$ 的像素，节省计算
- **Sussman 重初始化**：每几步将 $\phi$ 重设为符号距离函数 $|\nabla\phi|=1$：

$$\phi^{(n+1)} = \phi^{(n)} - \Delta t \cdot \text{sign}(\phi_0)(|\nabla\phi|-1)$$

- **CFL 条件**：$\Delta t \le \frac{1}{2d}$（$d$=维度），保证数值稳定

### 8.2 DRLSE（距离正则化水平集）

#### 8.2.1 双井势函数

DRLSE 用**双井势** $p(s)$ 替代传统水平集的惩罚项，避免重初始化：

$$p(s) = \begin{cases} \frac{1}{2\pi^2}(1-\cos(\pi s)) & s \le 1 \\ \frac{1}{2}(s-1)^2 & s > 1 \end{cases}$$

- $s \le 1$：井在 $s=0$（$\phi=0$ 边界附近保持 $|\nabla\phi|$ 小）
- $s > 1$：井在 $s=1$（远离边界处 $|\nabla\phi|=1$，保持符号距离特性）

#### 8.2.2 演化方程

$$\frac{\partial\phi}{\partial t} = \mu\cdot\text{div}(d_p(|\nabla\phi|)\nabla\phi) + \lambda\cdot\delta(\phi)\cdot\text{div}\left(\frac{g\nabla\phi}{|\nabla\phi|}\right) + \alpha\cdot g\cdot\delta(\phi)$$

其中 $g = 1/(1+|\nabla G_\sigma*I|^2)$ 是边缘指示函数，$d_p$ 是势函数的导数。

**面试要点**：DRLSE 不需要周期性重初始化（双井势自动维持 $|\nabla\phi|$ 的正确行为），比传统 Chan-Vese 更稳定。

### 8.3 Snake + GVF（梯度向量场）

#### 8.3.1 传统 Snake

Snake 是参数化曲线 $\mathbf{v}(s)=(x(s),y(s))$，能量：

$$E = \int \alpha|\mathbf{v}'|^2 + \beta|\mathbf{v}''|^2 - \gamma|\nabla I|^2\,ds$$

- $\alpha$：弹性（长度惩罚）
- $\beta$：刚性（曲率惩罚）
- $\gamma$：外部力（吸引到边缘）

#### 8.3.2 GVF 扩散

传统 Snake 的 $\nabla I$ 只在强边缘处有力，远处为零→初始化必须靠近目标。GVF 将边缘力扩散到全图：

$$\mathbf{f}_{\text{GVF}} = \mu\nabla^2\mathbf{f} - (\mathbf{f}-\nabla I)|\nabla I|^2$$

迭代求解（扩散方程）：
```cpp
for (int iter=0; iter<GVFIterations; ++iter) {
    u[idx] = u[idx] + mu*(laplacianU) - (u[idx]-edgeU[idx])*edgeMagSq[idx];
    v[idx] = v[idx] + mu*(laplacianV) - (v[idx]-edgeV[idx])*edgeMagSq[idx];
}
```

**面试要点**：GVF 的核心优势是**捕获范围大**——初始化远离目标也能收敛。代价是计算开销（需迭代扩散）。

### 8.4 GraphCut（最大流最小割）

#### 8.4.1 能量模型

$$E(L) = \sum_p D_p(L_p) + \sum_{p,q\in\mathcal N} V_{p,q}(L_p,L_q)$$

- $D_p$：数据项（像素 $p$ 属于前景/背景的代价）
- $V_{p,q}$：平滑项（相邻像素标签不同的代价，通常 $V=\lambda\cdot e^{-|I_p-I_q|^2/\sigma^2}$）

#### 8.4.2 Edmonds-Karp BFS 最大流

代码实现 Edmonds-Karp 算法（BFS 找增广路径）：

```cpp
while (bfsFindPath(source, sink, parent)) {
    // 找路径最小残余容量
    float pathFlow = INF;
    for (v=sink; v!=source; v=parent[v])
        pathFlow = min(pathFlow, capacity[parent[v]][v] - flow[parent[v]][v]);
    // 更正流
    for (v=sink; v!=source; v=parent[v]) {
        flow[parent[v]][v] += pathFlow;
        flow[v][parent[v]] -= pathFlow;  // 反向流
    }
    maxFlow += pathFlow;
}
```

**面试要点**：GraphCut 保证全局最优（最大流=最小割），但只适用于二值标签。多标签需 α-expansion 迭代。

### 8.5 区域生长

#### 8.5.1 连通性

- 6 连通（面相邻）：上下左右前后 6 个邻居
- 26 连通（面+边+角相邻）：3D 中所有 26 个邻居

#### 8.5.2 三种判定准则

| 准则 | 公式 | 适用 |
|------|------|------|
| **绝对差** | $|I(q)-I(seed)| < T$ | 均匀区域 |
| **均值差** | $|I(q)-\bar I_{region}| < T$ | 渐变区域 |
| **统计** | $|I(q)-\mu| < k\cdot\sigma$ | 噪声区域 |

代码支持 6/26 连通 + 三准则组合。

### 8.6 DenseCRF（全连接条件随机场）

#### 8.6.1 能量模型

$$E(x) = \sum_i \psi_u(x_i) + \sum_{i<j} \psi_p(x_i,x_j)$$

- $\psi_u$：一元势（来自 CNN/UNet 的 softmax 输出）
- $\psi_p$：二元势：$\psi_p(x_i,x_j) = w_1 e^{-|p_i-p_j|^2/\sigma_\alpha^2} + w_2 e^{-|p_i-p_j|^2/\sigma_\beta^2 - |I_i-I_j|^2/\sigma_\gamma^2}$

#### 8.6.2 Permutohedral lattice O(N·d) 推理

传统全连接 CRF 推理是 O(N²)，Permutohedral lattice 将其降到 **O(N·d)**：

```
步骤1: Splat — 将 N 个消息投影到高维 lattice 点
步骤2: Blur  — lattice 上做高斯模糊（卷积=消息传播）
步骤3: Slice — 从 lattice 取回每个像素的消息
```

代码：
```cpp
permutohedralLattice.splat(unaryPotentials);   // 投影
permutohedralLattice.blur();                    // 模糊=传播
auto result = permutohedralLattice.slice();     // 取回
```

**面试要点**：DenseCRF 是 CNN 后处理的标准方法，将粗糙分割细化到像素级。Permutohedral lattice 是核心加速技巧。

### 8.7 深度学习分割

#### 8.7.1 UNet2D

标准 UNet：编码器(4层下采样)→瓶颈→解码器(4层上采样+跳跃连接)。损失 = Dice + CrossEntropy：

$$\mathcal L = -\frac{2\sum_i p_i g_i}{\sum_i p_i + \sum_i g_i} + -\sum_i g_i\log p_i$$

#### 8.7.2 3D UNet 血管分割

- 输入：3D patch (32×32×32)
- 混合精度训练（AMP）：前半 FP16→加速，梯度 FP32→稳定
- 推理：滑窗 + Gaussian 融合（重叠区域加权平均）

#### 8.7.3 VoxelMorph 配准网络

用 CNN 学习变形场 $T$，输入 $(F, R)$ → 输出 $\mathbf{u}$。损失 = MSE + 平滑正则。比 Demons 快 10×（一次前向传播 vs 多次迭代）。

### 8.8 分割面试高频追问

**Q: Chan-Vese 和 Snake 的区别？**
→ Chan-Vese 是水平集（拓扑自适应，可分裂合并），Snake 是参数化曲线（拓扑固定）。Chan-Vese 不需要初始化靠近目标。

**Q: GraphCut 为什么全局最优？**
→ 最大流最小割定理保证。但只适用于二值标签，多标签需 α-expansion（近似最优）。

**Q: DenseCRF 和普通 CRF 的区别？**
→ 普通 CRF 只连接 4/8 邻域（局部），DenseCRF 连接所有像素对（全局）→长距离依赖。Permutohedral lattice 使推理从 O(N²)→O(N·d)。

**Q: UNet 跳跃连接的作用？**
→ 保留编码器的高频细节（边缘/纹理），避免解码器只输出模糊结果。

**Q: Dice loss 的梯度问题？**
→ 当预测和真值重叠很小时，Dice 梯度可能不稳定（分母小）。解决方案：Dice+CE 组合，CE 提供稳定梯度。

---

## 9. 深度学习与部署（TensorRT）

训练(PyTorch)→ONNX(opset17)→TRT engine + meta.json。C++ 用 nvinfer 推理，滑窗+高斯融合，TRT 失败回退 ONNXRuntime。前后处理：床板去除、HU 体、二值化导出 raw/json 给 C++。

---

## 10. 高频面试问答

### CT/PET/CUDA 方向

**Q: FBP 为什么要滤波？**
→ 直接反投影 = 卷 $1/r$ 模糊核（低频过采样），Ramp 滤波器 $|\omega|$ 补偿极坐标 Jacobian，恢复正确频率权重。不加→星形伪影。

**Q: 五种滤波器怎么选？**
→ Ram-Lak 最锐利但噪声最大；Shepp-Logan 平衡（临床常用）；Hann 最平滑但分辨率损失。选择取决于噪声水平和分辨率需求。

**Q: MLEM 为何天然非负？**
→ 乘法更新：$\lambda^{(k+1)} = \lambda^{(k)} \times (\text{正数}/\text{正数})$，正初值→正结果→永远非负。FBP 可能出现负值（噪声+滤波器旁瓣）。

**Q: OSEM 提速代价？**
→ 不严格收敛到全局最大似然解，在极限附近振荡。子集数越多加速越大但振荡越严重。解决方案：最后几次用 MLEM 收尾。

**Q: 2D vs 3D PET？**
→ 本工程逐切片（slice-by-slice），只利用平面内 LOR。真 3D 需斜 LOR（跨切片），信息利用更充分→更高信噪比，但系统矩阵更大→计算量指数增长。

**Q: 衰减校正为什么必须？**
→ 不校正→深部活度低估（γ 被更多组织吸收），SUV 定量分析完全不可靠。衰减因子 $A=e^{-\int\mu ds}$，$\mu$ 由 CT 提供。

**Q: CUDA 反投影为什么比正投影更容易并行？**
→ 反投影是"读正弦图→写图像"，每像素独立写无冲突；正投影是"读图像→写正弦图"，多射线写同一位置需 atomicAdd→冲突瓶颈。

**Q: 原子操作怎么优化？**
→ (1) 共享内存聚合 block 内写；(2) 每线程写独立位置再合并；(3) 用 float atomicAdd（CUDA 内建，比 int atomicAdd 快）。

**Q: cuFFT 和手写 FFT？**
→ cuFFT 自动选择最优蝶形策略、利用共享内存和 warp 级并行，比手写 Cooley-Tukey 基2 快 10–50×。手写只适合教学/小规模。

**Q: GPU 重建精度？**
→ 单精度 float ~7 位有效数字，对医学影像足够（HU±1）。但迭代法累积误差可能略不同，临床需验证与 CPU 结果的一致性。

### 配准方向

**Q: 刚性 vs 非刚性？**
→ 同一患者同模态→刚性（6参数足够）；不同模态/不同患者/术前术后→非刚性（Demons/FFD）。刚性保证拓扑，非刚性允许局部变形。

**Q: Demons 力公式推导？**
→ 光学流方程 $\nabla I \cdot \mathbf{u} + (R-F)=0$，解出 $\mathbf{u}=(R-F)\nabla F/(|\nabla F|^2+(R-F)^2)$。分母加 $(R-F)^2$ 防除零+限位移。

**Q: LCC-Demons 为什么适合多模态？**
→ Demons 假设亮度恒定（单模态），LCC 用局部互相关替代→允许非线性亮度关系→CT-MRI 配准。

**Q: FFD 解析梯度？**
→ $\partial D/\partial c = 2\sum (F_w-R)\nabla F_w \cdot B_i(u)B_j(v)B_k(w)$，比有限差分快 ~100×且更精确。每个像素只影响 4×4×4=64 个控制点。

**Q: 折叠如何防？**
→ Jacobian 行列式 $J>0$ →拓扑保持，$J<0$ →折叠。检测到折叠时惩罚或回退控制点。FFD 天然比 Demons 更不易折叠（稀疏控制点→全局平滑）。

**Q: 多分辨率为什么重要？**
→ 先粗后细避免局部极值。粗层大步长快速逼近全局最优，细层小步长精确微调。没有多分辨率→容易卡在局部极值。

### 分割方向

**Q: Chan-Vese 和 Snake？**
→ Chan-Vese 是水平集（拓扑自适应，可分裂合并），Snake 是参数化曲线（拓扑固定）。Chan-Vese 不需要初始化靠近目标。

**Q: DRLSE 双井势？**
→ $p(s)$ 在 $s=0$ 和 $s=1$ 处有两个井：边界附近保持 $|\nabla\phi|$ 小，远离边界保持 $|\nabla\phi|=1$。自动维持符号距离特性，不需要周期性重初始化。

**Q: GVF 为什么捕获范围大？**
→ 将边缘力 $\nabla I$ 扩散到全图（迭代扩散方程），远处也有力→初始化远离目标也能收敛。代价是计算开销。

**Q: GraphCut 全局最优？**
→ 最大流最小割定理保证二值标签全局最优。多标签需 α-expansion（近似最优，保证在最优的 2 倍以内）。

**Q: DenseCRF 加速原理？**
→ Permutohedral lattice：splat(投影)→blur(模糊=消息传播)→slice(取回)，将 O(N²) 降到 O(N·d)。核心思想：高维空间中离散化→卷积代替逐对计算。

**Q: UNet 跳跃连接？**
→ 保留编码器高频细节（边缘/纹理），避免解码器只输出模糊结果。本质是特征融合：低分辨率语义 + 高分辨率细节。

**Q: Dice loss 梯度问题？**
→ 当预测和真值重叠很小时，Dice 梯度不稳定（分母小→梯度爆炸）。解决方案：Dice+CE 组合，CE 提供稳定梯度。

### 通用/架构方向

**Q: 传输函数？**
→ 标量→RGBA 映射，决定体渲染可视化。线性段+控制点，opacity 控制透明度→决定哪些值可见。

**Q: TRT 比 PyTorch 快为何？**
→ 算子融合（Conv+BN+ReLU→单 kernel）+ FP16/INT8 量化 + kernel autotune（选择最优 kernel 参数）+ 内存优化（减少中间缓冲区）。

**Q: 项目最大难点？**
→ FFT 位逆序与频移对齐（零填充+中心化）、MLEM 灵敏度除零（加 epsilon）、3D 内存管理（16切片×128²×float）、TRT 版本兼容（opset17+FP16）。

**Q: 项目亮点？**
→ 物理→数值→GPU→AI 全闭环；FBP/MLEM 手撕未调库；CUDA 加速实测数十倍；配准5种算法全覆盖；分割传统+DL双路线。

---

## 11. 项目亮点与难点话术

亮点：物理→数值→GPU→AI 全闭环；FBP/MLEM 手撕未调库；CUDA 加速实测。
难点：FFT 位逆序与频移对齐、MLEM 灵敏度除零、3D 内存、TRT 版本兼容。
量化：CT FBP 256² 实时；PET 3D 16 切片 OSEM；CUDA 数十倍。

---

## 12. 一句话速答清单

- Radon=线积分；FBP=Ramp+反投影；SIRT 抗噪；MLEM=泊松 EM 乘法非负；OSEM=子集提速；CUDA 像素并行；TF=标量转 RGBA；Demons=光流力；FFD=B 样条；TRT=融合+量化。
