# 刚体配准全流程详解

> 本文档对应代码：`include/registration.hpp` §3-§5 + `src/main_registration.cpp` 中的 `runRigid()`

---

## 一、全局视角：刚体配准在做什么？

### 1.1 问题定义

给定两张 3D 医学图像：
- **Fixed（参考图）**：CT 体数据 `g_ref`，尺寸 `W×H×D`
- **Moving（浮动图）**：PET 体数据 `g_mov`，尺寸相同

目标：找到一组**刚体变换参数** `p = (tx, ty, tz, rx, ry, rz)`，使得将 Moving 变换后与 Fixed 尽可能"相似"。

```
┌─────────────┐         刚体变换 T(p)         ┌─────────────┐
│  Moving     │  ──────────────────────────►  │  ≈ Fixed    │
│  (PET)      │     旋转 R + 平移 t           │  (CT)       │
└─────────────┘                                └─────────────┘
```

### 1.2 "刚体"的含义

刚体变换 = **旋转 + 平移**，不包含缩放、剪切、形变。物理含义：物体在空间中移动但不发生形变。

6 个自由度（6-DOF）：

| 参数 | 含义 | 单位 |
|------|------|------|
| `tx, ty, tz` | 沿 X/Y/Z 轴的平移 | 体素（voxel） |
| `rx, ry, rz` | 绕 X/Y/Z 轴的旋转 | 弧度（radian） |

> **为什么 CT-PET 用刚体？** 同一患者的 CT 和 PET 通常在同一床位采集，头部/躯干的骨骼结构相对刚性。刚体配准是第一步，先做大致对齐，后续再用 Demons/FFD 做局部形变精化。

---

## 二、调用入口：`runRigid()`

```cpp
// src/main_registration.cpp
static void runRigid(int panelIdx, bool useNMI) {
    // ① 配置参数
    MedReg::RigidRegConfig3D cfg;
    cfg.useNMI = useNMI;       // 选择相似度度量
    cfg.numLevels = 2;         // 金字塔层数
    cfg.maxIterations = 120;   // 每层最大迭代
    cfg.maxStep = 3.f;         // 初始步长
    cfg.minStep = 0.008f;      // 收敛阈值
    cfg.relaxFactor = 0.95f;   // 步长衰减

    // ② 执行配准，得到最优参数
    auto p = MedReg::rigidRegistration3D(g_ref, g_mov, cfg);

    // ③ 用最优参数重采样 Moving → 对齐结果
    auto result = MedReg::applyRigid3D(g_mov, g_ref, p);

    // ④ 计算配准后度量
    float mse = MedReg::computeMSE3D(g_ref, result);
    float nmi = MedReg::computeNMI3D(g_ref, result);
}
```

**全流程分为 4 步**：

```
┌──────────────────────────────────────────────────────────────────┐
│  runRigid()                                                       │
│                                                                   │
│  ① 配置参数                                                       │
│       │                                                           │
│       ▼                                                           │
│  ② rigidRegistration3D()  ← 核心：多分辨率金字塔 + 梯度下降优化    │
│       │                                                           │
│       │  返回 RigidParam3D {tx,ty,tz, rx,ry,rz}                  │
│       ▼                                                           │
│  ③ applyRigid3D()  ← 用最优参数对 Moving 做变换重采样              │
│       │                                                           │
│       ▼                                                           │
│  ④ computeMSE3D / computeNMI3D  ← 评估配准质量                    │
└──────────────────────────────────────────────────────────────────┘
```

下面逐步深入每一步。

---

## 三、核心：`rigidRegistration3D()` — 多分辨率金字塔

### 3.1 代码结构

```cpp
inline RigidParam3D rigidRegistration3D(const Image3D& ref, const Image3D& mov,
                                         const RigidRegConfig3D& cfg) {
    int L = std::max(1, cfg.numLevels);  // 金字塔层数，默认 2

    // ---- 构建金字塔 ----
    std::vector<Image3D> rP(L), mP(L);
    rP[0] = ref;  mP[0] = mov;           // 第 0 层 = 原始分辨率

    for (int i = 1; i < L; i++) {
        // 降采样前先高斯平滑（防混叠）
        Image3D rS = rP[i-1], mS = mP[i-1];
        gaussianSmooth3D(rS.data, rS.width, rS.height, rS.depth, cfg.smoothSigma);
        gaussianSmooth3D(mS.data, mS.width, mS.height, mS.depth, cfg.smoothSigma);
        // 尺寸减半
        int nW = std::max(8, rP[i-1].width/2);
        int nH = std::max(8, rP[i-1].height/2);
        int nD = std::max(4, rP[i-1].depth/2);
        rP[i] = resizeVolume(rS, nW, nH, nD);
        mP[i] = resizeVolume(mS, nW, nH, nD);
    }

    // ---- 从粗到细逐层优化 ----
    RigidParam3D param;  // 初始为零变换
    for (int lvl = L-1; lvl >= 0; lvl--) {
        // 将上一层结果缩放到当前层分辨率
        float sX = (float)rP[lvl].width  / ref.width;
        float sY = (float)rP[lvl].height / ref.height;
        float sZ = (float)rP[lvl].depth  / ref.depth;

        RigidParam3D init = param;
        init.tx *= sX;  init.ty *= sY;  init.tz *= sZ;  // 平移按比例缩放

        // 在当前层做梯度下降优化
        RigidParam3D res = rigidRegSingleLevel3D(rP[lvl], mP[lvl], cfg, init);

        // 将结果缩放回原始分辨率
        param = res;
        param.tx /= sX;  param.ty /= sY;  param.tz /= sZ;
    }
    return param;
}
```

### 3.2 金字塔结构示意

以 `numLevels=2`、原始 `96×96×96` 为例：

```
Level 1 (粗):  48×48×48   ← 先在这里优化，速度快，能抓住大位移
      │
      │  结果传递给下一层作为初始值
      ▼
Level 0 (细):  96×96×96   ← 再在原始分辨率精化，修正小误差
```

### 3.3 为什么用多分辨率金字塔？

| 问题 | 不用金字塔 | 用金字塔 |
|------|-----------|---------|
| **大位移** | 梯度下降只能找到局部最优，如果初始位移大（>10 voxel），会陷入错误局部极值 | 粗层分辨率低、细节少，优化曲面更平滑，能捕获大范围位移 |
| **速度** | 全分辨率每次迭代要遍历 96³≈88万体素 | 粗层只有 48³≈11万体素，快 8 倍 |
| **精度** | — | 粗层找到大致位置，细层精修到亚体素精度 |
| **鲁棒性** | 对初始位置敏感 | 粗层平滑了噪声，更不容易被局部纹理误导 |

**类比**：看地图找路——先看世界地图确定大致方位（粗层），再看街道地图找具体路线（细层）。

### 3.4 为什么降采样前要高斯平滑？

直接隔点采样会产生**混叠伪影**（Aliasing）——高频细节折叠成低频假象。

```
原始信号:  ∿∿∿∿∿∿∿∿∿  (高频)
直接隔点:  ∿___∿___∿  → 看起来像低频信号（混叠！）
高斯平滑后: ∼∼∼∼∼∼∼∼  (高频被滤除)
隔点采样:  ∼__∼__∼__  → 正确的低频表示
```

`gaussianSmooth3D()` 用 σ=1.0 的高斯核在 X→Y→Z 三个方向分别做 1D 卷积（可分离滤波），效率远高于直接 3D 卷积。

### 3.5 为什么平移参数要按比例缩放？

粗层的 1 个体素 = 细层的 2 个体素。

```
粗层 (48³):  tx=5 voxel  →  实际物理位移 = 5 × 2 = 10 原始体素
细层 (96³):  tx=10 voxel  →  同样的物理位移
```

所以从粗层传递到细层时：`init.tx *= sX`（sX=0.5），从细层传回时：`param.tx /= sX`。

**旋转参数不需要缩放**——旋转角度是无量纲的（弧度），与分辨率无关。

---

## 四、单层优化：`rigidRegSingleLevel3D()` — 有限差分梯度下降

这是配准的核心算法。在每一层分辨率上，用梯度下降法寻找最优的 6 个参数。

### 4.1 算法骨架

```cpp
static RigidParam3D rigidRegSingleLevel3D(const Image3D& ref, const Image3D& mov,
                                           const RigidRegConfig3D& cfg,
                                           RigidParam3D init) {
    RigidParam3D cur = init;        // 当前最优参数
    float step = cfg.maxStep;       // 当前步长

    float diagLen = sqrt(W² + H² + D²);  // 体对角线长度（用于旋转梯度缩放）

    for (int it = 0; it < maxIterations && step > minStep; it++) {
        // ① 计算当前参数下的度量值（基线）
        float base = rigidMetric3D(ref, mov, cur, useNMI);

        // ② 用有限差分计算 6 个参数的梯度
        //    （对每个参数：微扰 → 重采样 → 算度量 → 差分）
        float dTx, dTy, dTz, dRx, dRy, dRz = ...;

        // ③ 归一化梯度
        float gn = sqrt(dTx² + dTy² + ... + dRz²);

        // ④ 沿负梯度方向走一步
        RigidParam3D next = cur;
        next.tx -= step * dTx / gn;
        next.rx -= step * dRx / gn / diagLen;
        ...

        // ⑤ 验收：如果度量变好了，接受；否则缩小步长
        if (rigidMetric3D(ref, mov, next) < base)
            cur = next;        // 接受
        else
            step *= relaxFactor;  // 拒绝，步长 ×0.95
    }
    return cur;
}
```

### 4.2 逐步详解

#### Step ①：计算基线度量

```cpp
float base = rigidMetric3D(ref, mov, cur, cfg.useNMI);
```

```cpp
static float rigidMetric3D(const Image3D& ref, const Image3D& mov,
                             const RigidParam3D& p, bool useNMI) {
    Image3D w = applyRigid3D(mov, ref, p);  // 用当前参数变换 Moving
    return useNMI ? -computeNMI3D(ref, w)   // NMI：越大越好，取负变为"越小越好"
                  :  computeMSE3D(ref, w);  // MSE：越小越好
}
```

**为什么 NMI 取负？** 统一为最小化问题。无论用 MSE 还是 NMI，优化器都是"往度量减小的方向走"。

**每次调用 `rigidMetric3D` 做了什么？**
1. 用当前参数 `(tx,ty,tz,rx,ry,rz)` 对 Moving 做刚体变换 → 得到 warped 图像
2. 计算 warped 与 Fixed 之间的相似度
3. 返回度量值

#### Step ②：有限差分计算梯度

对 6 个参数中的每一个，用**前向差分**估计偏导数：

$$
\frac{\partial M}{\partial \theta_i} \approx \frac{M(\theta + \epsilon \cdot e_i) - M(\theta)}{\epsilon}
$$

```cpp
// 平移参数：ε = 0.5 体素
p = cur; p.tx += EPS_T;  dTx = (rigidMetric3D(ref,mov,p) - base) / EPS_T;
p = cur; p.ty += EPS_T;  dTy = (rigidMetric3D(ref,mov,p) - base) / EPS_T;
p = cur; p.tz += EPS_T;  dTz = (rigidMetric3D(ref,mov,p) - base) / EPS_T;

// 旋转参数：ε = 0.003 弧度，梯度乘以 diagLen
p = cur; p.rx += EPS_R;  dRx = (rigidMetric3D(ref,mov,p) - base) / EPS_R * diagLen;
p = cur; p.ry += EPS_R;  dRy = (rigidMetric3D(ref,mov,p) - base) / EPS_R * diagLen;
p = cur; p.rz += EPS_R;  dRz = (rigidMetric3D(ref,mov,p) - base) / EPS_R * diagLen;
```

**为什么用有限差分而不是解析梯度？**

| 方式 | 优点 | 缺点 |
|------|------|------|
| **有限差分** | 实现简单，不需要推导公式，对任意度量都通用 | 每次迭代需要 6+1=7 次度量求值（1 次基线 + 6 次微扰），计算量大 |
| **解析梯度** | 精度高，只需 1 次前向+1 次反向传播 | 需要手动推导每个度量的梯度公式，实现复杂 |

本代码选择有限差分，因为：
1. 医学影像配准中度量函数复杂（NMI 涉及直方图、对数），解析推导困难
2. 代码简洁，易于维护和扩展新度量
3. 6-DOF 参数少，7 次求值的开销可接受

**为什么平移 ε=0.5 而旋转 ε=0.003？**

平移的单位是体素（~1mm），0.5 体素是一个有意义的微扰量。
旋转的单位是弧度，0.003 弧度 ≈ 0.17°，是一个小角度微扰。太大的旋转微扰会导致图像内容变化剧烈，梯度估计不准。

#### Step ③：旋转梯度缩放

```cpp
dRx = (...) / EPS_R * diagLen;  // 旋转梯度乘以体对角线长度
```

**为什么旋转梯度要乘 `diagLen`？**

平移和旋转对图像的影响量纲不同：
- 平移 1 体素 → 像素移动 1 个单位
- 旋转 1 弧度 → 边缘像素移动约 `diagLen` 个单位（旋转臂 = 到中心的距离）

如果不缩放，平移梯度会远大于旋转梯度，优化器会优先调整平移而忽略旋转。乘以 `diagLen` 后，两者量纲统一，优化器能同时有效地调整平移和旋转。

```
旋转 0.01 弧度，在体对角线 ~166 体素处：
  位移 ≈ 0.01 × 166 = 1.66 体素  ← 与平移 1.66 体素效果相当
```

#### Step ④：归一化梯度 + 步长控制

```cpp
float gn = sqrt(dTx² + dTy² + dTz² + dRx² + dRy² + dRz²);
if (gn < 1e-12f) break;  // 梯度几乎为零 → 已收敛

next.tx -= step * dTx / gn;           // 平移：直接除以梯度范数
next.rx -= step * dRx / gn / diagLen; // 旋转：再除以 diagLen 还原
```

**为什么归一化？**

不归一化时，梯度大小随图像内容和参数变化剧烈，步长难以设定：
- 梯度大时 → 步子太大，跳过最优点
- 梯度小时 → 步子太小，收敛极慢

归一化后，每次走的是**单位梯度方向上的固定步长**，行为稳定可控。

**旋转参数为什么再除以 `diagLen`？**

前面乘了 `diagLen` 来统一量纲，这里除回去，把旋转参数的变化量还原到正确的弧度尺度。

#### Step ⑤：步长松弛策略

```cpp
if (rigidMetric3D(ref, mov, next) < base)
    cur = next;           // 度量变小了 → 接受新参数
else
    step *= relaxFactor;  // 度量变大了 → 拒绝，缩小步长
```

**为什么不用固定步长？**

固定步长的问题：
- 太大 → 在最优点附近来回震荡，无法收敛
- 太小 → 远离最优点时收敛极慢

**自适应步长策略**（类似模拟退火）：
1. 初始用大步长 `maxStep=3.0`，快速接近最优区域
2. 如果走一步后度量变差，说明步子太大 → 乘以 0.95 缩小
3. 步长持续缩小直到 `minStep=0.008`，认为已收敛

```
迭代 1:  step=3.0   → 大步前进
迭代 5:  step=2.43  → 接近最优
迭代 10: step=1.86  → 精细调整
迭代 50: step=0.26  → 微调
迭代 120: step=0.008 → 收敛退出
```

---

## 五、变换重采样：`applyRigid3D()` — 逆映射 + 三线性插值

### 5.1 代码

```cpp
inline Image3D applyRigid3D(const Image3D& mov, const Image3D& ref,
                              const RigidParam3D& p) {
    float R[3][3]; buildRotMat3D(p.rx, p.ry, p.rz, R);  // 构造旋转矩阵
    float cx = ref.width*0.5f, cy = ref.height*0.5f, cz = ref.depth*0.5f;

    Image3D out; out.resize(ref.width, ref.height, ref.depth);

    for (int z=0; z<ref.depth; z++)
      for (int y=0; y<ref.height; y++)
        for (int x=0; x<ref.width; x++) {
            // 逆映射：从 Fixed 空间 → Moving 空间
            float dx = (x-cx) - p.tx;
            float dy = (y-cy) - p.ty;
            float dz = (z-cz) - p.tz;
            float mx = cx + R[0][0]*dx + R[1][0]*dy + R[2][0]*dz;
            float my = cy + R[0][1]*dx + R[1][1]*dy + R[2][1]*dz;
            float mz = cz + R[0][2]*dx + R[1][2]*dy + R[2][2]*dz;
            // 三线性插值采样
            out.data[out.idx(x,y,z)] = trilinear(mov, mx, my, mz);
        }
    return out;
}
```

### 5.2 为什么用逆映射（Inverse Mapping）？

**正映射**（Forward）：Moving 的每个体素 → 变换到 Fixed 空间
- 问题：变换后的坐标可能不落在 Fixed 的整数网格上 → 产生"空洞"

**逆映射**（Inverse）：Fixed 的每个体素 → 反向变换回 Moving 空间 → 采样
- 优势：Fixed 的每个体素都有对应值，不会产生空洞

```
正映射（有问题）:                逆映射（正确做法）:
  Moving 网格                      Fixed 网格
  ● ● ● ●                          ● ● ● ●
   ↘ ↘ ↘ ↘                         ↓ ↓ ↓ ↓  (每个输出点都查 Moving)
    ● ● ●  → Fixed 网格             Moving 空间（连续坐标）
    ● ○ ●  ← 空洞！                 ● ● ● ●
    ● ● ●                           ● ● ● ●
```

### 5.3 为什么绕中心旋转？

```cpp
float cx = ref.width*0.5f, cy = ref.height*0.5f, cz = ref.depth*0.5f;
float dx = (x - cx) - p.tx;  // 先平移到原点
// ... 旋转 ...
float mx = cx + R*...;       // 再平移回去
```

如果不绕中心，而是绕原点 (0,0,0) 旋转，图像会"飞走"——因为原点在角落，旋转臂很大。

绕中心旋转的物理含义：物体在原地"自转"，符合医学影像配准的直觉。

### 5.4 旋转矩阵构造：ZYX Euler 角

```cpp
inline void buildRotMat3D(float rx, float ry, float rz, float R[3][3]) {
    // R = Rz × Ry × Rx
    R[0][0]=cy*cz;  R[0][1]=cz*sx*sy-cx*sz;  R[0][2]=cx*cz*sy+sx*sz;
    R[1][0]=cy*sz;  R[1][1]=cx*cz+sx*sy*sz;  R[1][2]=cx*sy*sz-cz*sx;
    R[2][0]=-sy;    R[2][1]=cy*sx;            R[2][2]=cx*cy;
}
```

旋转顺序：先绕 X 轴，再绕 Y 轴，最后绕 Z 轴。矩阵乘法顺序为 $R = R_z \cdot R_y \cdot R_x$。

> **注意**：Euler 角有万向锁问题（Gimbal Lock），当 ry=±90° 时丢失一个自由度。但对于医学配准，旋转角度通常很小（<10°），不会触发万向锁。如果需要大角度旋转，应使用四元数（Versor）参数化。

### 5.5 三线性插值

```cpp
inline float trilinear(const Image3D& v, float x, float y, float z) {
    // 越界返回 0
    if (x < 0 || x > v.width-1.0001f || ...) return 0.0f;

    // 取 8 个最近邻体素
    int xi = (int)x, yi = (int)y, zi = (int)z;
    float fx = x-xi, fy = y-yi, fz = z-zi;

    // 三线性加权
    return ((v000*(1-fx)+v100*fx)*(1-fy) + (v010*(1-fx)+v110*fx)*fy) * (1-fz)
         + ((v001*(1-fx)+v101*fx)*(1-fy) + (v011*(1-fx)+v111*fx)*fy) *    fz;
}
```

逆映射后的坐标 `(mx, my, mz)` 是浮点数，不落在整数网格上。三线性插值用 8 个最近邻体素加权平均：

$$
f(x,y,z) = \sum_{i,j,k \in \{0,1\}} w_i \cdot w_j \cdot w_k \cdot f(x_i, y_j, z_k)
$$

**为什么用三线性而不是最近邻？**
- 最近邻：块状伪影，度量不连续，梯度信息差
- 三线性：C⁰ 连续，度量曲面平滑，优化器更容易找到正确方向

---

## 六、相似度度量

### 6.1 MSE（均方误差）

```cpp
inline float computeMSE3D(const Image3D& ref, const Image3D& mov) {
    double mse = 0.0; int N = ref.size();
    for (int i = 0; i < N; i++) {
        double d = ref.data[i] - mov.data[i];
        mse += d * d;
    }
    return (float)(mse / N);
}
```

$$
\text{MSE} = \frac{1}{N} \sum_{i=1}^{N} (I_{\text{ref}}(i) - I_{\text{mov}}(i))^2
$$

**适用场景**：同模态配准（CT-CT, MR-MR），两张图像的灰度值有直接对应关系。

**不适用 CT-PET**：CT 灰度反映组织密度（HU 值），PET 灰度反映代谢活性（SUV 值），两者没有线性对应关系。同一解剖位置在 CT 和 PET 中的灰度值完全不同，MSE 会给出错误指导。

### 6.2 NMI（归一化互信息）

```cpp
inline float computeNMI3D(const Image3D& ref, const Image3D& mov, int bins=64) {
    // 1. 计算参考图直方图 hR
    // 2. 计算浮动图直方图 hM
    // 3. 计算联合直方图 hJ
    // 4. 计算熵：Hr, Hm, Hj
    // 5. NMI = (Hr + Hm) / Hj
}
```

$$
\text{NMI} = \frac{H(R) + H(M)}{H(R, M)}
$$

**核心思想**：不假设两张图像的灰度有直接对应关系，而是衡量它们的**统计相关性**。

- 当两张图像完全对齐时，联合直方图最"尖锐"（某些灰度组合频繁出现），$H(R,M)$ 最小 → NMI 最大
- 当两张图像错位时，联合直方图最"散乱"（灰度组合均匀分布），$H(R,M)$ 最大 → NMI 最小

**为什么 CT-PET 必须用 NMI？**

| | CT | PET |
|---|---|---|
| 灰度含义 | 组织密度（骨骼高、肺低） | 代谢活性（肿瘤高、背景低） |
| 灰度范围 | -1000 ~ 3000 HU | 0 ~ ~20 SUV |
| 线性关系 | 无 | 无 |

NMI 不需要灰度线性对应，只需要**统计上相关**——即"CT 中的某个灰度值，在 PET 中倾向于对应某个灰度值范围"。

### 6.3 直方图分箱

```cpp
int r = std::clamp((int)((ref.data[i]-rMin)/rR*bins), 0, bins-1);
int m = std::clamp((int)((mov.data[i]-mMin)/mR*bins), 0, bins-1);
hR[r]++; hM[m]++; hJ[r*bins+m]++;
```

将连续灰度值离散化到 `bins=64` 个区间：
- 太少（如 8 bins）：灰度分辨率不够，度量不敏感
- 太多（如 256 bins）：直方图稀疏，统计不稳定，计算量大
- 64 bins 是医学配准的常用折中值

---

## 七、完整数据流总结

```
                    ┌──────────────────────────────────────────────────┐
                    │           rigidRegistration3D()                   │
                    │                                                   │
                    │  ┌─────────────────────────────────┐              │
                    │  │  构建金字塔 (2层)                 │              │
                    │  │  Level 0: 96³ (原始)              │              │
                    │  │  Level 1: 48³ (平滑+降采样)       │              │
                    │  └──────────┬──────────────────────┘              │
                    │             │                                      │
                    │     ┌───────▼────────┐                            │
                    │     │ Level 1 优化    │                            │
                    │     │ (48³, 粗)       │                            │
                    │     │                 │                            │
                    │     │ for each iter:  │                            │
                    │     │  ① 算基线度量    │                            │
                    │     │  ② 有限差分梯度  │                            │
                    │     │  ③ 归一化        │                            │
                    │     │  ④ 梯度下降      │                            │
                    │     │  ⑤ 步长松弛      │                            │
                    │     └───────┬────────┘                            │
                    │             │ tx,ty,tz 缩放传递                     │
                    │     ┌───────▼────────┐                            │
                    │     │ Level 0 优化    │                            │
                    │     │ (96³, 细)       │                            │
                    │     │ 同上流程         │                            │
                    │     └───────┬────────┘                            │
                    │             │                                      │
                    │     返回 RigidParam3D {tx,ty,tz,rx,ry,rz}        │
                    └─────────────┬────────────────────────────────────┘
                                  │
                    ┌─────────────▼────────────────────────────────────┐
                    │           applyRigid3D()                          │
                    │                                                   │
                    │  for each voxel (x,y,z) in Fixed:                 │
                    │    ① 平移到原点: (x-cx, y-cy, z-cz)               │
                    │    ② 减去平移: - (tx, ty, tz)                     │
                    │    ③ 逆旋转: R^T × ...                            │
                    │    ④ 平移回中心: + (cx, cy, cz)                   │
                    │    ⑤ 三线性插值采样 Moving                        │
                    │                                                   │
                    │  返回 warped Image3D                              │
                    └─────────────┬────────────────────────────────────┘
                                  │
                    ┌─────────────▼────────────────────────────────────┐
                    │           computeMSE3D / computeNMI3D             │
                    │                                                   │
                    │  比较 Fixed vs warped，输出度量值                  │
                    │  MSE ↓ 或 NMI ↑ → 配准质量好                      │
                    └──────────────────────────────────────────────────┘
```

---

## 八、参数调优指南

### 8.1 `RigidRegConfig3D` 参数说明

| 参数 | 默认值 | 作用 | 调优建议 |
|------|--------|------|---------|
| `maxIterations` | 150 | 每层最大迭代次数 | 体积大时减小（100-120），避免耗时过长 |
| `maxStep` | 2.0 | 初始步长（体素） | 预位移大时增大（3-5），小时减小（1-2） |
| `minStep` | 0.005 | 收敛阈值 | 越小越精确但越慢，通常 0.005-0.01 |
| `relaxFactor` | 0.95 | 步长衰减率 | 0.9=快速收敛但粗糙，0.98=慢但精细 |
| `useNMI` | false | 度量选择 | 同模态用 MSE，跨模态（CT-PET）用 NMI |
| `numLevels` | 2 | 金字塔层数 | 体积大时用 3-4 层，小时用 1-2 层 |
| `smoothSigma` | 1.0 | 降采样前平滑 | 噪声大时增大（1.5-2.0） |

### 8.2 当前代码中的配置

```cpp
// src/main_registration.cpp → runRigid()
cfg.useNMI = useNMI;       // [1] 键用 MSE，[2] 键用 NMI
cfg.numLevels = 2;         // 2 层金字塔：48³ → 96³
cfg.maxIterations = 120;   // 每层最多 120 次迭代
cfg.maxStep = 3.f;         // 初始步长 3 体素
cfg.minStep = 0.008f;      // 收敛阈值
cfg.relaxFactor = 0.95f;   // 步长每次衰减 5%
```

### 8.3 CT-PET 配准建议

- **必须用 NMI**（`useNMI=true`，按 `[2]` 键），因为 CT 和 PET 灰度无线性关系
- **金字塔层数 ≥ 2**，CT-PET 初始位移可能较大
- **迭代次数 100-150**，NMI 的梯度比 MSE 噪声大，需要更多迭代
- 如果配准效果不好，尝试增大 `maxStep` 到 5 或增加 `numLevels` 到 3

---

## 九、常见问题

### Q1: 为什么配准后 MSE 反而变大了？

**A**: 如果用的是 NMI 优化（`useNMI=true`），优化器最小化的是 `-NMI` 而非 MSE。NMI 最优不等于 MSE 最优——对于 CT-PET 这种跨模态场景，MSE 本身就不是正确的度量，NMI 变好才是真正的配准改善。

### Q2: 为什么旋转角度看起来很小？

**A**: CT-PET 配准中，患者通常在同一床位扫描，旋转位移很小（<5°）。如果旋转角度很大（>30°），可能是：
1. 图像方向不一致（DICOM 方向矩阵不同）
2. 优化陷入局部极值（尝试增加金字塔层数）

### Q3: 有限差分为什么用前向差分而不是中心差分？

**A**: 中心差分精度更高（O(ε²) vs O(ε)），但需要 2 倍的度量求值（每参数 2 次而非 1 次）。对于 6-DOF 配准，前向差分总共 7 次求值/迭代，中心差分需要 13 次。在精度和速度之间，前向差分是合理的折中。

### Q4: 为什么不用 L-BFGS 或 Adam 等高级优化器？

**A**: 有限差分梯度下降的优势在于：
1. **实现简单**，不依赖二阶信息（Hessian）
2. **通用性强**，对任意度量都适用
3. **内存友好**，不需要存储历史梯度（L-BFGS）或动量（Adam）
4. 6-DOF 参数空间小，简单优化器已足够

对于高维参数空间（如 B-Spline FFD 的数百个控制点），确实需要更高级的优化器。
