# 医学图像光线投射渲染全流程详解

## 概述
本项目实现了基于OpenGL的医学图像（DICOM）体绘制（Volume Rendering），采用光线投射（Ray Casting）算法。

---

## 完整渲染流程

### 阶段一：数据加载与预处理

#### 1. DICOM文件加载 (`dicom_utils.cpp`)
```cpp
SeriesData series = collectSeries(argv[1]);
```

**主要步骤：**
- 扫描目录下所有DICOM文件
- 解析每个文件的元数据：
  - `SliceMeta::imagePosition[3]` - 切片在3D空间的位置
  - `SliceMeta::orientationRow/Col[3]` - 图像方向
  - `SliceMeta::instanceNumber` - 实例编号
- 根据SeriesUID分组切片

#### 2. 体数据构建 (`volume_build.cpp`)
```cpp
VolumeBuildResult volumeData = buildVolume_none(series);
```

**构建过程：**
- 按图像位置排序所有切片
- 确定体数据维度：`width × height × depth`
- 读取每个切片的像素数据
- 归一化到 [0, 255] 范围
- 组织成连续的3D缓冲区

**输出结构：**
```cpp
struct VolumeBuildResult {
    std::vector<uint8_t> buffer;  // 体素数据
    uint32_t width, height, depth; // 维度
    double spacing[3];             // 物理间距
    double origin[3];              // 起始位置
}
```

---

### 阶段二：OpenGL资源初始化

#### 3. 创建3D纹理 (`main.cpp::create3DTextureFromVolume`)
```cpp
GLuint volumeTexture = create3DTextureFromVolume(volumeData);
```

**关键设置：**
```cpp
// 纹理参数
glTexParameteri(GL_TEXTURE_3D, GL_TEXTURE_WRAP_S/T/R, GL_CLAMP_TO_EDGE);
glTexParameteri(GL_TEXTURE_3D, GL_TEXTURE_MIN_FILTER, GL_LINEAR_MIPMAP_LINEAR);
glTexParameteri(GL_TEXTURE_3D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);

// 数据转换：uint8 → float [0,1]
floatData[i] = static_cast<float>(volumeData.buffer[i]) / 255.0f;

// 上传到GPU
glTexImage3D(GL_TEXTURE_3D, 0, GL_R32F, 
             width, height, depth, 
             0, GL_RED, GL_FLOAT, floatData.data());
```

#### 4. 创建代理几何体 (`main.cpp::createCubeGeometry`)
```cpp
CubeGeometry cube = createCubeGeometry();
```

**用途：**
- 光线投射需要一个覆盖整个体数据的边界框
- 创建单位立方体 [0,0,0] 到 [1,1,1]
- 8个顶点，36个索引（12个三角形）

#### 5. 传输函数设置 (`transferFunction.h`)
```cpp
TransferFunction transferFunction;
transferFunction.update(windowLevel, windowWidth);
```

**传输函数作用：**
- 将密度值映射到颜色和透明度
- 实现窗宽窗位调整（Window Level/Width）
- 支持多种预设：灰度、伪彩色、医学组织分类

**CT灰度传输函数：**
```cpp
vec4 ctGrayTransferFunction(float t) {
    float smooth_t = t * t * (3.0f - 2.0f * t); // 平滑插值
    float alpha = 1.0f - (1.0f - smooth_t) * (1.0f - smooth_t);
    return vec4(smooth_t, smooth_t, smooth_t, alpha);
}
```

---

### 阶段三：渲染管线

#### 6. 顶点着色器 (`volume_vert_glsl.vert`)

**主要任务：**
```glsl
void main() {
    // ① 计算裁剪空间坐标
    gl_Position = projection * modelview * vec4(inPosition, 1.0);
    
    // ② 计算相机在模型空间的位置
    cameraPos = inverse(modelview) * vec4(0.0, 0.0, 0.0, 1.0);
    
    // ③ 传递顶点位置（纹理坐标）
    vertexPos = vec4(inPosition, 1.0);
    
    // ④ 计算顶点法线
    worldNormal = normalize(cubeNormals[gl_VertexID % 8]);
}
```

**输出变量：**
- `cameraPos` - 相机位置（射线起点）
- `vertexPos` - 立方体顶点位置（射线方向参考）
- `worldPos` - 世界空间位置
- `worldNormal` - 顶点法线

---

#### 7. 片元着色器 - 光线投射核心 (`volume_frag_glsl.frag`)

##### 7.1 射线-包围盒求交
```glsl
vec3 rayOrigin = cameraPos.xyz;
vec3 rayDir = normalize(vertexPos.xyz - rayOrigin);

float tEnter, tExit;
if (!intersectRayAABB(rayOrigin, rayDir, boxMin, boxMax, tEnter, tExit)) {
    discard; // 射线未击中体数据
}
```

**AABB求交算法：**
```glsl
bool intersectRayAABB(vec3 rayOrigin, vec3 rayDir, 
                     vec3 boxMin, vec3 boxMax,
                     out float tEnter, out float tExit) {
    vec3 invRayDir = 1.0 / rayDir;
    vec3 t1 = (boxMin - rayOrigin) * invRayDir;
    vec3 t2 = (boxMax - rayOrigin) * invRayDir;
    
    vec3 tMin = min(t1, t2);
    vec3 tMax = max(t1, t2);
    
    tEnter = max(max(tMin.x, tMin.y), tMin.z);
    tExit = min(min(tMax.x, tMax.y), tMax.z);
    
    return tmax >= max(tmin, 0.0);
}
```

##### 7.2 确定步进参数
```glsl
vec3 entryPoint = rayOrigin + rayDir * tEnter;
vec3 exitPoint = rayOrigin + rayDir * tExit;
float rayLength = distance(entryPoint, exitPoint);
int numSteps = int(rayLength / stepSize) + 1;
```

**关键参数：**
- `stepSize = 0.0015` - 每步的长度（影响质量和性能）
- `numSteps` - 总步数（自适应）

##### 7.3 步进循环与采样

**主循环结构：**
```glsl
vec4 accumulatedColor = vec4(0.0);
float accumulatedAlpha = 0.0;

for (int i = 0; i < numSteps; ++i) {
    if (accumulatedAlpha > 0.99) break; // 早期终止
    
    // 1️⃣ 计算采样位置（带抖动）
    float noiseValue = screenSpaceBlueNoise(gl_FragCoord.xy);
    float jitter = (noiseValue - 0.5) * jitterStrength * stepSize;
    
    float t = float(i) / float(numSteps - 1);
    vec3 basePos = mix(entryPoint, exitPoint, t);
    vec3 samplePos = basePos + rayDir * jitter;
    
    // 2️⃣ 边界检查
    if (any(lessThan(samplePos, boxMin)) || 
        any(greaterThan(samplePos, boxMax))) break;
    
    // 3️⃣ 密度采样
    float density = texture(volume, samplePos).r;
    float windowedDensity = applyWindow(density, windowLevel, windowWidth);
    
    if (windowedDensity < 0.01) continue; // 跳过低密度区域
    
    // 4️⃣ 传输函数采样
    vec4 colorSample = texture(transferFunc, windowedDensity);
    
    // 5️⃣ 梯度计算（法线）
    vec3 normal = computeSimpleGradient(samplePos);
    
    // 6️⃣ 光照计算
    vec3 litColor = applyPhongLighting(colorSample.rgb, samplePos, 
                                       normal, viewDir);
    
    // 7️⃣ Alpha混合
    float alpha = clamp(colorSample.a * windowedDensity * 1.5, 0.0, 1.0);
    accumulatedColor.rgb += (1.0 - accumulatedAlpha) * alpha * litColor;
    accumulatedAlpha += (1.0 - accumulatedAlpha) * alpha;
}
```

---

##### 7.4 核心技术详解

###### 🎯 蓝噪声抖动（避免条纹伪影）
```glsl
float screenSpaceBlueNoise(vec2 pixelCoord) {
    vec2 uv = pixelCoord / screenSize * noiseScale;
    
    // 多层噪声叠加
    float noise1 = gradientNoise(uv * 1.0);
    float noise2 = gradientNoise(uv * 2.0 + 100.0);
    float noise3 = gradientNoise(uv * 4.0 + 200.0);
    
    return noise1 * 0.5 + noise2 * 0.25 + noise3 * 0.125;
}
```

**作用：**
- 消除步进采样产生的木纹伪影（Wood Grain Artifact）
- 使用蓝噪声而非白噪声，分布更均匀
- 每个像素和每个步进点都有不同的抖动

###### 🪟 窗宽窗位调整
```glsl
float applyWindow(float density, float level, float width) {
    float windowMin = level - width / 2.0;
    float windowMax = level + windowMax / 2.0;
    
    if (density <= windowMin) return 0.0;
    if (density >= windowMax) return 1.0;
    return (density - windowMin) / width;
}
```

**预设窗口：**
- **软组织窗**：`windowLevel=0.26, windowWidth=0.1` (HU≈40)
- **骨窗**：`windowLevel=0.35, windowWidth=0.5` (HU≈400)
- **肺窗**：`windowLevel=0.3, windowWidth=0.08` (HU≈200)

###### 🧮 梯度计算（中心差分法）
```glsl
vec3 computeSimpleGradient(vec3 pos) {
    const float eps = 0.01;
    vec3 grad;
    
    grad.x = texture(volume, pos + vec3(eps,0,0)).r 
           - texture(volume, pos - vec3(eps,0,0)).r;
    grad.y = texture(volume, pos + vec3(0,eps,0)).r 
           - texture(volume, pos - vec3(0,eps,0)).r;
    grad.z = texture(volume, pos + vec3(0,0,eps)).r 
           - texture(volume, pos - vec3(0,0,eps)).r;
    
    return normalize(grad);
}
```

**用途：**
- 梯度方向即为表面法线
- 用于光照计算
- 6次纹理采样（性能开销）

###### 💡 Phong光照模型
```glsl
vec3 applyPhongLighting(vec3 color, vec3 position, 
                       vec3 normal, vec3 viewDir) {
    vec3 lightDir = normalize(lightPos - position);
    
    // ① 环境光
    vec3 ambient = ambientStrength * color;
    
    // ② 漫反射
    float diff = max(dot(normal, lightDir), 0.0);
    vec3 diffuse = diff * diffuseStrength * color * lightColor;
    
    // ③ 镜面反射
    vec3 reflectDir = reflect(-lightDir, normal);
    float spec = pow(max(dot(viewDir, reflectDir), 0.0), shininess);
    vec3 specular = spec * specularStrength * lightColor;
    
    // ④ 距离衰减
    float distance = length(lightPos - position);
    float attenuation = 1.0 / (1.0 + 0.1*distance + 0.01*distance*distance);
    
    return ambient + (diffuse + specular) * attenuation;
}
```

**参数：**
- `ambient = 0.3` - 环境光强度
- `diffuse = 0.7` - 漫反射强度
- `specular = 0.3` - 镜面反射强度
- `shininess = 32.0` - 高光指数

###### 🎨 前向Alpha混合
```glsl
// 标准前向混合公式
C_new = C_acc + (1 - α_acc) × α_sample × C_sample
α_new = α_acc + (1 - α_acc) × α_sample
```

**特点：**
- 从前往后累积颜色
- 透明度饱和后自动终止
- 更真实的半透明效果

---

##### 7.5 后处理与输出
```glsl
// 边缘增强（减少锯齿）
if (accumulatedAlpha > 0.1) {
    float edgeNoise = screenSpaceBlueNoise(gl_FragCoord.xy * 2.0);
    accumulatedColor.rgb *= 1.0 + 0.2 * edgeNoise;
}

// 应用亮度
accumulatedColor.rgb *= brightness;

// Gamma校正（色调映射）
accumulatedColor.rgb = pow(accumulatedColor.rgb, vec3(1.0/2.2));

// 输出最终颜色
outColor = vec4(accumulatedColor.rgb, accumulatedAlpha);
```

---

### 阶段四：交互控制

#### 8. 轨道相机系统 (`camera.cpp`)
```cpp
OrbitCamera orbitCamera;
orbitCamera.setTarget(vec3(0.5, 0.5, 0.5)); // 观察中心
orbitCamera.setDistance(3.0f);               // 距离
orbitCamera.setPitchLimits(-89.0f, 89.0f);   // 俯仰角限制
```

**控制方式：**
- 鼠标左键拖动：旋转视角（Yaw和Pitch）
- 鼠标滚轮：缩放距离
- 相机围绕体数据中心旋转

#### 9. 键盘快捷键
| 按键 | 功能 | 说明 |
|------|------|------|
| ↑/↓ | 调整窗位 | windowLevel ± 0.003 |
| ←/→ | 调整窗宽 | windowWidth ± 0.003 |
| 2 | 软组织窗 | Level=0.26, Width=0.1 |
| 3 | 骨窗 | Level=0.35, Width=0.5 |
| 4 | 肺窗 | Level=0.3, Width=0.08 |
| I/O | 环境光 | 增加/减少 ambient |
| K/L | 漫反射 | 增加/减少 diffuse |
| ESC | 退出程序 | - |

---

## 性能优化技术

### 1. 早期光线终止（Early Ray Termination）
```glsl
if (accumulatedAlpha > 0.99) break;
```
- 透明度接近不透明时提前退出
- 减少不必要的采样

### 2. 空区域跳过（Empty Space Skipping）
```glsl
if (windowedDensity < 0.01) continue;
```
- 跳过低密度（空气等）区域
- 大幅减少计算量

### 3. 自适应步长
```cpp
int numSteps = int(rayLength / stepSize) + 1;
```
- 根据射线穿过体数据的实际长度调整步数
- 短射线少采样，长射线多采样

### 4. Mipmap加速
```cpp
glTexParameteri(GL_TEXTURE_3D, GL_TEXTURE_MIN_FILTER, 
                GL_LINEAR_MIPMAP_LINEAR);
```
- 远处细节使用低分辨率纹理
- 减少纹理带宽

### 5. 多重采样抗锯齿（MSAA）
```cpp
glfwWindowHint(GLFW_SAMPLES, 4);
glEnable(GL_MULTISAMPLE);
```
- 硬件级抗锯齿
- 减少几何边缘锯齿

---

## 渲染质量参数调优

### 关键参数影响

| 参数 | 值 | 影响 | 权衡 |
|------|-----|------|------|
| `stepSize` | 0.0015 | 采样步长 | 越小越细腻但越慢 |
| `densityScale` | 1.2 | 密度缩放 | 影响不透明度 |
| `brightness` | 1.5 | 整体亮度 | 后处理增强 |
| `jitterStrength` | 0.5 | 抖动强度 | 消除条纹但增加噪点 |
| `noiseScale` | 2.0 | 噪声频率 | 影响抖动分布 |

### 画质预设建议

**高质量（慢速）：**
```cpp
stepSize = 0.001
jitterStrength = 0.8
densityScale = 1.5
```

**平衡（推荐）：**
```cpp
stepSize = 0.0015
jitterStrength = 0.5
densityScale = 1.2
```

**性能（快速）：**
```cpp
stepSize = 0.003
jitterStrength = 0.3
densityScale = 1.0
```

---

## 算法数学原理

### 光线投射方程
$$
C(\mathbf{x}_0) = \int_{t_0}^{t_1} \tau(t) \cdot c(\mathbf{x}(t)) \cdot e^{-\int_{t_0}^{t} \tau(s)ds} dt
$$

其中：
- $C$ = 最终颜色
- $\tau(t)$ = 消光系数（密度）
- $c(\mathbf{x}(t))$ = 发射颜色
- $e^{-\int \tau(s)ds}$ = 透射率（衰减）

### 离散化近似（Riemann求和）
$$
C \approx \sum_{i=0}^{N} \tau_i \cdot c_i \cdot \Delta t \cdot \prod_{j=0}^{i-1}(1 - \alpha_j)
$$

其中：
- $\alpha_i = 1 - e^{-\tau_i \Delta t}$ （局部透明度）
- $\Delta t$ = `stepSize`（步长）

### Alpha混合递推公式
$$
\begin{aligned}
C_i &= C_{i-1} + (1 - \alpha_{i-1}) \cdot \alpha_i \cdot c_i \\
\alpha_i &= \alpha_{i-1} + (1 - \alpha_{i-1}) \cdot \alpha_i
\end{aligned}
$$

---

## 数据流图

```
┌─────────────┐
│ DICOM Files │
└──────┬──────┘
       │ dcmtk库解析
       ▼
┌─────────────┐
│ SliceMeta[] │ (位置、方向、像素)
└──────┬──────┘
       │ 排序、归一化
       ▼
┌─────────────┐
│ Volume3D    │ (width×height×depth)
└──────┬──────┘
       │ glTexImage3D
       ▼
┌─────────────┐
│ GPU Texture │ (GL_TEXTURE_3D)
└──────┬──────┘
       │
       ├────────────────┐
       │                │
       ▼                ▼
┌────────────┐  ┌──────────────┐
│Vertex      │  │Transfer      │
│Shader      │  │Function (1D) │
└─────┬──────┘  └──────┬───────┘
      │                │
      └────────┬───────┘
               ▼
      ┌────────────────┐
      │Fragment Shader │
      │ (Ray Casting)  │
      └────────┬───────┘
               │
               ▼
      ┌────────────────┐
      │  Framebuffer   │
      └────────┬───────┘
               │ glfwSwapBuffers
               ▼
         [屏幕显示]
```

---

## 未来改进方向

### 1. 渲染优化
- [ ] 实现章节优化（Octree加速）
- [ ] GPU计算Shader预处理
- [ ] 自适应采样率

### 2. 视觉效果
- [ ] 体积阴影（Shadow Mapping）
- [ ] 全局光照（环境光遮蔽）
- [ ] 景深效果（Depth of Field）

### 3. 功能扩展
- [ ] 多模态融合（CT+PET）
- [ ] 分割结果叠加显示
- [ ] 测量工具（距离、角度）

### 4. 后处理
- [ ] FXAA抗锯齿（已预留接口）
- [ ] Bloom效果
- [ ] 色调映射曲线

---

## 技术栈总结

| 组件 | 技术 | 版本 |
|------|------|------|
| 图形API | OpenGL | 4.3 Core |
| 窗口管理 | GLFW | 3.x |
| 数学库 | GLM | 0.9.x |
| DICOM解析 | DCMTK | 3.6.x |
| 着色器语言 | GLSL | 430 |
| 编译器 | MSVC | Visual Studio |

---

## 参考资源

1. **Real-Time Volume Graphics** - Klaus Engel et al.
2. **GPU Gems 系列** - NVIDIA
3. **OpenGL SuperBible** - 第7版
4. **DICOM标准文档** - NEMA PS3系列

---

## 相关文档

- [README_光线投射技术总览.md](README_光线投射技术总览.md) - 项目总览和快速开始
- [核心代码片段说明.md](核心代码片段说明.md) - 8个核心算法详解
- [坐标系统与光线生成详解.md](坐标系统与光线生成详解.md) - 坐标空间转换与光线发射原理 ⭐新增

---

## 常见问题排查

### Q1: 渲染结果全黑
- 检查窗宽窗位是否合理
- 验证DICOM数据是否正确加载
- 确认传输函数是否生效

### Q2: 出现条纹伪影
- 增加`jitterStrength`参数
- 减小`stepSize`提高采样率
- 检查噪声函数是否正常

### Q3: 性能低
- 增大`stepSize`（降低质量）
- 启用早期光线终止
- 优化窗口大小

### Q4: 色彩不正常
- 检查Gamma校正
- 验证传输函数映射
- 确认光照参数

---

## 总结

这个项目实现了一个功能完整的医学图像体绘制系统，核心技术包括：

✅ **光线投射** - 物理准确的体积渲染  
✅ **传输函数** - 灵活的密度到颜色映射  
✅ **Phong光照** - 真实感表面光照  
✅ **蓝噪声抖动** - 消除采样伪影  
✅ **窗宽窗位** - 医学图像标准调整  
✅ **交互相机** - 轨道视角控制  
✅ **性能优化** - 早期终止、空区域跳过  

整个流程从DICOM文件加载到最终渲染输出，展示了完整的医学图像可视化工作流。
