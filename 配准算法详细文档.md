# 医疗影像配准算法详细文档

> 来源代码库：`algo-registration`、`algo-registrationgpu`、`McsfAlgoRegistrationApplication`、`McsfAlgoRegistrationG`  
> 整理时间：2026-06-27

---

## 目录

1. [配准算法总体架构](#1-配准算法总体架构)
2. [核心数据结构](#2-核心数据结构)
3. [刚性配准（Rigid Registration）](#3-刚性配准rigid-registration)
4. [仿射配准（Affine Registration）](#4-仿射配准affine-registration)
5. [FFD 形变配准（B-Spline Free-Form Deformation）](#5-ffd-形变配准b-spline-free-form-deformation)
6. [Demons 形变配准](#6-demons-形变配准)
7. [LCC Demons 配准](#7-lcc-demons-配准)
8. [惯性 Demons 配准（Inertial Demons）](#8-惯性-demons-配准inertial-demons)
9. [对称归一化配准（SyN Registration）](#9-对称归一化配准syn-registration)
10. [GPU 加速配准](#10-gpu-加速配准)
11. [深度学习配准（DL Registration）](#11-深度学习配准dl-registration)
12. [应用层配准接口](#12-应用层配准接口)
13. [相似度量](#13-相似度量)
14. [优化器](#14-优化器)
15. [重采样与插值](#15-重采样与插值)
16. [性能对比](#16-性能对比)

---

## 1. 配准算法总体架构

```
                     ┌─────────────────────────────────────────────────────────┐
                     │        应用层（McsfAlgoRegistrationApplication）          │
                     │  BreastReg | NeuroReg | LiverReg | DualEnergy | 4DAnalysis│
                     └────────────────────┬────────────────────────────────────┘
                                          │
           ┌──────────────────────────────┼──────────────────────────────┐
           │                              │                              │
    ┌──────▼───────┐            ┌─────────▼────────┐          ┌────────▼────────┐
    │ 刚性/仿射配准  │            │  形变配准（CPU）   │          │  形变配准（GPU）  │
    │ McsfAlgoReg  │            │ McsfAlgoDemons   │          │ McsfAlgoDemonsG │
    │ Registration │            │ McsfAlgoFFD      │          │ McsfAlgoRegSyn  │
    └──────┬───────┘            └─────────┬────────┘          └────────┬────────┘
           │                              │                             │
    ┌──────▼───────┐            ┌─────────▼────────┐          ┌────────▼────────┐
    │  相似度量     │            │  多分辨率金字塔    │          │  CUDA 并行计算   │
    │  MSE / NMI   │            │  多格网 MultiGrid │          │  Thrust 库      │
    └──────┬───────┘            └─────────┬────────┘          └────────┬────────┘
           │                              │                             │
    ┌──────▼───────┐            ┌─────────▼────────┐          ┌────────▼────────┐
    │  优化器       │            │  插值/重采样      │          │  深度学习配准    │
    │ GD / L-BFGS  │            │  双线性/B样条     │          │ DLRegistration  │
    └──────────────┘            └──────────────────┘          └─────────────────┘
```

### 模块说明

| 模块 | 路径 | 功能 |
|------|------|------|
| `algo-registration` | `src/McsfAlgoRegistration/` | 刚性/仿射/FFD 配准核心 |
| `algo-registration` | `src/McsfAlgoDemons/` | Demons 形变配准系列（CPU） |
| `algo-registration` | `src/McsfAlgoDLRegistration/` | 深度学习配准 |
| `algo-registrationgpu` | `src/McsfAlgoRegistrationG/` | GPU 刚性配准 |
| `algo-registrationgpu` | `src/McsfAlgoDemonsG/` | GPU Demons 配准 |
| `algo-registrationgpu` | `src/McsfAlgoSynRegG/` | SyN 对称归一化配准 |
| `McsfAlgoRegistrationApplication` | `src/` | 应用层封装接口 |
| `McsfAlgoRegistrationG` | `src/` | 独立 GPU 配准库 |

---

## 2. 核心数据结构

### 2.1 CVolumeDataInfo（体积数据描述符）

```cpp
// 文件：algo-registration/include/algo-registration/McsfAlgoRegistration.h
class CVolumeDataInfo {
public:
    short* pImage;              // 图像数据指针（16位短整型 CT/MR HU 值）
    unsigned int uiSize[3];     // 图像尺寸 [Width, Height, Slices]
    double dSpacing[3];         // 物理间距 [x, y, z]（单位：毫米）
    double dPosition[3];        // 第一层图像在世界坐标系的位置
    double dOrientation[6];     // 行列方向余弦（DICOM ImageOrientationPatient）
    double RescaleSlope;        // CT 重缩放斜率
    double RescaleIntercept;    // CT 重缩放截距
};
```

**关键方法：**
- `SetSize()` / `SetSpacing()` / `SetPosition()` / `SetOrientation()`
- `DeleteImage()` — 释放图像内存
- `operator==` / `operator!=` — 比较尺寸和间距

### 2.2 PARAMETER_t（刚性/仿射配准参数）

```cpp
class PARAMETER_t {
    unsigned int dim;              // 维度 (2D/3D)
    bool m_bAffine;                // true=仿射 false=刚性
    bool m_bRotate;                // 是否允许旋转
    unsigned int NumOfBins;        // NMI 直方图 bins 数
    double MaxIterationStep;       // 最大迭代步长
    double MinIterationStep;       // 最小迭代步长（收敛阈值）
    unsigned int MaxIterationNum;  // 最大迭代次数
    double dRelaxationFactor;      // 步长松弛因子（0~1，典型值 0.95）
    double dMagnitudeTolerance;    // 梯度幅度容差
    bool bMultiResolution;         // 是否使用多分辨率金字塔
    unsigned int uiMetricOption;   // 相似度量选项（MSE/NMI/BDT）
    bool m_bUseGPU;                // 是否使用 GPU
    bool m_bUseMultiThread;        // 是否使用多线程
    unsigned int m_uiThreadNumber; // 线程数
    bool m_IfUsePriorMatrix;       // 是否使用先验变换矩阵
    double* m_PriorMatrix;         // 4×4 先验变换矩阵
    unsigned int m_SampleStep[3];  // 采样步长 [x,y,z]
    bool m_IfUseSampleRegionMask;  // 是否使用采样掩码
    unsigned char* m_SampleRegionMask; // 采样区域掩码
};
```

### 2.3 MultiGridSetting（FFD 多格网参数）

```cpp
// 文件：algo-registration/McsfAlgoMultiGridFFD.h
class MultiGridSetting {
    unsigned int dim;              // 维度
    bool PyramidSelfStart;         // 金字塔是否从最高层开始
    unsigned int PyramidOrder;     // 金字塔层数
    bool MultiGridSelfStart;       // 多格网是否自动起始
    unsigned int MultiGridDensity; // 格网密度
    unsigned int OptimizationOrder;// 优化顺序
    bool m_IfUseInitialDeformation;// 是否使用初始形变场
    double* m_InitialDeformation;  // 初始形变场
};
```

### 2.4 SynParameter（SyN 配准参数）

```cpp
struct SynParameter {
    unsigned m_iterNum;         // 迭代次数
    float m_shrinkFactor;       // 缩减因子（2.0 = 降采样到 1/2）
    float m_ImgSmoothSigma;     // 图像降采样前高斯平滑 σ
    float m_varForUpdateField;  // 更新场平滑方差
    float m_varForTotalField;   // 全局场平滑方差
    float m_learningRate;       // 学习率
};
```

---

## 3. 刚性配准（Rigid Registration）

### 3.1 算法原理

刚性配准假设图像间的变换由 **旋转矩阵 R** 和 **平移向量 T** 组成：

$$\mathbf{x}_{ref} = R \cdot \mathbf{x}_{mov} + T$$

**3D 刚性变换自由度：6**（3平移 + 3旋转）  
**3D 仿射变换自由度：12**（旋转 + 平移 + 缩放 + 剪切）

优化目标：

$$\min_{R,T} \mathcal{L}(I_{ref}, T(I_{mov}; R, T))$$

其中 $\mathcal{L}$ 为相似度量（MSE 或 NMI）。

### 3.2 优化流程

```
输入：Reference Image, Moving Image, PARAMETER_t
  │
  ├── 多分辨率金字塔（可选，bMultiResolution=true）
  │     Level N（最粗）→ Level N-1 → ... → Level 0（原始分辨率）
  │
  ├── 每层优化（梯度下降）：
  │     while(iter < MaxIterationNum && step > MinIterationStep):
  │       计算图像相似度量（MSE 或 NMI）
  │       有限差分估计梯度
  │       更新变换参数
  │       if 度量值提升: step *= RelaxationFactor
  │
  └── 输出：4×4 变换矩阵、平移向量、相似度值
```

### 3.3 关键 API

```cpp
// CPU 刚性/仿射配准（文件：McsfAlgoRegistration.h）
namespace Mcsf {
    bool ImageRegistrationRigid(
        PARAMETER_t para,
        const short* pReferenceImageData,
        const short* pMovingImageData,
        unsigned refWidth, unsigned refHeight, unsigned refSlice,
        unsigned movWidth, unsigned movHeight, unsigned movSlice,
        double refSpacingX, double refSpacingY, double refSpacingZ,
        double movSpacingX, double movSpacingY, double movSpacingZ,
        double* dResultMatrix,       // 输出：3×3 旋转矩阵
        double* dResultOffset,       // 输出：3D 偏移
        double* dResultTranslation,  // 输出：3D 平移
        double& dResultMetric,       // 输出：最终相似度值
        short* pOutImage,            // 输出：配准后图像
        IProgress* pProgress = NULL
    );

    // 4×4 矩阵版本
    bool ImageRegistrationRigidMatrix16(
        PARAMETER_t para,
        const short* pRef, const short* pMov,
        unsigned refSize[3], unsigned movSize[3],
        double refSpacing[3], double movSpacing[3],
        double dResultMatrix[16],   // 输出：4×4 变换矩阵
        double& dResultMetric,
        short* pOutImage = NULL,
        IProgress* pProgress = NULL
    );

    // 重采样（按变换矩阵对 Moving 图像重采样到 Reference 空间）
    int ResampleForCascadeRegistration(
        unsigned int dim,
        short* MoveImageData,
        unsigned int nRefWid, unsigned int nRefHei, unsigned int nRefSlice,
        unsigned int nMovWid, unsigned int nMovHei, unsigned int nMovSlice,
        double dRefSpacingX, double dRefSpacingY, double dRefSpacingZ,
        double dMovSpacingX, double dMovSpacingY, double dMovSpacingZ,
        double* TransformMatrix,     // 4×4 变换矩阵
        short* pOutImage,
        InterpolateType interpolator = LINEAR
    );
}
```

### 3.4 参数推荐值

| 场景 | MaxIterationNum | MaxIterationStep | RelaxationFactor | 相似度量 |
|------|----------------|-----------------|-----------------|--------|
| CT-CT 粗配准 | 500 | 2.0 | 0.95 | NMI |
| CT-CT 精配准 | 1000 | 0.5 | 0.98 | NMI |
| MR-MR 同模态 | 500 | 1.0 | 0.95 | MSE |
| CT-MR 多模态 | 800 | 1.0 | 0.95 | NMI |
| MR 脑灌注运动校正 | 300 | 0.5 | 0.95 | MSE |

### 3.5 使用示例

```cpp
// 脑神经运动校正（来自 McsfAlgoRegistrationApplication.cpp）
Mcsf::PARAMETER_t param;
param.Init(3, 1000, 0.1);   // dim=3, bins=1000, scale=0.1
param.SetMaxIterationStep(2);
param.SetMinIterationStep(0.01);
param.SetRelaxationFactor(0.95);
param.SetMaxIterationNum(500);
param.SetUseMultiResolution(true);
param.SetMetricOption(1);  // 1=NMI

double resultMatrix[16] = {0};
double resultMetric = 0;
short* pOutput = new short[refW * refH * refSlice];

bool ok = Mcsf::ImageRegistrationRigidMatrix16(
    param, pRefImg, pMovImg,
    refSize, movSize,
    refSpacing, movSpacing,
    resultMatrix, resultMetric, pOutput);
```

---

## 4. 仿射配准（Affine Registration）

### 4.1 算法原理

仿射变换在刚性基础上增加了缩放（Scale）和剪切（Shear）：

$$\mathbf{x}_{ref} = A \cdot \mathbf{x}_{mov} + T, \quad A \in \mathbb{R}^{3 \times 3}$$

3D 仿射自由度：**12**（矩阵 A 的 9 个元素 + 平移 3 个）

通过设置 `m_bAffine = true` 启用仿射模式：

```cpp
param.SetAffine(true);  // 启用仿射
param.SetRotate(true);  // 包含旋转分量
```

### 4.2 典型应用

- MR 多序列配准（T1/T2/FLAIR）
- CT-PET 配准（不同扫描仪/床位）
- 4D CT 相位间配准

---

## 5. FFD 形变配准（B-Spline Free-Form Deformation）

### 5.1 算法原理

FFD 使用 **三次 B 样条（Cubic B-Spline）** 控制点网格描述形变场：

$$\mathbf{T}(\mathbf{x}) = \mathbf{x} + \sum_{l,m,n} \Phi_{l,m,n}(\mathbf{x}) \cdot \mathbf{c}_{i+l,j+m,k+n}$$

其中 $\Phi_{l,m,n}$ 是三次 B 样条基函数，$\mathbf{c}$ 是控制点位移。

**多格网（Multi-Grid）策略：**
- 从稀疏格网（低频形变）到密集格网（高频细节）逐步优化
- 每层格网密度是上层的 2 倍

**金字塔（Pyramid）策略：**
- 从低分辨率到高分辨率逐层细化

```
控制点网格密度: 4 → 8 → 16 → 32 (pixels/cell)
图像分辨率:    1/4 → 1/2 → 原始
```

### 5.2 关键 API

```cpp
// 文件：algo-registration/McsfAlgoMultiGridFFD.h
namespace Mcsf {
    bool MultiGridFFD(
        MultiGridSetting* Setting,     // 配准参数
        short* pReferenceImageData,
        short* pMovingImageData,
        unsigned int* pReferenceImageSize,
        unsigned int* pMovingImageSize,
        double* pReferenceImagePixelSpacing,
        double* pMovingImagePixelSpacing,
        double* pResultParameter,      // 输出：B-Spline 控制点参数
        short* pOutImage,              // 输出：配准后图像
        float* FFDpDis                 // 输出：形变场（体素位移）
    );
}
```

### 5.3 参数配置示例

```cpp
Mcsf::MultiGridSetting setting;
setting.dim = 3;
setting.PyramidSelfStart = true;
setting.PyramidOrder = 3;           // 3 层金字塔
setting.MultiGridSelfStart = true;
setting.MultiGridDensity = 4;       // 最密格网密度 2^4=16 个控制点/维
setting.OptimizationOrder = 3;      // 从第3层开始优化

// 若已有刚性配准结果，可用作初始形变
if (hasRigidResult) {
    setting.SetIfUseInitialDeformation(true);
    setting.SetInitialDeformation(pRigidDeformField);
}

float* pDeformField = new float[refW * refH * refSlice * 3];
double* pBSplineParams = new double[setting.ComputeParameterNumber()];
Mcsf::MultiGridFFD(&setting, pRef, pMov, refSize, movSize, 
                    refSpacing, movSpacing,
                    pBSplineParams, pOutput, pDeformField);
```

### 5.4 形变场格式

输出形变场 `FFDpDis` 为三通道浮点数组，格式为：
- `pDis[0 * N ... 1 * N - 1]`：X 方向位移（体素）
- `pDis[1 * N ... 2 * N - 1]`：Y 方向位移（体素）
- `pDis[2 * N ... 3 * N - 1]`：Z 方向位移（体素）

其中 `N = Width × Height × Slices`。

---

## 6. Demons 形变配准

### 6.1 算法原理

Demons 算法基于**光流方程（Optical Flow）**，将图像配准视为液体流动问题。

**光流力（Optical Force）计算（Thirion, 1998）：**

$$\mathbf{f}(\mathbf{x}) = \frac{(I_{ref}(\mathbf{x}) - I_{mov}(\mathbf{x})) \cdot \nabla I_{ref}(\mathbf{x})}{|\nabla I_{ref}(\mathbf{x})|^2 + \alpha^2 (I_{ref}(\mathbf{x}) - I_{mov}(\mathbf{x}))^2}$$

**位移场更新：**
$$\mathbf{d}^{t+1}(\mathbf{x}) = G_\sigma * (\mathbf{d}^t(\mathbf{x}) + s \cdot \mathbf{f}(\mathbf{x}))$$

其中 $G_\sigma$ 是高斯平滑核，$s$ 是更新步长，$\alpha$ 是力归一化系数。

### 6.2 类层次结构

```
DemonsInterface (纯虚基类)
    └── BaseDemons
            ├── MultiGridDemons        ← 多分辨率 Demons
            ├── InertialDemons         ← 惯性 Demons
            ├── InertialNMIDemons      ← NMI 惯性 Demons
            └── LCCDemons              ← LCC Demons
```

### 6.3 BaseDemons 接口

```cpp
// 文件：algo-registration/McsfAlgoBaseDemons.h
class BaseDemons : implements DemonsInterface {
public:
    // 参数设置
    void SetAlpha(float alpha);       // 力归一化系数（典型值：1.0）
    void SetSigma(float sigma);       // 高斯平滑 σ（典型值：3.0~10.0）
    void SetStep(float step);         // 更新步长（典型值：0.1~1.0）
    void SetCriteria(float criteria); // 收敛阈值（典型值：0.001）
    void SetItt(unsigned int itt);    // 最大迭代次数（典型值：100~500）
    void SetRefMask(unsigned char* pRefMask); // 配准区域掩码

    // 核心接口
    bool CalculateOpticalForce(short* RefImg, short* MovImg, int* iDim, 
                                float* Force, float& fError);
    bool CalculateMovingField(float* pForce, float* pDis, int* iDim);
    bool ApplyMovingField(short* MovImg, short* MovImgNew, int* iDim, float* pDis);
    bool DemonsRegistration(short* RefImg, short* MovImg, int* iDim, float* Disp);
};
```

### 6.4 MultiGridDemons

```cpp
// 文件：algo-registration/McsfAlgoMultiGridDemons.h
class MultiGridDemonsGeneral {
public:
    // deepth: 金字塔层数（从 deepth 到 deepEnd）
    // deepEnd: 最精细层（通常为1）
    MultiGridDemonsGeneral(unsigned deepth, unsigned deepEnd, BaseDemons** pDemons);
    bool DoRegistration(short* RefImg, short* MovImg, int* iDim, float* Disp);
};

class MultiGridDemons : public MultiGridDemonsGeneral {
public:
    MultiGridDemons(unsigned deepth, unsigned deepEnd, BaseDemons* Demons);
};
```

### 6.5 使用示例

```cpp
// 基础 Demons
BaseDemons demons(1.0f, 3.0f, 0.5f, 0.001f, 200, false);
float* pDis = new float[w * h * d * 3]();
demons.DemonsRegistration(pRef, pMov, iDim, pDis);

// 多格网 Demons（4 层）
const int N = 4;
BaseDemons** MTT = new BaseDemons*[N];
for (int i = 0; i < N; i++) {
    MTT[i] = new BaseDemons(1.0f, 3.0f, 0.5f, 0.001f, 50, false);
}
MultiGridDemonsGeneral mg(4, 1, MTT);
mg.DoRegistration(pRef, pMov, iDim, pDis);
```

---

## 7. LCC Demons 配准

### 7.1 算法原理

LCC（Local Cross-Correlation）Demons 用**局部互相关**代替强度差异作为相似度量，对多模态配准或对比度不一致情况更鲁棒。

**局部互相关（LCC）：**

$$LCC(\mathbf{x}) = \frac{\left(\sum_{\mathbf{y} \in N(\mathbf{x})} (I_{ref}(\mathbf{y}) - \bar{I}_{ref})(I_{mov}(\mathbf{y}) - \bar{I}_{mov})\right)^2}{\sum_{\mathbf{y}} (I_{ref} - \bar{I}_{ref})^2 \cdot \sum_{\mathbf{y}} (I_{mov} - \bar{I}_{mov})^2}$$

**LCC 力的计算（Hermosillo et al.）：**

$$\mathbf{f}(\mathbf{x}) = -\frac{\partial LCC}{\partial \mathbf{d}} = -\frac{2 E_{ref \cdot mov}}{E_{ref}^2} (\mathbf{x} - \bar{\mathbf{x}}_{mov}) \nabla I_{ref} + \frac{2 E_{ref \cdot mov}^2}{E_{ref}^3} (I_{ref} - \bar{I}_{ref}) \nabla I_{ref}$$

### 7.2 LCCDemons 接口

```cpp
// 文件：algo-registration/McsfAlgoLCCDemons.h
class LCCDemons : public BaseDemons {
public:
    // sigma:        局部窗口的高斯 σ（控制邻域大小）
    // step:         更新步长（学习率）
    // criteria:     收敛阈值
    // smooth_sigma: 力场高斯平滑 σ
    // bRelax:       是否使用自适应步长（Relaxation 策略）
    LCCDemons(float sigma, float step, float criteria, int maxItt, 
              float smooth_sigma, bool bRelax = false);
    
    bool DemonsRegistration(short* RefImg, short* MovImg, int* iDim, float* Disp);
    bool CalculateOpticalForce(short* RefImg, short* MovImg, int* iDim, 
                               float* Force, float& fError);
    bool CalculateMovingField(float* pForce, float* pDis, int* iDim);
};
```

### 7.3 Relaxation 策略

当 `bRelax=true` 时，启用自适应步长：
- 如果误差不下降，步长乘以 0.5
- 最多连续 5 次步长减半后停止

```cpp
// 推荐多格网 LCC 配置（来自注释文档）
const int N = 4;
BaseDemons** MTT = new BaseDemons*[N];
MTT[0] = new LCCDemons(4, 0.2f, 0.00001f, 30, 6);
MTT[1] = new LCCDemons(4, 0.2f, 0.00001f, 30, 6);
MTT[2] = new LCCDemons(4, 0.2f, 0.00001f, 30, 6);
MTT[3] = new LCCDemons(4, 0.2f, 0.00001f, 30, 6);
std::unique_ptr<MultiGridDemonsGeneral> mg(new MultiGridDemonsGeneral(4, 1, MTT));
mg->DoRegistration(pRef, pMov, iDim, pDis);
```

---

## 8. 惯性 Demons 配准（Inertial Demons）

### 8.1 算法原理

惯性 Demons 在标准 Demons 的位移场更新中引入**动量项**（类似物理惯性），提高收敛速度并避免局部极值：

$$\mathbf{v}^{t+1} = \beta \cdot \mathbf{v}^t + \mathbf{f}^t$$
$$\mathbf{d}^{t+1} = \mathbf{d}^t + G_\sigma * \mathbf{v}^{t+1}$$

其中 $\beta$ 是动量衰减系数（`m_beta`）。

### 8.2 接口

```cpp
// 文件：algo-registration/McsfAlgoInertialDemons.h
class InertialDemons : implements BaseDemons {
public:
    InertialDemons(float alpha, float beta, float sigma, float step, 
                   float criteria, unsigned int maxItt, bool multi);
    bool DemonsRegistration(short* RefImg, short* MovImg, int* iDim, float* Disp);
};

// NMI 版本的惯性 Demons
class InertialNMIDemons : implements BaseNMIDemons {
public:
    InertialNMIDemons(float alpha, float beta, float sigma, float step,
                      float criteria, unsigned int maxItt, bool multi);
    bool DemonsRegistration(short* RefImg, short* MovImg, int* iDim, float* Disp);
};
```

---

## 9. 对称归一化配准（SyN Registration）

### 9.1 算法原理

SyN（Symmetric Normalization，来自 ANTs 框架）是当前最先进的**微分同胚（Diffeomorphic）**形变配准算法。

**核心思想：** 同时从参考图像和运动图像方向各优化半个形变，保证整体变换的微分同胚性（拓扑保持）。

**变换模型：**
$$\phi(\mathbf{x}, 1) = \phi_1 \circ \phi_2^{-1}(\mathbf{x})$$

**优化能量：**
$$E = \sum_{x} M(I_{ref} \circ \phi_1^{-1}, I_{mov} \circ \phi_2^{-1}) + R(\phi_1, \phi_2)$$

其中 $M$ 是相似度量（LCC 或 MI），$R$ 是正则化项（高斯平滑形变场）。

### 9.2 SyN 相似度量

```cpp
// 文件：algo-registrationgpu/McsfAlgoRegSynMetric.h
namespace Mcsf {
    class SynMetric;   // CPU 版
    class SynMetricG;  // GPU 版
    
    // 可用度量：LCC（局部互相关），MI（互信息），组合度量
}
```

### 9.3 CPU SyN 接口

```cpp
// 文件：algo-registrationgpu/McsfAlgoRegSyn.h
namespace Mcsf {
    // 2D/3D SyN 配准（CPU）
    void SynReg(
        const std::vector<SynParameter>& regParam,  // 多分辨率参数列表
        SynMetric& metric,                           // 相似度量对象
        const float* pRef, const char* pRefMask,    // 参考图像及掩码
        const float* pMoving, const char* pMovMask, // 运动图像及掩码
        const unsigned uiSize[3],                   // 图像尺寸
        float* pOutMoving,                          // 输出：配准后运动图像
        float* pOutRef = nullptr,                   // 输出：配准后参考图像（可选）
        float* pOutDis = nullptr,                   // 输出：正向形变场
        float* pOutInvDis = nullptr,                // 输出：逆向形变场
        unsigned threads = 1                        // 线程数
    );
    
    // 3D SyN 配准（GPU）
    void SynReg3DG(
        const std::vector<SynParameter>& regParam,
        SynMetricG& metric,
        const float* pRef, const char* pRefMask,
        const float* pMoving, const char* pMovMask,
        const unsigned uiSize[3],
        float* pOutMoving, float* pOutRef = nullptr,
        float* pOutDis = nullptr, float* pOutInvDis = nullptr
    );
}
```

### 9.4 典型多分辨率配置

```cpp
// 3 层金字塔 SyN 配置（粗→细）
std::vector<Mcsf::SynParameter> params = {
    // {迭代次数, 缩减因子, 图像平滑σ, 更新场方差, 全局场方差, 学习率}
    {100, 4.0f, 3.0f, 3.0f, 0.5f, 0.25f},  // 1/4 分辨率
    {70,  2.0f, 2.0f, 2.0f, 0.5f, 0.25f},  // 1/2 分辨率
    {50,  1.0f, 0.0f, 1.0f, 0.5f, 0.25f},  // 原始分辨率
};
```

---

## 10. GPU 加速配准

### 10.1 GPU 刚性配准

```cpp
// 文件：algo-registrationgpu/McsfAlgoRegistrationG.h
bool ImageRegistrationRigidG(
    Mcsf::PARAMETER_t para,
    const short* pReferenceImageData_h,
    const short* pMovingImageData_h,
    unsigned refWidth, unsigned refHeight, unsigned refSlice,
    unsigned movWidth, unsigned movHeight, unsigned movSlice,
    double refSpacingX, double refSpacingY, double refSpacingZ,
    double movSpacingX, double movSpacingY, double movSpacingZ,
    double* dResultMatrix_h,        // 输出：3×3 旋转矩阵
    double* dResultOffset_h,        // 输出：偏移
    double* dResultTranslation_h,   // 输出：平移
    double& dResultMetric,
    short* pOutImage_h,
    Mcsf::IProgress* pProgress = NULL
);
```

**实现技术：**
- CUDA 并行化相似度量计算（互信息直方图、均方误差）
- 每个 CUDA 线程负责一个体素的计算
- 共享内存优化直方图累加
- Thrust 库实现并行规约

### 10.2 GPU Demons 配准

```cpp
// 文件：algo-registrationgpu/McsfAlgoMultiGridDemonsG.h
class MultiGridDemonsGeneralG : public MultiGridRegistrationInterfaceG {
public:
    MultiGridDemonsGeneralG(unsigned deepth, unsigned deepEnd, BaseDemonsG** pDemons);
    
    bool DoRegistration(const short* h_refImg, const short* h_movImg,
                       const int img_dim[3], float* h_dis,
                       double dStart = 0, double dEnd = 1,
                       IProgress* pProgress = 0, short* h_outImg = NULL);
    
    // 高级接口：支持指定配准分辨率（与图像分辨率不同）
    bool DoRegistration_withBuffer(
        const short* h_refImg, const short* h_movImg,
        const int img_dim[3], const int reg_dim[3], float* h_dis,
        double dStart = 0, double dEnd = 1,
        IProgress* pProgress = 0, short* h_outImg = NULL
    );
};
```

**CUDA Kernels（形变配准流水线）：**
- `CalculateOpticalForce.cu`：并行计算光流力
- `CalculateMovingField.cu`：并行更新位移场
- `ApplyMovingField.cu`：并行应用形变
- `Histogram.cu` / `Entropy.cu`：并行直方图与熵计算
- `JointHistogram.cu`：并行联合直方图
- `MutualInformationForce.cu`：MI 驱动的形变力
- `NMutualInformationForce.cu`：NMI 驱动的形变力
- `JointEntropyForce.cu`：联合熵驱动的形变力

### 10.3 GPU 重采样

```cpp
// 文件：algo-registrationgpu/McsfAlgoRegistrationG.h
int ResampleForRigidRegistrationG(
    unsigned int dim,
    short* MoveImageData,
    unsigned int nRefWid, unsigned int nRefHei, unsigned int nRefSlice,
    unsigned int nMovWid, unsigned int nMovHei, unsigned int nMovSlice,
    double dRefSpacingX, double dRefSpacingY, double dRefSpacingZ,
    double dMovSpacingX, double dMovSpacingY, double dMovSpacingZ,
    double* TransformMatrix,
    short* pOutImage,
    InterpolateType interpolator = LINEAR
);

// 浮点版本（支持级联变换）
int ResampleForRegistrationMatrix16G(
    unsigned int dim,
    float* MoveImageData,
    // ...（同上但输入输出为 float）
    double* TransformMatrix,
    float* pOutImage,
    bool bMovBoundExtend = false,
    InterpolateType interpolator = LINEAR
);
```

### 10.4 GPU 内存管理

```cpp
// 查询 GPU 可用内存
bool GetGPUMem(size_t& freeBytes, size_t& totalBytes);

// 预估 GPU 内存需求峰值
double GetGPUPeakMemUsed(Mcsf::PARAMETER_t para, 
                          const short* pRefImg,
                          const unsigned refSize[3],
                          const unsigned movSize[3]);
```

---

## 11. 深度学习配准（DL Registration）

### 11.1 深度学习形变配准（McsfAlgoDLRegistration）

基于 **VoxelMorph** 框架，使用卷积神经网络直接预测形变场。

**支持的网络：**
- `R3Net`：3D 配准网络（`R3Net.h` / `R3Net.cpp`）
- `Vnet2d_bn_prelu_down4`：2D 配准网络（下采样 4 倍，BatchNorm + PReLU）
- `VecIntCPU`：速度场积分（Scaling and Squaring，将速度场积分为微分同胚形变场）

```cpp
// 文件：algo-registration/McsfAlgoDLRegistration.h
namespace Mcsf {
    class McsfAlgoDLRegistration {
    public:
        // 初始化网络
        bool initialize(
            const std::string netName,   // 网络名称
            const std::string netFile,   // 模型文件路径（.engine 或 .onnx）
            int iDim,                    // 维度（2 或 3）
            bool bVecInt,                // 是否使用速度场积分
            float downFactor = 0.5f,     // 下采样因子（网络推理时）
            int nsteps = 7               // 速度场积分步数（2^7=128）
        );
        
        // 3D 形变配准推理
        bool GetDeformationField(
            const float* pImgRef,        // 参考图像（归一化浮点）
            const float* pImgMov,        // 运动图像
            const unsigned uiSize[3],    // 图像尺寸
            float* dispOut,              // 输出：形变场（3 通道）
            int padValue = 0             // 填充值
        );
        
        // 2D 动态序列形变配准
        bool GetDeformationFieldDynamic2D(
            const float* pImgN,          // 所有帧的图像数据
            const unsigned uiSize[3],    // 每帧图像尺寸
            unsigned refIndex,           // 参考帧索引
            unsigned imgNum,             // 总帧数
            int batchSize,               // 推理批大小
            float* dispOut               // 输出形变场
        );
    };
}
```

### 11.2 深度学习刚性配准

基于深度神经网络的**粗配准（Rough Rigid Registration）**，用于 CT-CT 配准的初始化。

```cpp
namespace Mcsf {
    // 初始化/释放 DL 模型
    void DLRoughRigidRegCTCT_InitialModel(void** model);
    void DLRoughRigidRegCTCT_ReleaseModel(void** model);
    
    // 运行 DL 刚性配准
    // 返回值：0=成功, 1=空指针, 2=预处理错误, 3=模型错误
    int DLRoughRigidRegCTCT_Run(
        short* pRefImageNoBed,   // 参考图像（已去床板）
        unsigned int* uiRefSize,
        double* dRefSpacing,
        short* pMovImageNoBed,   // 运动图像（已去床板）
        unsigned int* uiMovSize,
        double* dMovSpacing,
        void* model,             // DL 模型指针
        double pPriorMatrix[16]  // 输出：4×4 变换矩阵
    );
}
```

**使用场景：** 作为传统配准的初始化，特别是在 CT 图像方向差异较大时（如轴位/矢状位），提供更好的初始位置。

---

## 12. 应用层配准接口

### 12.1 乳腺 MRI 配准

```cpp
// 文件：McsfAlgoRegistrationApplication.h
bool BreastImageRegistration(
    short* pReferenceImageData,
    unsigned int* pReferenceImageSize,
    double* pReferenceImagePixelSpacing,
    unsigned int MovingImageNumber,   // 运动图像数量
    short** pMovingImageData,
    unsigned int* pMovingImageSize,
    double* pMovingImagePixelSpacing,
    unsigned int uiRegistrationMethodOption,  
    // 1=仅刚性, 2=仅形变, 3=刚性+形变
    short** pCorrectedMovingImageData,
    IProgress* pProgress = 0
);
```

**内部流程：**
1. 刚性配准（NMI 度量，多分辨率）
2. （可选）FFD 形变配准（B-Spline 多格网）

### 12.2 脑神经运动校正

```cpp
bool MRNeuroMotionCorrection(
    PARAMETER_t* para,
    short** const pPhases,           // 所有时相数据
    unsigned int nWid, unsigned int nHei, unsigned int nSlice,
    unsigned int nPhase,             // 时相数量
    double dSpacingX, double dSpacingY, double dSpacingZ,
    double** const dResultMatrix,    // 输出：每个时相的旋转矩阵
    double** const dResultOffset,    // 输出：每个时相的偏移
    double* dResultMetric,           // 输出：每个时相的相似度值
    short** const pOutPhases,        // 输出：校正后的所有时相
    IProgress* pProgress = 0
);
```

**应用场景：** BOLD（功能 MRI）和 DTI（弥散张量成像）时间序列运动校正

### 12.3 MR-TOF 配准

```cpp
bool MRNeuroRegistration_TOF(
    CVolumeDataInfo& vMRData,     // 输入 MR 数据（含位置/方向信息）
    CVolumeDataInfo& vTOFData,    // 输入 TOF 数据
    double* transformMatrix,       // 输出：4×4 变换矩阵
    IProgress* pProgress = 0
);
```

**特点：** 自动处理不同坐标系（MR 在 LPS/RAS 下，ToF 使用设备坐标），通过 `dPosition` 和 `dOrientation` 信息对齐物理空间。

### 12.4 肝脏配准

```cpp
// 文件：McsfAlgoRegistrationApplicationLiver.cpp
// 使用 Demons 形变配准，针对肝脏形态变化优化参数
```

**关键参数：**
- 刚性阶段：宽松的 NMI 配准对齐整体位置
- 形变阶段：LCC Demons 处理呼吸运动形变

### 12.5 双能量配准

```cpp
// 文件：McsfAlgoRegistrationApplicationDualEnergy.cpp
// CT 双能量扫描（80kVp / 140kVp）配准，使用 MI 度量处理不同能量的对比度差异
```

### 12.6 4D 分析配准

```cpp
// 文件：McsfAlgoRegistrationApp4DAnalysis.cpp
// 4D CT/MR 时间序列配准，典型应用：
// - 4D CT 呼吸运动分析
// - 心脏 MR 运动分析
// 支持 GPU Demons 加速
```

### 12.7 MR 动态配准

```cpp
// 文件：McsfAlgoRegistrationAppMRDynamic.cpp
// MR 动态增强序列配准（DCE-MRI）
// 使用 SyN + LCC 或 Demons 形变配准
```

### 12.8 局部配准

```cpp
// 文件：McsfAlgoRegistrationAppLocal.cpp
// 针对感兴趣区域（ROI）的局部配准
// 先全局刚性配准，再对 ROI 内进行精细形变配准
```

### 12.9 点集刚性配准

```cpp
bool RT_rigidpointalign(
    double* movepts,      // 运动点集（N×dim）
    double* staticpts,    // 固定点集（N×dim）
    int N, int dim,       // 点数和维度
    double* rotationmatrix,  // 输出：旋转矩阵（dim×dim）
    double* trans            // 输出：平移向量（dim）
);
```

**算法：** SVD 分解求最优刚性变换（Kabsch 算法）：
$$R, T = \arg\min \sum_i ||p_i - (R q_i + T)||^2$$

---

## 13. 相似度量

### 13.1 均方误差（MSE）

$$MSE = \frac{1}{N} \sum_{\mathbf{x}} (I_{ref}(\mathbf{x}) - I_{mov}(\mathbf{x}))^2$$

**适用：** 同模态配准（CT-CT, MR-MR），对比度相同情况。  
**实现：** `MeanSquareErrorMetric.h/.cpp`

### 13.2 归一化互信息（NMI）

$$NMI = \frac{H(I_{ref}) + H(I_{mov})}{H(I_{ref}, I_{mov})}$$

其中 $H$ 是边缘熵，$H(I_{ref}, I_{mov})$ 是联合熵（通过联合直方图估计）。

**适用：** 多模态配准（CT-MR, PET-CT），强度关系非线性。  
**实现：** `MutualInformationMetric.h/.cpp`  
**参数：** `NumOfBins`（直方图 bins 数，典型值 64~256）

### 13.3 局部互相关（LCC）

$$LCC(\mathbf{x}) = \frac{\left(\sum_{\mathbf{y} \in N(\mathbf{x})} (I_r - \mu_r)(I_m - \mu_m)\right)^2}{\sum (I_r - \mu_r)^2 \sum (I_m - \mu_m)^2}$$

**适用：** 局部对比度变化（增强前后），LCC Demons。  
**实现：** 在 `LCC_Force` 类中实现

### 13.4 BDT（Block-based Distance Transform）

改进的 MSE 变体，在图像块级别计算，对噪声更鲁棒：  
**实现：** `MeanSquareErrorMetricBDT.h/.cpp`, `MutualInformationMetricBDT.h/.cpp`

---

## 14. 优化器

### 14.1 梯度下降（Gradient Descent）

```cpp
// 文件：Optimization.h/.cpp
class coptimization {
    double m_StepLength;         // 当前步长
    double m_MaximumStepLength;  // 最大步长
    double m_MinimumStepLength;  // 最小步长（收敛阈值）
    double m_RelaxationFactor;   // 步长松弛因子
    unsigned int m_MaximumIterations;
    double m_MagnitudeTolerance; // 梯度幅度容差
};
```

**工作原理：**
1. 计算当前参数下的度量值
2. 有限差分估计梯度
3. 按步长更新参数
4. 若度量值提升，步长乘以 RelaxationFactor（缩小）
5. 直到步长 < MinimumStepLength 或达到最大迭代次数

### 14.2 L-BFGS 优化器

适用于大参数空间（如 FFD 的控制点参数），利用历史梯度估计 Hessian 矩阵。

```cpp
// 文件：OptimizationLBFGS.h/.cpp
// 基于 liblbfgs 库实现
// 参数：m（历史梯度数，典型值5），epsilon（收敛阈值）
```

**优势：** 收敛速度远快于普通梯度下降，对 FFD 参数优化效果显著。

---

## 15. 重采样与插值

### 15.1 插值类型

```cpp
enum InterpolateType { 
    NN,       // 最近邻插值（最快，边缘粗糙）
    LINEAR,   // 三线性插值（平衡速度和质量，最常用）
    SIGMOID,  // Sigmoid 加权插值
    BSPLINE   // B样条插值（最高质量，最慢）
};
```

### 15.2 CPU 重采样

```cpp
// 按变换矩阵重采样
int ResampleForCascadeRegistration(
    unsigned int dim, short* MoveImageData,
    unsigned int nRefWid, unsigned int nRefHei, unsigned int nRefSlice,
    unsigned int nMovWid, unsigned int nMovHei, unsigned int nMovSlice,
    double dRefSpacing[3], double dMovSpacing[3],
    double* TransformMatrix,
    short* pOutImage,
    InterpolateType interpolator = LINEAR
);

// 按形变场重采样
int ResampleByDeformField(
    short* pMovImage, unsigned int* pMovSize, double* pMovSpacing,
    short* pRefImage, unsigned int* pRefSize, double* pRefSpacing,
    float* pDeformField,   // 3 通道位移场（mm）
    short* pOutImage
);
```

### 15.3 GPU 重采样

```cpp
// GPU 三线性插值（见 ResampleFunc.cu）
// CUDA texture memory 优化，自动处理边界
```

---

## 16. 性能对比

### 16.1 算法性能指标（典型 512×512×100 体积）

| 算法 | 精度 | 速度（CPU） | 速度（GPU） | 适用模态 | 变换自由度 |
|------|------|------------|------------|--------|-----------|
| 刚性配准 (MSE) | 中 | 5-30s | 1-5s | 同模态 | 6 DOF |
| 刚性配准 (NMI) | 高 | 10-60s | 2-10s | 多模态 | 6 DOF |
| 仿射配准 | 中高 | 10-60s | 2-15s | 多模态 | 12 DOF |
| FFD 形变 | 高 | 2-10min | — | 多模态 | 数千 |
| Demons | 中高 | 1-5min | 10-60s | 同模态 | 密集场 |
| Multi-Grid Demons | 高 | 2-8min | 20-120s | 同模态 | 密集场 |
| LCC Demons | 高 | 2-8min | — | 多模态 | 密集场 |
| SyN（CPU） | 最高 | 5-20min | — | 多模态 | 密集场 |
| SyN（GPU） | 最高 | — | 1-5min | 多模态 | 密集场 |
| DL 形变 | 高 | 30-120s | 0.5-5s | 同/多模态 | 密集场 |
| DL 刚性 | 中 | — | 0.5-2s | CT-CT | 6 DOF |

### 16.2 应用场景选择

| 临床应用 | 推荐算法 | 说明 |
|---------|---------|------|
| CT-CT 刚性配准 | DL刚性(初始化) + GPU刚性 | 速度快，精度高 |
| CT-MR 配准 | NMI 刚性 + SyN | 多模态，需要高精度 |
| 4D CT 呼吸门控 | GPU Multi-Grid Demons | 大量时相，速度优先 |
| 乳腺 MRI | 刚性 + FFD | 需要捕捉腺体形变 |
| 脑 MRI 运动校正 | MSE 刚性 | 速度优先，刚性假设合理 |
| 肝脏 MR 动态 | LCC Demons / SyN | 对比度变化大，需形变 |
| 神经外科术中 | 刚性 + 局部形变 | 精度优先 |
| 放疗计划 | FFD / SyN | 最高精度形变 |

---

## 附录：完整 API 速查表

### 配准函数

```cpp
// ====== 刚性/仿射 ======
bool ImageRegistrationRigid(PARAMETER_t para, ...);
bool ImageRegistrationRigidMatrix16(PARAMETER_t para, ...);

// ====== FFD 形变 ======
bool MultiGridFFD(MultiGridSetting*, short* ref, short* mov, ...);

// ====== Demons ======
BaseDemons::DemonsRegistration(ref, mov, dim, disp);
MultiGridDemons::DoRegistration(ref, mov, dim, disp);
LCCDemons::DemonsRegistration(ref, mov, dim, disp);

// ====== SyN ======
void SynReg(params, metric, ref, refMask, mov, movMask, size, outMov, ...);
void SynReg3DG(params, metric, ref, refMask, mov, movMask, size, outMov, ...);

// ====== GPU 刚性 ======
bool ImageRegistrationRigidG(para, ref, mov, ...);

// ====== GPU Demons ======
MultiGridDemonsGeneralG::DoRegistration(ref, mov, dim, dis, ...);

// ====== DL 配准 ======
McsfAlgoDLRegistration::GetDeformationField(ref, mov, size, disp);
int DLRoughRigidRegCTCT_Run(ref, refSize, refSpacing, mov, movSize, movSpacing, model, matrix);

// ====== 应用层 ======
bool BreastImageRegistration(...);
bool MRNeuroMotionCorrection(...);
bool MRBrainPerfusionMotionCorrection(...);
bool MRNeuroRegistration_TOF(mrData, tofData, matrix);
bool RT_rigidpointalign(movPts, staticPts, N, dim, R, T);

// ====== 重采样 ======
int ResampleForCascadeRegistration(dim, mov, refSize, movSize, refSpacing, movSpacing, matrix, out, interp);
int ResampleForRigidRegistrationG(dim, mov, refSize, movSize, spacing, matrix, out, interp);
int ResampleByDeformField(mov, movSize, movSpacing, ref, refSize, refSpacing, deform, out);
```
