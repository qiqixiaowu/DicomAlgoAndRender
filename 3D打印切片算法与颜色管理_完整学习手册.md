# 3D打印切片算法与颜色管理算法 — 完整学习手册

> 本文档系统性地覆盖3D打印中两大核心算法领域：**切片算法（Slicing）** 和 **颜色管理算法（Color Management）**。
> 从基础概念到高级实现，包含伪代码、数学公式和工程实践。

---

## 目录

- [第一部分：3D打印切片算法](#第一部分3d打印切片算法)
  - [1. 切片算法概述](#1-切片算法概述)
  - [2. STL文件解析与数据结构](#2-stl文件解析与数据结构)
  - [3. 几何求交——核心数学基础](#3-几何求交核心数学基础)
  - [4. 轮廓提取与排序](#4-轮廓提取与排序)
  - [5. 填充算法（Infill）](#5-填充算法infill)
  - [6. 外壳/周界生成（Perimeter/Shell）](#6-外壳周界生成perimetershell)
  - [7. 支撑结构生成（Support）](#7-支撑结构生成support)
  - [8. 自适应切片（Adaptive Slicing）](#8-自适应切片adaptive-slicing)
  - [9. 工具路径规划与优化（Toolpath）](#9-工具路径规划与优化toolpath)
  - [10. G-code生成](#10-g-code生成)
  - [11. 高级切片技术](#11-高级切片技术)
- [第二部分：颜色管理算法](#第二部分颜色管理算法)
  - [12. 颜色管理基础](#12-颜色管理基础)
  - [13. 颜色空间与转换](#13-颜色空间与转换)
  - [14. ICC色彩配置文件](#14-icc色彩配置文件)
  - [15. 3D打印中的多色/多材料管理](#15-3d打印中的多色多材料管理)
  - [16. 颜色映射与插值算法](#16-颜色映射与插值算法)
  - [17. 抖动算法（Dithering）](#17-抖动算法dithering)
  - [18. 颜色校准流程](#18-颜色校准流程)
  - [19. 体素级颜色管理（全彩3D打印）](#19-体素级颜色管理全彩3d打印)
- [第三部分：工程实践与参考实现](#第三部分工程实践与参考实现)
  - [20. 开源切片器架构分析](#20-开源切片器架构分析)
  - [21. 性能优化策略](#21-性能优化策略)
  - [22. 推荐学习路径与资源](#22-推荐学习路径与资源)

---

# 第一部分：3D打印切片算法

## 1. 切片算法概述

### 1.1 什么是切片

切片（Slicing）是将3D模型（通常是STL/OBJ/AMF格式）沿Z轴方向以固定或自适应层厚切分为一系列2D截面，然后为每一层生成打印路径（工具路径），最终输出G-code指令控制3D打印机运动的过程。

```
┌─────────────────────────────────────────────────┐
│              切片流水线总览                        │
│                                                   │
│  3D模型(STL) → 层切分 → 轮廓提取 → 填充生成       │
│       → 支撑生成 → 路径优化 → G-code输出           │
└─────────────────────────────────────────────────┘
```

### 1.2 切片的核心步骤

| 步骤 | 输入 | 输出 | 关键算法 |
|------|------|------|----------|
| 1. 模型加载 | STL/OBJ文件 | 三角面片网格 | 文件解析 |
| 2. 模型修复 | 三角面片网格 | 水密网格 | 孔洞填充、法线一致化 |
| 3. 层切分 | 水密网格 | 每层Z高度 | 等距/自适应 |
| 4. 求交计算 | 网格 + Z平面 | 2D线段集合 | 三角形-平面求交 |
| 5. 轮廓连接 | 2D线段集合 | 有序闭合多边形 | 线段连接、排序 |
| 6. 填充生成 | 闭合多边形 | 填充路径 | 扫描线、六边形等 |
| 7. 外壳生成 | 闭合多边形 | 偏移轮廓 | 多边形偏移 |
| 8. 支撑生成 | 悬空区域 | 支撑结构 | 悬垂检测、树形支撑 |
| 9. 路径优化 | 所有路径 | 优化路径 | 旅行商问题(TSP)近似 |
| 10. G-code | 优化路径 | G-code文件 | 路径序列化 |

### 1.3 关键参数

```
层高 (Layer Height):        0.05mm ~ 0.3mm，影响精度和打印时间
喷嘴直径 (Nozzle Diameter):  通常0.4mm，决定挤出宽度
填充密度 (Infill Density):   0% ~ 100%，影响强度和材料用量
外壳层数 (Perimeters):       通常2~3层，影响表面质量
顶层/底层层数:               影响3D打印的顶面质量
打印速度:                    20~150 mm/s
```

---

## 2. STL文件解析与数据结构

### 2.1 STL文件格式

STL是3D打印最常用的文件格式，用三角面片网格表示物体表面。

**ASCII STL格式：**
```
solid model
  facet normal nx ny nz
    outer loop
      vertex x1 y1 z1
      vertex x2 y2 z2
      vertex x3 y3 z3
    endloop
  endfacet
  ...
endsolid model
```

**二进制STL格式（更常用，更紧凑）：**
```
Header:     80字节（文件描述）
Count:      4字节（uint32，三角面片数量）
Per Facet:  50字节
  - Normal:    3 × float32 (12字节)
  - Vertices:  3 × 3 × float32 (36字节)
  - AttrByte:  uint16 (2字节)
```

### 2.2 数据结构设计

```cpp
// 顶点
struct Vertex {
    float x, y, z;
};

// 三角面片
struct Triangle {
    Vertex normal;      // 法线
    Vertex v[3];        // 三个顶点
    uint16_t attr;      // 属性字节（可用于颜色）
};

// 网格
struct Mesh {
    std::vector<Triangle> triangles;
    
    // 边界框
    Vertex min, max;
    
    // 统计信息
    size_t triangleCount;
};
```

### 2.3 高效解析（二进制STL）

```cpp
Mesh loadBinarySTL(const std::string& filepath) {
    std::ifstream file(filepath, std::ios::binary);
    Mesh mesh;
    
    // 跳过80字节头部
    char header[80];
    file.read(header, 80);
    
    // 读取三角面片数量
    uint32_t count;
    file.read(reinterpret_cast<char*>(&count), 4);
    mesh.triangles.resize(count);
    
    // 批量读取所有三角形
    file.read(reinterpret_cast<char*>(mesh.triangles.data()), 
              count * 50);
    
    // 计算边界框
    computeBoundingBox(mesh);
    
    return mesh;
}
```

### 2.4 模型修复

切片前需要确保模型是**水密的（watertight）**，即没有孔洞或重叠面。

**常见问题与修复：**

| 问题 | 检测方法 | 修复方法 |
|------|----------|----------|
| 孔洞 | 边只被一个三角形引用 | 三角化补洞 |
| 法线不一致 | 相邻三角形法线方向矛盾 | 统一法线方向 |
| 自相交 | 三角形之间相交检测 | 布尔运算修复 |
| 重叠面 | 面片距离过近 | 删除冗余面 |
| 非流形边 | 边被>2个三角形引用 | 分离或删除 |

---

## 3. 几何求交——核心数学基础

### 3.1 三角形与水平面求交

这是切片算法最核心的计算：对每个三角面片，求其与Z=Z₀平面的交线。

**数学原理：**

给定三角形三个顶点 $P_0(x_0,y_0,z_0)$, $P_1(x_1,y_1,z_1)$, $P_2(x_2,y_2,z_2)$ 和切平面 $Z = Z_0$：

1. 计算每个顶点相对于切平面的符号：
$$d_i = z_i - Z_0, \quad i = 0,1,2$$

2. 分类：
   - 若所有 $d_i$ 同号（全正或全负）→ 无交点，跳过
   - 若某个 $d_i = 0$ → 顶点恰在平面上（特殊处理）
   - 否则 → 有2个交点，形成一条线段

3. 线性插值求交点：

当 $d_0$ 和 $d_1$ 异号时，边 $P_0P_1$ 与平面的交点为：
$$t = \frac{Z_0 - z_0}{z_1 - z_0} = \frac{-d_0}{d_1 - d_0}$$

$$P_{\text{intersect}} = P_0 + t \cdot (P_1 - P_0)$$

即：
$$x = x_0 + t \cdot (x_1 - x_0)$$
$$y = y_0 + t \cdot (y_1 - y_0)$$

### 3.2 求交实现

```cpp
struct Point2D {
    float x, y;
};

struct LineSegment2D {
    Point2D p1, p2;
};

// 单个三角形与Z平面的求交
bool sliceTriangle(const Triangle& tri, float z0, LineSegment2D& outSeg) {
    float d[3];
    d[0] = tri.v[0].z - z0;
    d[1] = tri.v[1].z - z0;
    d[2] = tri.v[2].z - z0;
    
    // 统计正负号
    int posCount = 0, negCount = 0;
    for (int i = 0; i < 3; i++) {
        if (d[i] > 0) posCount++;
        else if (d[i] < 0) negCount++;
    }
    
    // 全在同侧，无交点
    if (posCount == 3 || negCount == 3) return false;
    
    // 找到两条与平面相交的边
    Point2D intersections[2];
    int idx = 0;
    
    for (int i = 0; i < 3; i++) {
        int j = (i + 1) % 3;
        if (d[i] * d[j] < 0) {  // 异号，有交点
            float t = -d[i] / (d[j] - d[i]);
            intersections[idx].x = tri.v[i].x + t * (tri.v[j].x - tri.v[i].x);
            intersections[idx].y = tri.v[i].y + t * (tri.v[j].y - tri.v[i].y);
            idx++;
        }
    }
    
    if (idx == 2) {
        outSeg.p1 = intersections[0];
        outSeg.p2 = intersections[1];
        return true;
    }
    return false;
}
```

### 3.3 批量求交与加速

```cpp
// 对整个网格在Z=z0处切片
std::vector<LineSegment2D> sliceMeshAtZ(const Mesh& mesh, float z0) {
    std::vector<LineSegment2D> segments;
    
    for (const auto& tri : mesh.triangles) {
        // 快速排除：三角形不在z0附近
        float zmin = std::min({tri.v[0].z, tri.v[1].z, tri.v[2].z});
        float zmax = std::max({tri.v[0].z, tri.v[1].z, tri.v[2].z});
        if (z0 < zmin || z0 > zmax) continue;
        
        LineSegment2D seg;
        if (sliceTriangle(tri, z0, seg)) {
            segments.push_back(seg);
        }
    }
    return segments;
}
```

**加速策略：**

1. **Z排序预过滤**：按三角形的Z范围排序，二分查找相关三角形
2. **BVH（层次包围盒）**：构建BVH树，快速剔除不相交的三角形子集
3. **空间网格哈希**：将三角形按Z区间分桶，只检查对应桶
4. **并行化**：每层切片独立，可多线程并行处理

```cpp
// BVH加速结构
struct BVHNode {
    Vertex bboxMin, bboxMax;
    std::vector<int> triangleIndices;  // 叶子节点
    BVHNode* left = nullptr;
    BVHNode* right = nullptr;
    
    bool intersectsZ(float z0) const {
        return z0 >= bboxMin.z && z0 <= bboxMax.z;
    }
};

// 使用BVH的切片
std::vector<LineSegment2D> sliceMeshBVH(const Mesh& mesh, 
                                         const BVHNode* bvh, 
                                         float z0) {
    std::vector<LineSegment2D> segments;
    std::stack<const BVHNode*> stack;
    stack.push(bvh);
    
    while (!stack.empty()) {
        const BVHNode* node = stack.top();
        stack.pop();
        
        if (!node->intersectsZ(z0)) continue;
        
        if (node->left == nullptr && node->right == nullptr) {
            // 叶子节点，检查实际三角形
            for (int idx : node->triangleIndices) {
                LineSegment2D seg;
                if (sliceTriangle(mesh.triangles[idx], z0, seg)) {
                    segments.push_back(seg);
                }
            }
        } else {
            if (node->left) stack.push(node->left);
            if (node->right) stack.push(node->right);
        }
    }
    return segments;
}
```

---

## 4. 轮廓提取与排序

### 4.1 从线段到闭合多边形

求交得到的是一组**无序的2D线段**，需要将它们连接成**有序的闭合多边形（轮廓）**。

```
求交结果（无序线段）          连接后的闭合轮廓
  ┌──┐    ┌──┐              ┌──────────┐
  │  │    │  │              │          │
  └──┘    └──┘     →        │  ┌───┐   │
                          │  │   │   │
  ┌──┐    ┌──┐              │  └───┘   │
  │  │    │  │              │          │
  └──┘    └──┘              └──────────┘
```

### 4.2 线段连接算法

**核心思路**：使用哈希表建立端点到线段的映射，从一个端点出发，不断找到连接的下一条线段。

```cpp
struct Polygon {
    std::vector<Point2D> points;
    bool isOuter;  // 外轮廓 or 内孔
};

// 线段连接成多边形
std::vector<Polygon> connectSegments(std::vector<LineSegment2D>& segments) {
    std::vector<Polygon> polygons;
    
    // 构建端点哈希表（使用坐标量化作为key）
    auto pointKey = [](const Point2D& p) {
        // 量化到0.001mm精度避免浮点误差
        int64_t key = (int64_t)(p.x * 1000) * 100000 + (int64_t)(p.y * 1000);
        return key;
    };
    
    // pointToSegments: 每个端点对应哪些线段
    std::unordered_map<int64_t, std::vector<int>> endpointMap;
    for (int i = 0; i < segments.size(); i++) {
        endpointMap[pointKey(segments[i].p1)].push_back(i);
        endpointMap[pointKey(segments[i].p2)].push_back(i);
    }
    
    std::vector<bool> used(segments.size(), false);
    
    for (int start = 0; start < segments.size(); start++) {
        if (used[start]) continue;
        
        Polygon poly;
        used[start] = true;
        poly.points.push_back(segments[start].p1);
        poly.points.push_back(segments[start].p2);
        
        Point2D current = segments[start].p2;
        
        // 从当前端点继续找下一条线段
        while (true) {
            int64_t key = pointKey(current);
            auto& candidates = endpointMap[key];
            
            int nextSeg = -1;
            for (int idx : candidates) {
                if (used[idx]) continue;
                nextSeg = idx;
                break;
            }
            
            if (nextSeg == -1) break;  // 没有下一条了
            
            used[nextSeg] = true;
            // 确定连接方向
            if (dist(segments[nextSeg].p1, current) < dist(segments[nextSeg].p2, current)) {
                poly.points.push_back(segments[nextSeg].p2);
                current = segments[nextSeg].p2;
            } else {
                poly.points.push_back(segments[nextSeg].p1);
                current = segments[nextSeg].p1;
            }
        }
        
        polygons.push_back(poly);
    }
    
    return polygons;
}
```

### 4.3 轮廓方向判定与嵌套关系

**判定外轮廓 vs 内孔：**

使用**有符号面积法**判定多边形方向：
- 逆时针（正面积）→ 外轮廓
- 顺时针（负面积）→ 内孔

$$A = \frac{1}{2} \sum_{i=0}^{n-1} (x_i \cdot y_{i+1} - x_{i+1} \cdot y_i)$$

```cpp
float signedArea(const Polygon& poly) {
    float area = 0;
    int n = poly.points.size();
    for (int i = 0; i < n; i++) {
        int j = (i + 1) % n;
        area += poly.points[i].x * poly.points[j].y;
        area -= poly.points[j].x * poly.points[i].y;
    }
    return area / 2.0f;
}

bool isOuterRing(const Polygon& poly) {
    return signedArea(poly) > 0;  // 逆时针为外轮廓
}
```

**嵌套关系判定（哪些孔属于哪个外轮廓）：**

使用**点-多边形包含测试**（射线法）：

```cpp
bool pointInPolygon(const Point2D& pt, const Polygon& poly) {
    int n = poly.points.size();
    bool inside = false;
    for (int i = 0, j = n - 1; i < n; j = i++) {
        if (((poly.points[i].y > pt.y) != (poly.points[j].y > pt.y)) &&
            (pt.x < (poly.points[j].x - poly.points[i].x) * (pt.y - poly.points[i].y) 
             / (poly.points[j].y - poly.points[i].y) + poly.points[i].x)) {
            inside = !inside;
        }
    }
    return inside;
}
```

### 4.4 轮廓简化

为减少G-code指令数量，可对轮廓进行**Douglas-Peucker简化**：

```cpp
// Douglas-Peucker算法
std::vector<Point2D> simplifyDP(const std::vector<Point2D>& points, 
                                 float epsilon) {
    if (points.size() < 3) return points;
    
    // 找到距离首尾连线最远的点
    float maxDist = 0;
    int maxIdx = 0;
    for (int i = 1; i < points.size() - 1; i++) {
        float d = pointToLineDistance(points[i], points.front(), points.back());
        if (d > maxDist) {
            maxDist = d;
            maxIdx = i;
        }
    }
    
    if (maxDist > epsilon) {
        // 递归简化
        auto left = simplifyDP({points.begin(), points.begin() + maxIdx + 1}, epsilon);
        auto right = simplifyDP({points.begin() + maxIdx, points.end()}, epsilon);
        
        std::vector<Point2D> result(left.begin(), left.end() - 1);
        result.insert(result.end(), right.begin(), right.end());
        return result;
    } else {
        return {points.front(), points.back()};
    }
}
```

---

## 5. 填充算法（Infill）

### 5.1 填充类型概述

| 填充类型 | 强度 | 速度 | 材料用量 | 适用场景 |
|----------|------|------|----------|----------|
| 直线（Lines） | 低 | 快 | 少 | 快速原型 |
| 网格（Grid） | 中 | 中 | 中 | 通用 |
| 三角形（Triangles） | 中高 | 中 | 中 | 结构件 |
| 六边形（Honeycomb） | 高 | 慢 | 多 | 高强度件 |
| 陀螺仪（Gyroid） | 高 | 中 | 中 | 各向同性强度 |
| 同心（Concentric） | 低 | 快 | 少 | 柔性材料 |
| 立方体（Cubic） | 高 | 慢 | 多 | 高强度 |

### 5.2 直线填充算法（扫描线法）

最基础的填充：在轮廓内部以固定间距生成平行线。

```cpp
struct InfillConfig {
    float density;       // 0.0 ~ 1.0
    float nozzleWidth;   // 喷嘴宽度（mm）
    int angle;           // 填充角度（度）
};

std::vector<LineSegment2D> generateLineInfill(
    const std::vector<Polygon>& polygons,
    const InfillConfig& config)
{
    std::vector<LineSegment2D> infillLines;
    
    // 计算填充间距
    float spacing = config.nozzleWidth / config.density;
    
    // 计算边界框
    float minX = FLT_MAX, maxX = -FLT_MAX;
    float minY = FLT_MAX, maxY = -FLT_MAX;
    for (const auto& poly : polygons) {
        for (const auto& pt : poly.points) {
            minX = std::min(minX, pt.x);
            maxX = std::max(maxX, pt.x);
            minY = std::min(minY, pt.y);
            maxY = std::max(maxY, pt.y);
        }
    }
    
    // 生成扫描线
    float angleRad = config.angle * M_PI / 180.0f;
    float cosA = cos(angleRad), sinA = sin(angleRad);
    
    // 沿垂直于填充方向生成扫描线
    for (float d = 0; d < (maxX - minX + maxY - minY); d += spacing) {
        // 扫描线方程：x*cosA + y*sinA = d
        // 与所有多边形边求交
        std::vector<Point2D> intersections;
        
        for (const auto& poly : polygons) {
            int n = poly.points.size();
            for (int i = 0; i < n; i++) {
                int j = (i + 1) % n;
                
                // 计算边的两个端点到扫描线的有符号距离
                float d1 = poly.points[i].x * cosA + poly.points[i].y * sinA - d;
                float d2 = poly.points[j].x * cosA + poly.points[j].y * sinA - d;
                
                if (d1 * d2 < 0) {  // 异号，有交点
                    float t = d1 / (d1 - d2);
                    Point2D pt;
                    pt.x = poly.points[i].x + t * (poly.points[j].x - poly.points[i].x);
                    pt.y = poly.points[i].y + t * (poly.points[j].y - poly.points[i].y);
                    intersections.push_back(pt);
                }
            }
        }
        
        // 按沿扫描线方向排序交点
        std::sort(intersections.begin(), intersections.end(),
            [cosA, sinA](const Point2D& a, const Point2D& b) {
                float ta = -a.x * sinA + a.y * cosA;  // 沿扫描线方向的参数
                float tb = -b.x * sinA + b.y * cosA;
                return ta < tb;
            });
        
        // 两两配对生成填充线段
        for (int i = 0; i + 1 < intersections.size(); i += 2) {
            infillLines.push_back({intersections[i], intersections[i + 1]});
        }
    }
    
    return infillLines;
}
```

### 5.3 六边形填充

六边形（蜂窝）填充因其各向同性的力学性能而广泛使用。

**生成方法**：预生成六边形网格，然后用多边形裁剪。

```cpp
std::vector<LineSegment2D> generateHoneycombInfill(
    const std::vector<Polygon>& boundaries,
    float cellSize)
{
    std::vector<LineSegment2D> hexagons;
    
    // 六边形参数
    float h = cellSize * sqrt(3.0f) / 2.0f;  // 半高
    float w = cellSize;                        // 宽度
    
    // 计算边界框
    // ... (同上)
    
    // 生成六边形网格
    for (float row = minY - h; row < maxY + h; row += h * 1.5f) {
        int rowIdx = (int)((row - minY) / (h * 1.5f));
        float xOffset = (rowIdx % 2 == 0) ? 0 : w * 0.5f;
        
        for (float col = minX - w; col < maxX + w; col += w) {
            float cx = col + xOffset;
            float cy = row;
            
            // 生成六边形的6条边
            Point2D hex[6];
            for (int i = 0; i < 6; i++) {
                float angle = M_PI / 3.0f * i;
                hex[i].x = cx + cellSize * 0.5f * cos(angle);
                hex[i].y = cy + cellSize * 0.5f * sin(angle);
            }
            
            // 用边界裁剪每条边
            for (int i = 0; i < 6; i++) {
                int j = (i + 1) % 6;
                LineSegment2D seg = {hex[i], hex[j]};
                
                // Cohen-Sutherland或Liang-Barsky裁剪
                for (const auto& poly : boundaries) {
                    LineSegment2D clipped;
                    if (clipSegmentToPolygon(seg, poly, clipped)) {
                        hexagons.push_back(clipped);
                    }
                }
            }
        }
    }
    
    return hexagons;
}
```

### 5.4 陀螺仪填充（Gyroid）

陀螺仪填充是一种**三周期极小曲面**结构，具有优异的各向同性力学性能。

**数学定义：**

$$\sin(x)\cos(y) + \sin(y)\cos(z) + \sin(z)\cos(x) = 0$$

**生成方法**：在3D空间中对该隐式曲面进行等值面提取（Marching Cubes），然后按层切分。

```python
import numpy as np
from skimage import measure

def generate_gyroid_infill(size, resolution, layer_z):
    """生成指定Z层的Gyroid填充"""
    # 生成网格
    x = np.linspace(0, size, resolution)
    y = np.linspace(0, size, resolution)
    X, Y = np.meshgrid(x, y)
    
    # Gyroid隐式函数（固定Z=layer_z）
    Z = layer_z
    field = (np.sin(X) * np.cos(Y) + 
             np.sin(Y) * np.cos(Z) + 
             np.sin(Z) * np.cos(X))
    
    # 提取等值线（2D Marching Squares）
    contours = measure.find_contours(field, 0.0)
    
    return contours
```

### 5.5 填充密度与间距计算

```
填充间距 = 喷嘴宽度 / 填充密度

示例：
  喷嘴宽度 = 0.4mm
  填充密度 = 20% (0.2)
  填充间距 = 0.4 / 0.2 = 2.0mm

  填充密度 = 50% (0.5)
  填充间距 = 0.4 / 0.5 = 0.8mm
```

---

## 6. 外壳/周界生成（Perimeter/Shell）

### 6.1 多边形偏移

外壳生成本质上是**多边形偏移（Polygon Offsetting）**：将外轮廓向内偏移一定距离，生成多层外壳。

```
外轮廓 ──→ 偏移1层 ──→ 偏移2层 ──→ 填充区域
  ┌──────────┐
  │ ┌──────┐ │
  │ │ ┌──┐ │ │
  │ │ │**│ │ │    ** = 填充区域
  │ │ └──┘ │ │
  │ └──────┘ │
  └──────────┘
```

### 6.2 偏移算法

**方法1：顶点法线偏移（简单但不鲁棒）**

```cpp
Polygon offsetPolygon(const Polygon& poly, float distance) {
    Polygon result;
    int n = poly.points.size();
    
    for (int i = 0; i < n; i++) {
        int prev = (i - 1 + n) % n;
        int next = (i + 1) % n;
        
        // 计算入边和出边的方向
        Point2D inDir = normalize({
            poly.points[i].x - poly.points[prev].x,
            poly.points[i].y - poly.points[prev].y
        });
        Point2D outDir = normalize({
            poly.points[next].x - poly.points[i].x,
            poly.points[next].y - poly.points[i].y
        });
        
        // 计算角平分线方向（指向内侧）
        Point2D bisector = {
            inDir.x + outDir.x,
            inDir.y + outDir.y
        };
        bisector = normalize(bisector);
        
        // 旋转90度得到法线方向
        Point2D normal = {-inDir.y, inDir.x};  // 左侧法线
        
        // 确定偏移方向
        float miterLength = distance / sqrt(0.5 * (1 + inDir.x * outDir.x + inDir.y * outDir.y));
        
        Point2D offset = {
            poly.points[i].x + normal.x * miterLength,
            poly.points[i].y + normal.y * miterLength
        };
        
        result.points.push_back(offset);
    }
    
    return result;
}
```

**方法2：Clipper库（工业标准）**

大多数开源切片器使用 [Clipper](http://www.angusj.com/delphi/clipper.php) 库进行鲁棒的多边形偏移和布尔运算。

```cpp
#include "clipper.hpp"

std::vector<Polygon> offsetPolygonClipper(const std::vector<Polygon>& polys, 
                                           float offset)
{
    ClipperLib::ClipperOffset co;
    co.ArcTolerance = 0.01;  // 圆弧精度
    
    // 转换为Clipper格式（整数坐标）
    const float SCALE = 1000.0f;
    ClipperLib::Paths input;
    for (const auto& poly : polys) {
        ClipperLib::Path path;
        for (const auto& pt : poly.points) {
            path << ClipperLib::IntPoint(pt.x * SCALE, pt.y * SCALE);
        }
        input << path;
    }
    
    co.AddPaths(input, ClipperLib::jtMiter, ClipperLib::etClosedPolygon);
    
    ClipperLib::Paths solution;
    co.Execute(solution, -offset * SCALE);  // 负值=向内偏移
    
    // 转换回浮点坐标
    std::vector<Polygon> result;
    for (const auto& path : solution) {
        Polygon poly;
        for (const auto& pt : path) {
            poly.points.push_back({(float)pt.X / SCALE, (float)pt.Y / SCALE});
        }
        result.push_back(poly);
    }
    
    return result;
}
```

### 6.3 外壳生成流程

```cpp
std::vector<std::vector<Polygon>> generateShells(
    const std::vector<Polygon>& outerContours,
    int numShells,
    float nozzleWidth)
{
    std::vector<std::vector<Polygon>> shells;
    
    // 第一层外壳 = 外轮廓本身
    shells.push_back(outerContours);
    
    // 逐层向内偏移
    std::vector<Polygon> current = outerContours;
    for (int i = 1; i < numShells; i++) {
        current = offsetPolygonClipper(current, nozzleWidth);
        if (current.empty()) break;  // 没有空间了
        shells.push_back(current);
    }
    
    return shells;
}
```

---

## 7. 支撑结构生成（Support）

### 7.1 悬垂检测

需要支撑的区域是模型中**向下倾斜超过临界角度**的面。

**临界角度**通常为45°（FDM打印），可配置。

```cpp
struct SupportRegion {
    std::vector<Polygon> areas;  // 需要支撑的区域
    float zStart, zEnd;          // 支撑起始/结束高度
};

// 检测需要支撑的三角形面片
std::vector<SupportRegion> detectOverhang(const Mesh& mesh, 
                                           float maxAngle = 45.0f)
{
    float cosThreshold = cos(maxAngle * M_PI / 180.0f);
    
    // 悬垂三角形：法线Z分量 < -cosThreshold
    std::vector<Triangle> overhangTris;
    for (const auto& tri : mesh.triangles) {
        // 法线已经存储在STL中，或重新计算
        Vertex normal = tri.normal;
        if (normal.z < -cosThreshold) {
            overhangTris.push_back(tri);
        }
    }
    
    // 将悬垂三角形投影到XY平面，合并为区域
    // ... (使用多边形布尔并运算)
    
    return {};
}
```

### 7.2 支撑类型

| 类型 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 树形支撑 | 易拆除，接触少 | 生成慢 | 复杂模型 |
| 栅格支撑 | 生成快，稳定 | 难拆除 | 平面悬垂 |
| 齿状支撑 | 易分离 | 强度低 | 小面积悬垂 |

### 7.3 树形支撑生成

树形支撑从悬空区域向下"生长"，逐渐收窄并偏移到最近的支撑面。

```cpp
struct SupportTree {
    Point2D root;       // 根部位置（支撑面）
    Point2D tip;        // 顶部位置（悬空区域）
    float zStart, zEnd; // 高度范围
    float radius;       // 当前半径
};

std::vector<SupportTree> generateTreeSupport(
    const std::vector<SupportRegion>& regions,
    const Mesh& mesh,
    float layerHeight)
{
    std::vector<SupportTree> trees;
    
    for (const auto& region : regions) {
        // 在悬空区域采样支撑点
        for (const auto& poly : region.areas) {
            // 计算多边形重心作为支撑点
            Point2D centroid = computeCentroid(poly);
            
            // 从悬空高度向下搜索最近的支撑面
            Point2D currentPos = centroid;
            float currentZ = region.zStart;
            
            while (currentZ > mesh.min.z) {
                currentZ -= layerHeight;
                
                // 检查当前位置是否有模型支撑
                if (hasModelSupportAt(currentPos, currentZ, mesh)) {
                    // 找到支撑面，生成树
                    SupportTree tree;
                    tree.root = currentPos;
                    tree.tip = centroid;
                    tree.zStart = currentZ;
                    tree.zEnd = region.zStart;
                    tree.radius = nozzleWidth * 2;
                    trees.push_back(tree);
                    break;
                }
                
                // 向最近支撑方向偏移（简化版）
                currentPos = moveTowardsSupport(currentPos, currentZ, mesh);
            }
        }
    }
    
    return trees;
}
```

---

## 8. 自适应切片（Adaptive Slicing）

### 8.1 动机

固定层高在平坦区域浪费精度，在陡峭区域精度不足。自适应切片根据**局部曲率**动态调整层高。

```
固定层高:                    自适应层高:
├──── 0.2mm ────┤            ├── 0.3mm ──┤  (平坦区域，厚层)
├──── 0.2mm ────┤            ├── 0.3mm ──┤
├──── 0.2mm ────┤            ├── 0.1mm ──┤  (陡峭区域，薄层)
├──── 0.2mm ────┤            ├── 0.1mm ──┤
├──── 0.2mm ────┤            ├── 0.2mm ──┤
```

### 8.2 曲率计算

**方法：比较相邻层的轮廓面积差异**

如果两层轮廓差异大，说明曲率高，应减小层高。

$$\text{curvature}(z) = \frac{|A(z + \Delta z) - A(z)|}{\Delta z \cdot A(z)}$$

其中 $A(z)$ 是Z高度处的轮廓面积。

### 8.3 自适应切片算法

```cpp
struct AdaptiveSliceConfig {
    float minLayerHeight = 0.05f;  // 最小层高
    float maxLayerHeight = 0.3f;   // 最大层高
    float maxError = 0.01f;        // 最大允许误差
};

std::vector<float> computeAdaptiveLayerHeights(
    const Mesh& mesh,
    const AdaptiveSliceConfig& config)
{
    std::vector<float> layers;
    float z = mesh.min.z;
    
    while (z < mesh.max.z) {
        // 从最大层高开始尝试
        float h = config.maxLayerHeight;
        
        // 二分搜索找到满足误差约束的最大层高
        while (h > config.minLayerHeight) {
            float error = computeSliceError(mesh, z, z + h);
            if (error <= config.maxError) break;
            h *= 0.8f;  // 减小层高重试
        }
        
        h = std::max(h, config.minLayerHeight);
        layers.push_back(h);
        z += h;
    }
    
    return layers;
}

// 计算切片误差：比较实际模型与切片近似之间的偏差
float computeSliceError(const Mesh& mesh, float zLow, float zHigh) {
    // 在zLow和zHigh之间采样多个高度
    // 比较中间高度的实际轮廓与线性插值轮廓的差异
    float zMid = (zLow + zHigh) / 2.0f;
    
    auto contourLow = sliceMeshAtZ(mesh, zLow);
    auto contourMid = sliceMeshAtZ(mesh, zMid);
    auto contourHigh = sliceMeshAtZ(mesh, zHigh);
    
    // 计算中间轮廓与上下轮廓插值的差异
    auto interpolated = interpolateContours(contourLow, contourHigh, 0.5f);
    
    return contourDistance(contourMid, interpolated);
}
```

### 8.4 基于STL法线的快速自适应

利用STL面片的法线信息直接估算最优层高：

$$h_{\text{optimal}} = \frac{\Delta_{\text{max}}}{|\tan(\theta)|}$$

其中 $\theta$ 是面片法线与Z轴的夹角，$\Delta_{\text{max}}$ 是最大允许表面台阶误差。

```cpp
float computeOptimalLayerHeight(const Triangle& tri, float maxStaircaseError) {
    // 法线与Z轴的夹角
    float cosTheta = fabs(tri.normal.z);
    float sinTheta = sqrt(1.0f - cosTheta * cosTheta);
    
    if (sinTheta < 1e-6f) return maxLayerHeight;  // 水平面
    
    float tanTheta = sinTheta / cosTheta;
    return maxStaircaseError / tanTheta;
}
```

---

## 9. 工具路径规划与优化（Toolpath）

### 9.1 路径顺序

一层的打印顺序通常为：
1. **外壳/周界**（由内向外或由外向内）
2. **填充**
3. **支撑**

### 9.2 旅行移动优化

打印头在非挤出状态下的移动（travel move）应最小化，这是一个**旅行商问题（TSP）**。

**最近邻启发式（简单快速）：**

```cpp
std::vector<Path> optimizePathOrder(std::vector<Path>& paths, 
                                     Point2D startPos)
{
    std::vector<Path> ordered;
    Point2D current = startPos;
    
    while (!paths.empty()) {
        // 找到最近的路径
        int nearestIdx = 0;
        float minDist = FLT_MAX;
        
        for (int i = 0; i < paths.size(); i++) {
            float d = dist(current, paths[i].startPoint);
            if (d < minDist) {
                minDist = d;
                nearestIdx = i;
            }
        }
        
        // 可能反转路径方向以减少移动
        if (dist(current, paths[nearestIdx].endPoint) < 
            dist(current, paths[nearestIdx].startPoint)) {
            paths[nearestIdx].reverse();
        }
        
        current = paths[nearestIdx].endPoint;
        ordered.push_back(paths[nearestIdx]);
        paths.erase(paths.begin() + nearestIdx);
    }
    
    return ordered;
}
```

### 9.3 接缝优化

每条闭合路径的起点/终点会留下可见的"接缝"。优化接缝位置：

| 策略 | 描述 |
|------|------|
| 最近点 | 选择离上一条路径终点最近的点 |
| 背面 | 统一放在模型背面（不可见侧） |
| 最尖锐角 | 放在最尖锐的角处（接缝不明显） |
| 对齐 | 各层接缝对齐（形成直线） |

```cpp
Point2D findOptimalSeam(const Polygon& poly, Point2D lastPos) {
    // 策略：选择最尖锐的角
    int n = poly.points.size();
    int bestIdx = 0;
    float minAngle = FLT_MAX;
    
    for (int i = 0; i < n; i++) {
        int prev = (i - 1 + n) % n;
        int next = (i + 1) % n;
        
        // 计算内角
        Point2D v1 = {poly.points[i].x - poly.points[prev].x,
                      poly.points[i].y - poly.points[prev].y};
        Point2D v2 = {poly.points[next].x - poly.points[i].x,
                      poly.points[next].y - poly.points[i].y};
        
        float angle = atan2(v1.x * v2.y - v1.y * v2.x, 
                            v1.x * v2.x + v1.y * v2.y);
        float interiorAngle = M_PI - fabs(angle);
        
        if (interiorAngle < minAngle) {
            minAngle = interiorAngle;
            bestIdx = i;
        }
    }
    
    return poly.points[bestIdx];
}
```

### 9.4 挤出量计算

**挤出模型**：挤出量与移动距离成正比，考虑层高、线宽和材料。

$$E = \frac{d \cdot h \cdot w}{\pi \cdot r^2} \cdot k$$

其中：
- $d$ = 移动距离
- $h$ = 层高
- $w$ = 挤出宽度
- $r$ = 耗材半径（通常1.75mm）
- $k$ = 挤出补偿系数（通常~1.0）

```cpp
float computeExtrusion(float distance, float layerHeight, 
                        float extrusionWidth, float filamentRadius)
{
    float filamentArea = M_PI * filamentRadius * filamentRadius;
    float extrudedVolume = distance * layerHeight * extrusionWidth;
    return extrudedVolume / filamentArea;
}
```

---

## 10. G-code生成

### 10.1 G-code基础指令

| 指令 | 含义 | 示例 |
|------|------|------|
| G0 | 快速移动（不挤出） | `G0 X10 Y20 Z0.2` |
| G1 | 线性移动（挤出） | `G1 X10 Y20 E1.5` |
| G28 | 回原点 | `G28` |
| G92 | 设置当前位置 | `G92 E0` |
| M104 | 设置喷嘴温度 | `M104 S200` |
| M140 | 设置热床温度 | `M140 S60` |
| M106 | 风扇速度 | `M106 S128` |
| M109 | 等待喷嘴温度 | `M109 S200` |

### 10.2 G-code生成器

```cpp
class GCodeGenerator {
    std::ofstream file;
    float currentX = 0, currentY = 0, currentZ = 0;
    float currentE = 0;
    float feedRate = 3000;  // mm/min
    bool relativeE = false;
    
public:
    void open(const std::string& filepath) {
        file.open(filepath);
        writeHeader();
    }
    
    void writeHeader() {
        file << "; Generated by Custom Slicer\n";
        file << "G28 ; Home\n";
        file << "G92 E0 ; Reset extruder\n";
        file << "M104 S200 ; Set hotend temp\n";
        file << "M140 S60 ; Set bed temp\n";
        file << "M109 S200 ; Wait for hotend\n";
        file << "M190 S60 ; Wait for bed\n";
        file << "G21 ; mm units\n";
        file << "G90 ; absolute positioning\n";
        file << "M82 ; absolute extrusion\n";
        file << "\n";
    }
    
    void writeLayer(float z) {
        currentZ = z;
        file << "; LAYER:" << z << "\n";
        file << "G0 Z" << z << " F" << feedRate << "\n";
    }
    
    void writeTravel(float x, float y, float speed = 6000) {
        file << "G0 X" << x << " Y" << y << " F" << speed << "\n";
        currentX = x;
        currentY = y;
    }
    
    void writeExtrude(float x, float y, float e, float speed = 1800) {
        if (relativeE) {
            file << "G1 X" << x << " Y" << y 
                 << " E" << e << " F" << speed << "\n";
            currentE += e;
        } else {
            currentE += e;
            file << "G1 X" << x << " Y" << y 
                 << " E" << currentE << " F" << speed << "\n";
        }
        currentX = x;
        currentY = y;
    }
    
    void writeArc(float x, float y, float cx, float cy, 
                  bool clockwise, float e, float speed = 1800) {
        // G2 = 顺时针圆弧, G3 = 逆时针圆弧
        file << (clockwise ? "G2" : "G3") 
             << " X" << x << " Y" << y
             << " I" << (cx - currentX) 
             << " J" << (cy - currentY)
             << " E" << (relativeE ? e : currentE + e)
             << " F" << speed << "\n";
        currentX = x;
        currentY = y;
        currentE += e;
    }
    
    void writeFooter() {
        file << "\n; End G-code\n";
        file << "M104 S0 ; Turn off hotend\n";
        file << "M140 S0 ; Turn off bed\n";
        file << "M107 ; Fan off\n";
        file << "G0 X0 Y200 ; Move bed forward\n";
        file << "M84 ; Disable motors\n";
    }
};
```

### 10.3 完整层输出

```cpp
void writeLayerGCode(GCodeGenerator& gcode, 
                     const LayerData& layer,
                     float layerHeight)
{
    // 移动到层高度
    gcode.writeLayer(layer.z);
    
    // 1. 打印外壳
    for (const auto& shell : layer.shells) {
        for (const auto& poly : shell) {
            if (poly.points.empty()) continue;
            
            // 移到起点（不挤出）
            gcode.writeTravel(poly.points[0].x, poly.points[0].y);
            
            // 挤出绘制轮廓
            for (int i = 1; i < poly.points.size(); i++) {
                float dist = distance(poly.points[i-1], poly.points[i]);
                float e = computeExtrusion(dist, layerHeight, nozzleWidth, filamentRadius);
                gcode.writeExtrude(poly.points[i].x, poly.points[i].y, e);
            }
            
            // 闭合
            float dist = distance(poly.points.back(), poly.points[0]);
            float e = computeExtrusion(dist, layerHeight, nozzleWidth, filamentRadius);
            gcode.writeExtrude(poly.points[0].x, poly.points[0].y, e);
        }
    }
    
    // 2. 打印填充
    for (const auto& seg : layer.infill) {
        gcode.writeTravel(seg.p1.x, seg.p1.y);
        float dist = distance(seg.p1, seg.p2);
        float e = computeExtrusion(dist, layerHeight, nozzleWidth, filamentRadius);
        gcode.writeExtrude(seg.p2.x, seg.p2.y, e);
    }
    
    // 3. 打印支撑
    // ...
}
```

---

## 11. 高级切片技术

### 11.1 变宽度挤出（Variable Width Extrusion）

在转弯处自动调整挤出宽度，避免过挤出。

### 11.2 接缝隐藏（Seam Hiding）

通过分析模型几何，将接缝放在不可见的位置。

### 11.3 铁丝打印（Ironing）

在顶层表面用热喷嘴再次熨平，获得光滑表面。

```
G-code示例:
G1 X10 Y10 E5.0 F1200   ; 正常打印
G1 X10 Y10 F1200         ; 铁丝模式（不挤出，仅熨平）
```

### 11.4 多材料切片

为多挤出头打印机生成不同材料的路径：

```cpp
struct Toolpath {
    int extruder;  // 挤出头编号
    std::vector<Point3D> points;
    std::vector<float> extrusion;
};

// 按挤出头分组路径
std::vector<std::vector<Toolpath>> groupByExtruder(
    const std::vector<Toolpath>& paths)
{
    // 先按挤出头排序，减少切换次数
    // ...
}
```

### 11.5 连续打印（Sequential Printing）

多个零件依次打印，避免在零件间长距离移动。

---

# 第二部分：颜色管理算法

## 12. 颜色管理基础

### 12.1 为什么需要颜色管理

3D打印中的颜色管理解决以下问题：

1. **屏幕到打印的颜色一致性**：显示器RGB → 打印材料颜色
2. **多材料颜色混合**：CMYK/W基色 → 目标颜色
3. **体素级颜色赋值**：3D模型每个体素的颜色映射
4. **颜色空间转换**：不同设备间的颜色传递

### 12.2 颜色管理流水线

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 源颜色空间 │ ──→ │ PCS中间空间 │ ──→ │ 目标颜色空间 │ ──→ │ 打印输出  │
│ (sRGB等)  │     │ (CIE XYZ/ │     │ (CMYK/    │     │ (多喷头/  │
│           │     │  CIE Lab) │     │  设备RGB) │     │  体素)    │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     ↑                ↑                  ↑
  源ICC配置       标准中间空间        目标ICC配置
```

### 12.3 核心概念

| 概念 | 描述 |
|------|------|
| 色域（Gamut） | 设备能再现的颜色范围 |
| ICC配置文件 | 描述设备颜色特性的文件 |
| PCS（Profile Connection Space） | 颜色转换的中间空间（CIE XYZ或Lab） |
| 渲染意图（Rendering Intent） | 色域外颜色的处理策略 |
| 色差（Delta E） | 两个颜色之间的感知差异度量 |

---

## 13. 颜色空间与转换

### 13.1 常用颜色空间

#### RGB（加色模型）
- 用于显示器、屏幕
- sRGB：标准RGB色域
- Adobe RGB：更宽色域
- ProPhoto RGB：最宽色域

#### CMYK（减色模型）
- 用于印刷和部分3D打印
- C(青) M(品红) Y(黄) K(黑)
- K通道用于提高对比度和节省材料

#### CIE XYZ（设备无关）
- 所有人眼可见颜色
- X、Y、Z三刺激值
- Y ≈ 亮度

#### CIE Lab（感知均匀）
- L：亮度（0-100）
- a：红绿轴（-128到+128）
- b：黄蓝轴（-128到+128）
- **感知均匀**：相同数值差异 ≈ 相同视觉差异

#### HSV/HSB（直觉模型）
- H：色相（0-360°）
- S：饱和度（0-100%）
- V/B：明度（0-100%）

### 13.2 颜色空间转换公式

#### sRGB → CIE XYZ

首先对sRGB值进行gamma逆变换（线性化）：

$$C_{\text{linear}} = \begin{cases} \frac{C_{sRGB}}{12.92}, & C_{sRGB} \leq 0.04045 \\ \left(\frac{C_{sRGB} + 0.055}{1.055}\right)^{2.4}, & C_{sRGB} > 0.04045 \end{cases}$$

然后矩阵变换（D65 illuminant）：

$$\begin{bmatrix} X \\ Y \\ Z \end{bmatrix} = \begin{bmatrix} 0.4124 & 0.3576 & 0.1805 \\ 0.2126 & 0.7152 & 0.0722 \\ 0.0193 & 0.1192 & 0.9505 \end{bmatrix} \begin{bmatrix} R_{\text{linear}} \\ G_{\text{linear}} \\ B_{\text{linear}} \end{bmatrix}$$

```cpp
struct ColorRGB { float r, g, b; };        // 0.0-1.0
struct ColorXYZ { float X, Y, Z; };
struct ColorLab { float L, a, b; };

// sRGB线性化
float srgbToLinear(float c) {
    if (c <= 0.04045f) return c / 12.92f;
    return pow((c + 0.055f) / 1.055f, 2.4f);
}

// sRGB → XYZ
ColorXYZ srgbToXYZ(const ColorRGB& rgb) {
    float r = srgbToLinear(rgb.r);
    float g = srgbToLinear(rgb.g);
    float b = srgbToLinear(rgb.b);
    
    ColorXYZ xyz;
    xyz.X = 0.4124f * r + 0.3576f * g + 0.1805f * b;
    xyz.Y = 0.2126f * r + 0.7152f * g + 0.0722f * b;
    xyz.Z = 0.0193f * r + 0.1192f * g + 0.9505f * b;
    return xyz;
}
```

#### CIE XYZ → CIE Lab

$$L = 116 \cdot f\left(\frac{Y}{Y_n}\right) - 16$$
$$a = 500 \cdot \left[f\left(\frac{X}{X_n}\right) - f\left(\frac{Y}{Y_n}\right)\right]$$
$$b = 200 \cdot \left[f\left(\frac{Y}{Y_n}\right) - f\left(\frac{Z}{Z_n}\right)\right]$$

其中 $f(t)$ 为：

$$f(t) = \begin{cases} t^{1/3}, & t > \delta^3 \\ \frac{t}{3\delta^2} + \frac{4}{29}, & t \leq \delta^3 \end{cases}$$

$\delta = 6/29$，$(X_n, Y_n, Z_n)$ 是参考白点的XYZ值（D65: $X_n=0.9505, Y_n=1.0, Z_n=1.089$）。

```cpp
ColorXYZ xyzToLab(const ColorXYZ& xyz) {
    // D65参考白点
    const float Xn = 0.9505f, Yn = 1.0f, Zn = 1.089f;
    const float delta = 6.0f / 29.0f;
    
    auto f = [delta](float t) {
        if (t > delta * delta * delta)
            return cbrt(t);
        return t / (3 * delta * delta) + 4.0f / 29.0f;
    };
    
    float fx = f(xyz.X / Xn);
    float fy = f(xyz.Y / Yn);
    float fz = f(xyz.Z / Zn);
    
    ColorLab lab;
    lab.L = 116.0f * fy - 16.0f;
    lab.a = 500.0f * (fx - fy);
    lab.b = 200.0f * (fy - fz);
    return lab;
}
```

#### RGB → CMYK（简化版）

```cpp
struct ColorCMYK { float c, m, y, k; };  // 0.0-1.0

ColorCMYK rgbToCMYK(const ColorRGB& rgb) {
    float k = 1.0f - std::max({rgb.r, rgb.g, rgb.b});
    
    if (k >= 1.0f) {  // 纯黑
        return {0, 0, 0, 1};
    }
    
    float c = (1.0f - rgb.r - k) / (1.0f - k);
    float m = (1.0f - rgb.g - k) / (1.0f - k);
    float y = (1.0f - rgb.b - k) / (1.0f - k);
    
    return {c, m, y, k};
}

ColorRGB cmykToRGB(const ColorCMYK& cmyk) {
    ColorRGB rgb;
    rgb.r = (1.0f - cmyk.c) * (1.0f - cmyk.k);
    rgb.g = (1.0f - cmyk.m) * (1.0f - cmyk.k);
    rgb.b = (1.0f - cmyk.y) * (1.0f - cmyk.k);
    return rgb;
}
```

### 13.3 色差计算（Delta E）

色差用于衡量两个颜色的感知差异，在颜色校准中至关重要。

#### ΔE*ab（CIE1976）

$$\Delta E_{ab}^* = \sqrt{(L_1 - L_2)^2 + (a_1 - a_2)^2 + (b_1 - b_2)^2}$$

#### ΔE*94（CIE1994，更准确）

$$\Delta E_{94}^* = \sqrt{\left(\frac{\Delta L}{k_L S_L}\right)^2 + \left(\frac{\Delta C}{k_C S_C}\right)^2 + \left(\frac{\Delta H}{k_H S_H}\right)^2}$$

其中：
- $S_L = 1$, $S_C = 1 + K_1 C_1$, $S_H = 1 + K_2 C_1$
- 对于纺织品：$K_1 = 0.048, K_2 = 0.014$
- 对于图形艺术：$K_1 = 0.045, K_2 = 0.015$

#### ΔE*00（CIE2000，最准确）

$$\Delta E_{00} = \sqrt{\left(\frac{\Delta L'}{k_L S_L}\right)^2 + \left(\frac{\Delta C'}{k_C S_C}\right)^2 + \left(\frac{\Delta H'}{k_H S_H}\right)^2 + R_T \cdot \frac{\Delta C'}{k_C S_C} \cdot \frac{\Delta H'}{k_H S_H}}$$

```cpp
// CIE2000色差（简化实现）
float deltaE00(const ColorLab& lab1, const ColorLab& lab2) {
    const float kL = 1.0f, kC = 1.0f, kH = 1.0f;
    
    float avgL = (lab1.L + lab2.L) / 2.0f;
    float C1 = sqrt(lab1.a * lab1.a + lab1.b * lab1.b);
    float C2 = sqrt(lab2.a * lab2.a + lab2.b * lab2.b);
    float avgC = (C1 + C2) / 2.0f;
    
    float avgC7 = pow(avgC, 7);
    float G = 0.5f * (1.0f - sqrt(avgC7 / (avgC7 + pow(25.0f, 7))));
    
    float a1p = (1.0f + G) * lab1.a;
    float a2p = (1.0f + G) * lab2.a;
    
    float C1p = sqrt(a1p * a1p + lab1.b * lab1.b);
    float C2p = sqrt(a2p * a2p + lab2.b * lab2.b);
    
    float h1p = atan2(lab1.b, a1p);
    float h2p = atan2(lab2.b, a2p);
    if (h1p < 0) h1p += 2 * M_PI;
    if (h2p < 0) h2p += 2 * M_PI;
    
    float dLp = lab2.L - lab1.L;
    float dCp = C2p - C1p;
    
    float dhp = h2p - h1p;
    if (fabs(dhp) > M_PI) {
        if (h2p <= h1p) dhp += 2 * M_PI;
        else dhp -= 2 * M_PI;
    }
    
    float dHp = 2.0f * sqrt(C1p * C2p) * sin(dhp / 2.0f);
    
    float avgLp = (lab1.L + lab2.L) / 2.0f;
    float avgCp = (C1p + C2p) / 2.0f;
    
    float avghp = (h1p + h2p) / 2.0f;
    if (fabs(h1p - h2p) > M_PI) {
        if (h1p + h2p < 2 * M_PI) avghp += M_PI;
        else avghp -= M_PI;
    }
    
    float T = 1.0f - 0.17f * cos(avghp - M_PI / 6.0f)
              + 0.24f * cos(2.0f * avghp)
              + 0.32f * cos(3.0f * avghp + M_PI / 30.0f)
              - 0.20f * cos(4.0f * avghp - 63.0f * M_PI / 180.0f);
    
    float dTheta = 30.0f * M_PI / 180.0f * 
                   exp(-pow((avghp * 180.0f / M_PI - 275.0f) / 25.0f, 2));
    
    float avgCp7 = pow(avgCp, 7);
    float RC = 2.0f * sqrt(avgCp7 / (avgCp7 + pow(25.0f, 7)));
    float SL = 1.0f + 0.015f * pow(avgLp - 50.0f, 2) / 
               sqrt(20.0f + pow(avgLp - 50.0f, 2));
    float SC = 1.0f + 0.045f * avgCp;
    float SH = 1.0f + 0.015f * avgCp * T;
    float RT = -sin(2.0f * dTheta) * RC;
    
    float dE = sqrt(
        pow(dLp / (kL * SL), 2) +
        pow(dCp / (kC * SC), 2) +
        pow(dHp / (kH * SH), 2) +
        RT * (dCp / (kC * SC)) * (dHp / (kH * SH))
    );
    
    return dE;
}
```

**色差感知参考：**

| ΔE 值 | 感知差异 |
|--------|----------|
| 0-1 | 几乎无法察觉 |
| 1-2 | 仔细观察可察觉 |
| 2-10 | 一眼可察觉 |
| 10-50 | 明显不同 |
| >50 | 完全不同的颜色 |

---

## 14. ICC色彩配置文件

### 14.1 ICC配置文件结构

ICC配置文件描述设备的颜色特性，包含：

| 标签 | 描述 |
|------|------|
| profileDescription | 配置文件描述 |
| copyright | 版权信息 |
| mediaWhitePoint | 介质白点 |
| redColorant / greenColorant / blueColorant | RGB原色XYZ值 |
| redTRC / greenTRC / blueTRC | 色调响应曲线 |
| AToB0 / BToA0 | 颜色转换查找表（LUT） |
| gamut | 色域边界 |

### 14.2 渲染意图（Rendering Intent）

当源颜色超出目标设备色域时，需要选择处理策略：

| 渲染意图 | 描述 | 适用场景 |
|----------|------|----------|
| 感知（Perceptual） | 压缩整体色域，保持相对关系 | 照片、自然图像 |
| 饱和度（Saturation） | 保持饱和度，牺牲色相准确度 | 图表、图形 |
| 相对色度（Relative Colorimetric） | 超出色域的颜色映射到最近色域边界 | Logo、精确颜色 |
| 绝对色度（Absolute Colorimetric） | 同上但保持白点差异 | 打样、校对 |

```cpp
enum class RenderingIntent {
    Perceptual,
    Saturation,
    RelativeColorimetric,
    AbsoluteColorimetric
};

// 颜色转换：源ICC → PCS → 目标ICC
ColorRGB convertColor(const ColorRGB& srcColor,
                       const ICCProfile& srcProfile,
                       const ICCProfile& dstProfile,
                       RenderingIntent intent)
{
    // 1. 源颜色 → PCS (通过源ICC的AtoB表)
    ColorXYZ pcs = srcProfile.toPCS(srcColor, intent);
    
    // 2. PCS → 目标颜色 (通过目标ICC的BtoA表)
    ColorRGB dstColor = dstProfile.fromPCS(pcs, intent);
    
    return dstColor;
}
```

### 14.3 在3D打印中使用ICC

```
设计软件(sRGB) → ICC转换 → 3D打印材料颜色空间
     ↓                           ↓
  源ICC: sRGB              目标ICC: 打印机+材料组合
```

**关键挑战**：3D打印材料的颜色特性不同于2D打印，需要为每种材料组合创建专用ICC配置文件。

---

## 15. 3D打印中的多色/多材料管理

### 15.1 多色打印方法

| 方法 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| 多挤出头 | 每种颜色一个挤出头 | 简单直接 | 喷头间需要校准 |
| 换料（Filament Switch） | 中途切换耗材 | 单喷头即可 | 切换时浪费材料 |
| CMYK混合喷头 | 基色混合出目标色 | 理论上无限色 | 混合精度有限 |
| 全彩粘结（CJP） | 彩色粘结剂逐层粘结粉末 | 全彩 | 脆弱、需后处理 |
| PolyJet/MJP | 多材料喷墨 | 高精度全彩 | 设备昂贵 |

### 15.2 多挤出头颜色分配

```cpp
struct ExtruderConfig {
    int id;
    ColorRGB color;          // 该挤出头的材料颜色
    float temperature;       // 挤出温度
    float flowMultiplier;    // 流量补偿
};

// 为每层路径分配挤出头
std::vector<LayerPlan> assignExtruders(
    const std::vector<LayerData>& layers,
    const std::vector<ExtruderConfig>& extruders)
{
    std::vector<LayerPlan> plans;
    
    for (const auto& layer : layers) {
        LayerPlan plan;
        
        for (const auto& region : layer.regions) {
            // 找到颜色最接近的挤出头
            int bestExtruder = 0;
            float minDeltaE = FLT_MAX;
            
            ColorLab targetLab = srgbToLab(region.color);
            
            for (const auto& ext : extruders) {
                ColorLab extLab = srgbToLab(ext.color);
                float dE = deltaE00(targetLab, extLab);
                if (dE < minDeltaE) {
                    minDeltaE = dE;
                    bestExtruder = ext.id;
                }
            }
            
            plan.regions.push_back({region, bestExtruder});
        }
        
        // 优化挤出头切换顺序
        plan.regions = optimizeExtruderOrder(plan.regions);
        
        plans.push_back(plan);
    }
    
    return plans;
}
```

### 15.3 CMYK颜色混合

对于支持CMYK基色混合的打印机，需要将目标RGB颜色转换为CMYK材料比例。

```cpp
struct MaterialMix {
    float c, m, y, k, w;  // 各基材比例（总和=1.0）
};

// RGB → 材料混合比例
MaterialMix rgbToMaterialMix(const ColorRGB& target, 
                              const std::vector<ColorRGB>& baseColors)
{
    // 使用Neugebauer方程或查找表
    // 简化版：最小二乘法拟合
    
    int n = baseColors.size();  // 基色数量
    Eigen::VectorXf targetXYZ(3);
    
    ColorXYZ xyz = srgbToXYZ(target);
    targetXYZ << xyz.X, xyz.Y, xyz.Z;
    
    // 构建基色XYZ矩阵
    Eigen::MatrixXf baseMatrix(3, n);
    for (int i = 0; i < n; i++) {
        ColorXYZ baseXYZ = srgbToXYZ(baseColors[i]);
        baseMatrix(0, i) = baseXYZ.X;
        baseMatrix(1, i) = baseXYZ.Y;
        baseMatrix(2, i) = baseXYZ.Z;
    }
    
    // 最小二乘法求解混合比例
    Eigen::VectorXf ratios = baseMatrix.colPivHouseholderQr().solve(targetXYZ);
    
    // 确保比例非负且归一化
    ratios = ratios.cwiseMax(0);
    ratios /= ratios.sum();
    
    MaterialMix mix;
    // ... 映射到mix结构
    return mix;
}
```

### 15.4 Neugebauer模型

用于预测油墨/材料混合后的颜色：

$$XYZ_{\text{mix}} = \sum_{i} a_i \cdot XYZ_i$$

其中 $a_i$ 是第 $i$ 种组合的面积覆盖率，$XYZ_i$ 是该组合的XYZ值。

对于CMYK四色，有 $2^4 = 16$ 种组合（纸张白、C、M、Y、CM、CY、MY、CMY、K、CK、MK、YK、CMK、CYK、MYK、CMYK）。

Demichen方程扩展了Neugebauer模型，考虑了半色调网点扩散：

$$a_i = \prod_{j \in i} c_j \cdot \prod_{j \notin i} (1 - c_j)$$

其中 $c_j$ 是第 $j$ 种基色的名义覆盖率。

---

## 16. 颜色映射与插值算法

### 16.1 体素颜色映射

全彩3D打印中，需要为3D模型的每个体素分配颜色。

```cpp
struct Voxel {
    Point3D position;
    ColorRGB color;
    float density;  // 材料密度
};

// 从纹理化3D模型提取体素颜色
std::vector<Voxel> extractVoxelColors(const Mesh& mesh,
                                       const Texture3D& texture,
                                       float voxelSize)
{
    std::vector<Voxel> voxels;
    
    // 1. 体素化网格
    auto grid = voxelize(mesh, voxelSize);
    
    // 2. 为每个内部体素采样纹理颜色
    for (auto& cell : grid) {
        if (cell.isInside) {
            Voxel voxel;
            voxel.position = cell.center;
            voxel.color = texture.sample(cell.center);
            voxel.density = 1.0f;
            voxels.push_back(voxel);
        }
    }
    
    return voxels;
}
```

### 16.2 颜色插值

在体素之间或层间进行颜色过渡时，需要选择合适的插值空间。

| 插值空间 | 效果 | 推荐场景 |
|----------|------|----------|
| RGB线性 | 可能出现灰色过渡 | 快速近似 |
| HSV | 色相过渡自然 | 彩虹渐变 |
| Lab | 感知均匀过渡 | 精确颜色匹配 |
| XYZ | 物理意义明确 | ICC流程 |

```cpp
// Lab空间插值（感知最优）
ColorRGB interpolateColorLab(const ColorRGB& c1, const ColorRGB& c2, float t) {
    ColorLab lab1 = srgbToLab(c1);
    ColorLab lab2 = srgbToLab(c2);
    
    ColorLab lab;
    lab.L = lab1.L + t * (lab2.L - lab1.L);
    lab.a = lab1.a + t * (lab2.a - lab1.a);
    lab.b = lab1.b + t * (lab2.b - lab1.b);
    
    return labToSrgb(lab);
}

// HSV空间插值（色相最短路径）
ColorRGB interpolateColorHSV(const ColorRGB& c1, const ColorRGB& c2, float t) {
    auto hsv1 = rgbToHSV(c1);
    auto hsv2 = rgbToHSV(c2);
    
    // 色相取最短路径
    float dh = hsv2.h - hsv1.h;
    if (dh > 180) dh -= 360;
    if (dh < -180) dh += 360;
    
    HSV hsv;
    hsv.h = fmod(hsv1.h + t * dh + 360, 360);
    hsv.s = hsv1.s + t * (hsv2.s - hsv1.s);
    hsv.v = hsv1.v + t * (hsv2.v - hsv1.v);
    
    return hsvToRGB(hsv);
}
```

### 16.3 颜色梯度映射

将标量场（如温度、应力）映射为颜色：

```cpp
// 颜色映射表（LUT）
class ColorLUT {
    std::vector<std::pair<float, ColorRGB>> stops;
    
public:
    void addStop(float value, const ColorRGB& color) {
        stops.push_back({value, color});
        std::sort(stops.begin(), stops.end());
    }
    
    ColorRGB sample(float value) const {
        if (stops.empty()) return {0, 0, 0};
        if (value <= stops.front().first) return stops.front().second;
        if (value >= stops.back().first) return stops.back().second;
        
        for (int i = 0; i < stops.size() - 1; i++) {
            if (value >= stops[i].first && value <= stops[i + 1].first) {
                float t = (value - stops[i].first) / 
                          (stops[i + 1].first - stops[i].first);
                return interpolateColorLab(stops[i].second, 
                                           stops[i + 1].second, t);
            }
        }
        return stops.back().second;
    }
};

// 常用颜色映射
ColorLUT createJetLUT() {
    ColorLUT lut;
    lut.addStop(0.0f, {0.0f, 0.0f, 0.5f});   // 深蓝
    lut.addStop(0.125f, {0.0f, 0.0f, 1.0f});  // 蓝
    lut.addStop(0.375f, {0.0f, 1.0f, 1.0f});  // 青
    lut.addStop(0.625f, {1.0f, 1.0f, 0.0f});  // 黄
    lut.addStop(0.875f, {1.0f, 0.0f, 0.0f});  // 红
    lut.addStop(1.0f, {0.5f, 0.0f, 0.0f});    // 深红
    return lut;
}
```

---

## 17. 抖动算法（Dithering）

### 17.1 为什么需要抖动

当目标颜色无法用有限基色精确表达时，通过**空间混色**（不同颜色像素/体素的排列）在视觉上近似目标颜色。

### 17.2 误差扩散抖动（Error Diffusion）

最常用的抖动方法，将量化误差传播到相邻像素。

**Floyd-Steinberg算法：**

```
当前像素 → 量化 → 计算误差 → 分配给邻居

        *    →   7/16
  3/16  5/16  1/16

(* = 当前像素)
```

```cpp
void floydSteinbergDither(std::vector<std::vector<ColorRGB>>& image,
                           const std::vector<ColorRGB>& palette)
{
    int height = image.size();
    int width = image[0].size();
    
    for (int y = 0; y < height; y++) {
        bool leftToRight = (y % 2 == 0);  // 蛇形扫描
        
        for (int xi = 0; xi < width; xi++) {
            int x = leftToRight ? xi : (width - 1 - xi);
            
            ColorRGB oldColor = image[y][x];
            
            // 找到最接近的调色板颜色
            ColorRGB newColor = findClosestColor(oldColor, palette);
            image[y][x] = newColor;
            
            // 计算量化误差
            ColorRGB error = {
                oldColor.r - newColor.r,
                oldColor.g - newColor.g,
                oldColor.b - newColor.b
            };
            
            // 分配误差给邻居
            int dx = leftToRight ? 1 : -1;
            
            if (x + dx >= 0 && x + dx < width)
                addError(image[y][x + dx], error, 7.0f / 16.0f);
            
            if (y + 1 < height) {
                if (x - dx >= 0 && x - dx < width)
                    addError(image[y + 1][x - dx], error, 3.0f / 16.0f);
                
                addError(image[y + 1][x], error, 5.0f / 16.0f);
                
                if (x + dx >= 0 && x + dx < width)
                    addError(image[y + 1][x + dx], error, 1.0f / 16.0f);
            }
        }
    }
}

ColorRGB findClosestColor(const ColorRGB& target, 
                           const std::vector<ColorRGB>& palette) {
    ColorLab targetLab = srgbToLab(target);
    
    ColorRGB best = palette[0];
    float minDE = FLT_MAX;
    
    for (const auto& color : palette) {
        ColorLab colorLab = srgbToLab(color);
        float dE = deltaE00(targetLab, colorLab);
        if (dE < minDE) {
            minDE = dE;
            best = color;
        }
    }
    
    return best;
}

void addError(ColorRGB& color, const ColorRGB& error, float weight) {
    color.r = clamp(color.r + error.r * weight, 0.0f, 1.0f);
    color.g = clamp(color.g + error.g * weight, 0.0f, 1.0f);
    color.b = clamp(color.b + error.b * weight, 0.0f, 1.0f);
}
```

**其他误差扩散算法：**

| 算法 | 误差分布模式 | 特点 |
|------|-------------|------|
| Floyd-Steinberg | 7/16, 3/16, 5/16, 1/16 | 经典，平衡 |
| Jarvis-Judice-Ninke | 12点分布 | 更平滑，但模糊 |
| Stucki | Jarvis的改进 | 更清晰 |
| Atkinson | 仅扩散到8个邻居中的4个 | 保留细节，有颗粒感 |
| Burkes | Floyd-Steinberg的简化 | 计算快 |

### 17.3 有序抖动（Ordered Dithering）

使用预定义的阈值矩阵，无需误差传播，适合并行处理。

```cpp
// 4x4 Bayer矩阵
const int bayer4x4[4][4] = {
    { 0,  8,  2, 10},
    {12,  4, 14,  6},
    { 3, 11,  1,  9},
    {15,  7, 13,  5}
};

void orderedDither(std::vector<std::vector<ColorRGB>>& image,
                   const std::vector<ColorRGB>& palette) {
    int height = image.size();
    int width = image[0].size();
    
    for (int y = 0; y < height; y++) {
        for (int x = 0; x < width; x++) {
            // 添加阈值偏移
            int threshold = bayer4x4[y % 4][x % 4] * 16 - 128;  // 0-255范围
            
            ColorRGB dithered = {
                clamp(image[y][x].r * 255 + threshold, 0, 255) / 255.0f,
                clamp(image[y][x].g * 255 + threshold, 0, 255) / 255.0f,
                clamp(image[y][x].b * 255 + threshold, 0, 255) / 255.0f
            };
            
            image[y][x] = findClosestColor(dithered, palette);
        }
    }
}
```

### 17.4 3D打印中的体素抖动

在体素级全彩3D打印中，抖动在3D空间中进行：

```cpp
// 3D Floyd-Steinberg抖动
void dither3D(std::vector<std::vector<std::vector<ColorRGB>>>& volume,
              const std::vector<ColorRGB>& palette)
{
    int depth = volume.size();
    int height = volume[0].size();
    int width = volume[0][0].size();
    
    for (int z = 0; z < depth; z++) {
        for (int y = 0; y < height; y++) {
            for (int x = 0; x < width; x++) {
                ColorRGB oldColor = volume[z][y][x];
                ColorRGB newColor = findClosestColor(oldColor, palette);
                volume[z][y][x] = newColor;
                
                ColorRGB error = {
                    oldColor.r - newColor.r,
                    oldColor.g - newColor.g,
                    oldColor.b - newColor.b
                };
                
                // 3D误差扩散（6个邻居）
                // 右、下、后 三个方向
                if (x + 1 < width)
                    addError(volume[z][y][x + 1], error, 7.0f / 26.0f);
                if (y + 1 < height)
                    addError(volume[z][y + 1][x], error, 5.0f / 26.0f);
                if (z + 1 < depth)
                    addError(volume[z + 1][y][x], error, 3.0f / 26.0f);
                
                // 对角邻居
                if (x + 1 < width && y + 1 < height)
                    addError(volume[z][y + 1][x + 1], error, 3.0f / 26.0f);
                if (x + 1 < width && z + 1 < depth)
                    addError(volume[z + 1][y][x + 1], error, 2.0f / 26.0f);
                if (y + 1 < height && z + 1 < depth)
                    addError(volume[z + 1][y + 1][x], error, 2.0f / 26.0f);
                
                // ... 更多邻居
            }
        }
    }
}
```

---

## 18. 颜色校准流程

### 18.1 校准目标

建立**输入颜色**（设计文件中的RGB）与**实际打印颜色**之间的映射关系，使打印结果与设计一致。

### 18.2 校准步骤

```
1. 打印颜色靶标（Color Target）
   → 包含已知RGB值的色块阵列

2. 测量打印结果
   → 使用分光光度计/色差仪测量每个色块的Lab值

3. 建立映射模型
   → 拟合 RGB输入 → Lab实际 的变换关系

4. 生成ICC配置文件或LUT
   → 用于后续打印的颜色补偿

5. 验证
   → 打印验证靶标，检查色差
```

### 18.3 颜色靶标设计

```cpp
struct ColorTarget {
    std::vector<ColorPatch> patches;
};

struct ColorPatch {
    ColorRGB inputColor;    // 设计颜色
    ColorLab measuredColor; // 实际测量颜色
    int row, col;           // 靶标中的位置
};

// 生成校准靶标
ColorTarget generateCalibrationTarget() {
    ColorTarget target;
    
    // 基本色块
    std::vector<ColorRGB> primaryColors = {
        {1, 0, 0}, {0, 1, 0}, {0, 0, 1},  // RGB
        {0, 1, 1}, {1, 0, 1}, {1, 1, 0},  // CMY
        {0, 0, 0}, {1, 1, 1},              // 黑白
    };
    
    // 灰阶
    for (int i = 0; i <= 10; i++) {
        float v = i / 10.0f;
        primaryColors.push_back({v, v, v});
    }
    
    // 随机采样色块
    for (int i = 0; i < 100; i++) {
        primaryColors.push_back({
            (float)rand() / RAND_MAX,
            (float)rand() / RAND_MAX,
            (float)rand() / RAND_MAX
        });
    }
    
    // 排列为网格
    int cols = 12;
    for (int i = 0; i < primaryColors.size(); i++) {
        ColorPatch patch;
        patch.inputColor = primaryColors[i];
        patch.row = i / cols;
        patch.col = i % cols;
        target.patches.push_back(patch);
    }
    
    return target;
}
```

### 18.4 颜色映射模型

**多项式回归模型：**

$$\begin{bmatrix} R' \\ G' \\ B' \end{bmatrix} = M \cdot \begin{bmatrix} R \\ G \\ B \\ RG \\ RB \\ GB \\ R^2 \\ G^2 \\ B^2 \\ 1 \end{bmatrix}$$

其中 $M$ 是通过最小二乘法拟合的 $3 \times 10$ 矩阵。

```cpp
#include <Eigen/Dense>

// 多项式颜色校正
class PolynomialColorModel {
    Eigen::MatrixXf M;  // 3×10 变换矩阵
    
public:
    void fit(const std::vector<ColorPatch>& patches) {
        int n = patches.size();
        
        // 构建设计矩阵 X (n×10) 和目标矩阵 Y (n×3)
        Eigen::MatrixXf X(n, 10);
        Eigen::MatrixXf Y(n, 3);
        
        for (int i = 0; i < n; i++) {
            float r = patches[i].inputColor.r;
            float g = patches[i].inputColor.g;
            float b = patches[i].inputColor.b;
            
            X(i, 0) = r;  X(i, 1) = g;  X(i, 2) = b;
            X(i, 3) = r*g; X(i, 4) = r*b; X(i, 5) = g*b;
            X(i, 6) = r*r; X(i, 7) = g*g; X(i, 8) = b*b;
            X(i, 9) = 1.0f;
            
            // 目标：测量到的Lab转回RGB
            ColorRGB measuredRGB = labToSrgb(patches[i].measuredColor);
            Y(i, 0) = measuredRGB.r;
            Y(i, 1) = measuredRGB.g;
            Y(i, 2) = measuredRGB.b;
        }
        
        // 最小二乘法: M = (X^T X)^-1 X^T Y
        M = (X.transpose() * X).inverse() * X.transpose() * Y;
    }
    
    ColorRGB correct(const ColorRGB& input) const {
        Eigen::VectorXf x(10);
        x << input.r, input.g, input.b,
             input.r * input.g, input.r * input.b, input.g * input.b,
             input.r * input.r, input.g * input.g, input.b * input.b,
             1.0f;
        
        Eigen::Vector3f result = M * x;
        return {result(0), result(1), result(2)};
    }
};
```

### 18.5 3D LUT（三维查找表）

更精确的颜色映射使用3D LUT，对RGB空间的每个网格点存储校准后的颜色。

```cpp
class ColorLUT3D {
    int gridSize;  // 通常17或33
    std::vector<ColorRGB> table;  // gridSize^3 个条目
    
public:
    ColorLUT3D(int size = 17) : gridSize(size) {
        table.resize(size * size * size);
    }
    
    ColorRGB sample(const ColorRGB& input) const {
        // 三线性插值
        float r = input.r * (gridSize - 1);
        float g = input.g * (gridSize - 1);
        float b = input.b * (gridSize - 1);
        
        int r0 = (int)r, r1 = std::min(r0 + 1, gridSize - 1);
        int g0 = (int)g, g1 = std::min(g0 + 1, gridSize - 1);
        int b0 = (int)b, b1 = std::min(b0 + 1, gridSize - 1);
        
        float fr = r - r0, fg = g - g0, fb = b - b0;
        
        // 8个角的值
        auto get = [&](int ri, int gi, int bi) {
            return table[ri * gridSize * gridSize + gi * gridSize + bi];
        };
        
        ColorRGB c000 = get(r0, g0, b0);
        ColorRGB c100 = get(r1, g0, b0);
        ColorRGB c010 = get(r0, g1, b0);
        ColorRGB c110 = get(r1, g1, b0);
        ColorRGB c001 = get(r0, g0, b1);
        ColorRGB c101 = get(r1, g0, b1);
        ColorRGB c011 = get(r0, g1, b1);
        ColorRGB c111 = get(r1, g1, b1);
        
        // 三线性插值
        ColorRGB result;
        result.r = lerp(
            lerp(lerp(c000.r, c100.r, fr), lerp(c010.r, c110.r, fr), fg),
            lerp(lerp(c001.r, c101.r, fr), lerp(c011.r, c111.r, fr), fg),
            fb);
        result.g = lerp(
            lerp(lerp(c000.g, c100.g, fr), lerp(c010.g, c110.g, fr), fg),
            lerp(lerp(c001.g, c101.g, fr), lerp(c011.g, c111.g, fr), fg),
            fb);
        result.b = lerp(
            lerp(lerp(c000.b, c100.b, fr), lerp(c010.b, c110.b, fr), fg),
            lerp(lerp(c001.b, c101.b, fr), lerp(c011.b, c111.b, fr), fg),
            fb);
        
        return result;
    }
    
    static float lerp(float a, float b, float t) {
        return a + t * (b - a);
    }
};
```

---

## 19. 体素级颜色管理（全彩3D打印）

### 19.1 全彩3D打印流程

```
┌─────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────┐
│ 彩色3D模型   │ → │ 体素化        │ → │ 颜色映射+抖动 │ → │ 切片+打印 │
│ (VRML/AMF/  │   │ (Voxelization)│   │ (Color Map + │   │          │
│  3MF+纹理)  │   │              │   │  Dither)     │   │          │
└─────────────┘   └──────────────┘   └──────────────┘   └──────────┘
```

### 19.2 体素化算法

将三角网格转换为体素表示：

```cpp
struct VoxelGrid {
    std::vector<bool> occupancy;    // 体素占用
    std::vector<ColorRGB> colors;   // 体素颜色
    int width, height, depth;
    float voxelSize;
    
    int index(int x, int y, int z) const {
        return x + y * width + z * width * height;
    }
};

// 基于射线投射的体素化
VoxelGrid voxelizeMesh(const Mesh& mesh, float voxelSize) {
    // 计算网格尺寸
    float w = mesh.max.x - mesh.min.x;
    float h = mesh.max.y - mesh.min.y;
    float d = mesh.max.z - mesh.min.z;
    
    VoxelGrid grid;
    grid.width = (int)(w / voxelSize) + 1;
    grid.height = (int)(h / voxelSize) + 1;
    grid.depth = (int)(d / voxelSize) + 1;
    grid.voxelSize = voxelSize;
    grid.occupancy.resize(grid.width * grid.height * grid.depth, false);
    grid.colors.resize(grid.width * grid.height * grid.depth);
    
    // 对每条X轴射线，找到与网格的交点
    for (int z = 0; z < grid.depth; z++) {
        for (int y = 0; y < grid.height; y++) {
            float py = mesh.min.y + y * voxelSize;
            float pz = mesh.min.z + z * voxelSize;
            
            // 射线: (x, py, pz), x从min到max
            // 找到所有与三角形相交的x值
            std::vector<float> intersections;
            
            for (const auto& tri : mesh.triangles) {
                float t;
                if (rayTriangleIntersect(py, pz, tri, t)) {
                    intersections.push_back(mesh.min.x + t * w);
                }
            }
            
            // 排序交点，两两之间的体素为内部
            std::sort(intersections.begin(), intersections.end());
            
            for (int i = 0; i + 1 < intersections.size(); i += 2) {
                int x0 = (int)((intersections[i] - mesh.min.x) / voxelSize);
                int x1 = (int)((intersections[i + 1] - mesh.min.x) / voxelSize);
                
                for (int x = x0; x <= x1; x++) {
                    grid.occupancy[grid.index(x, y, z)] = true;
                }
            }
        }
    }
    
    return grid;
}
```

### 19.3 颜色到材料的映射

将连续颜色空间映射到有限的打印材料组合：

```cpp
// 将体素颜色映射到材料组合
struct MaterialAssignment {
    int materialIndex;
    float ratio;  // 混合比例（0-1）
};

std::vector<MaterialAssignment> mapColorToMaterials(
    const ColorRGB& targetColor,
    const std::vector<ColorRGB>& materialColors)
{
    // 方法1：最近邻（单一材料）
    // 方法2：最近K个材料的加权混合
    // 方法3：Neugebauer模型反解
    
    // 简化：使用最近3个材料的加权混合
    std::vector<std::pair<float, int>> distances;
    
    ColorLab targetLab = srgbToLab(targetColor);
    
    for (int i = 0; i < materialColors.size(); i++) {
        ColorLab matLab = srgbToLab(materialColors[i]);
        float dE = deltaE00(targetLab, matLab);
        distances.push_back({dE, i});
    }
    
    std::partial_sort(distances.begin(), 
                      distances.begin() + 3, 
                      distances.end());
    
    // 反距离加权
    std::vector<MaterialAssignment> assignments;
    float totalWeight = 0;
    
    for (int i = 0; i < 3; i++) {
        float weight = 1.0f / (distances[i].first + 0.01f);
        totalWeight += weight;
        assignments.push_back({distances[i].second, weight});
    }
    
    // 归一化
    for (auto& a : assignments) {
        a.ratio /= totalWeight;
    }
    
    return assignments;
}
```

### 19.4 层间颜色过渡处理

在Z方向上，相邻层的颜色可能不同，需要处理过渡：

```cpp
// 层间颜色过渡平滑
void smoothLayerColors(std::vector<VoxelGrid>& layers, int smoothWindow) {
    for (int z = smoothWindow; z < layers.size() - smoothWindow; z++) {
        for (int y = 0; y < layers[z].height; y++) {
            for (int x = 0; x < layers[z].width; x++) {
                if (!layers[z].occupancy[layers[z].index(x, y, 0)]) continue;
                
                // 在Z方向上取窗口平均
                ColorRGB avgColor = {0, 0, 0};
                int count = 0;
                
                for (int dz = -smoothWindow; dz <= smoothWindow; dz++) {
                    int zz = z + dz;
                    if (zz < 0 || zz >= layers.size()) continue;
                    if (!layers[zz].occupancy[layers[zz].index(x, y, 0)]) continue;
                    
                    avgColor.r += layers[zz].colors[layers[zz].index(x, y, 0)].r;
                    avgColor.g += layers[zz].colors[layers[zz].index(x, y, 0)].g;
                    avgColor.b += layers[zz].colors[layers[zz].index(x, y, 0)].b;
                    count++;
                }
                
                if (count > 0) {
                    avgColor.r /= count;
                    avgColor.g /= count;
                    avgColor.b /= count;
                    layers[z].colors[layers[z].index(x, y, 0)] = avgColor;
                }
            }
        }
    }
}
```

---

# 第三部分：工程实践与参考实现

## 20. 开源切片器架构分析

### 20.1 CuraEngine (Ultimaker)

**架构特点：**
- C++实现，高性能
- 模块化设计：模型处理 → 切片 → 路径生成 → G-code
- 使用Clipper库进行多边形运算
- 支持多材料、多挤出头

**核心模块：**
```
CuraEngine/
├── mesh/          # 网格处理
├── sliceData/     # 切片数据结构
├── utils/         # 工具类
├── FffProcessor.cpp    # 主处理流程
├── FffGcodeWriter.cpp  # G-code生成
├── FffPolygonGenerator.cpp  # 多边形生成
├── infill/        # 填充算法
│   ├── Infill.cpp
│   ├── GridInfill.cpp
│   ├── GyroidInfill.cpp
│   └── ...
└── support/       # 支撑生成
```

### 20.2 PrusaSlicer (Prusa)

**架构特点：**
- 基于Slic3r演进
- C++重写，性能优秀
- 支持树形支撑、可变层高
- 多材料/多色打印支持强

**核心流程：**
```
1. Model import & repair
2. Object arrangement (打印板排列)
3. Slicing (层切分)
4. Perimeter generation (外壳)
5. Infill generation (填充)
6. Support generation (支撑)
7. Toolpath planning (路径规划)
8. G-code export
```

### 20.3 Slic3r

**架构特点：**
- Perl + C++混合
- 开源社区驱动
- 适合学习参考

### 20.4 OrcaSlicer / BambuStudio

**架构特点：**
- 基于PrusaSlicer分支
- 增强多色打印支持
- 颜色管理更完善

---

## 21. 性能优化策略

### 21.1 空间索引加速

| 数据结构 | 适用场景 | 复杂度 |
|----------|----------|--------|
| BVH树 | 三角形求交加速 | O(log n) |
| 八叉树 | 体素化、空间查询 | O(log n) |
| KD树 | 最近邻搜索 | O(log n) |
| 空间网格哈希 | 均匀分布数据 | O(1) |

### 21.2 并行化

```cpp
// 多线程切片（每层独立）
#include <omp.h>

void parallelSlice(const Mesh& mesh, 
                   const std::vector<float>& layerHeights,
                   std::vector<LayerData>& layers)
{
    #pragma omp parallel for schedule(dynamic)
    for (int i = 0; i < layerHeights.size(); i++) {
        auto segments = sliceMeshBVH(mesh, bvh, layerHeights[i]);
        auto polygons = connectSegments(segments);
        layers[i].polygons = polygons;
        layers[i].z = layerHeights[i];
    }
}
```

### 21.3 GPU加速

使用CUDA/OpenCL加速大规模求交计算：

```cuda
// CUDA并行三角形求交
__global__ void sliceTrianglesKernel(const Triangle* triangles, 
                                      int count, 
                                      float z0,
                                      LineSegment2D* output,
                                      int* outputCount)
{
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    if (idx >= count) return;
    
    const Triangle& tri = triangles[idx];
    
    // 快速Z范围检查
    float zmin = fminf(fminf(tri.v[0].z, tri.v[1].z), tri.v[2].z);
    float zmax = fmaxf(fmaxf(tri.v[0].z, tri.v[1].z), tri.v[2].z);
    if (z0 < zmin || z0 > zmax) return;
    
    // 求交
    LineSegment2D seg;
    if (sliceTriangle(tri, z0, seg)) {
        int outIdx = atomicAdd(outputCount, 1);
        output[outIdx] = seg;
    }
}
```

### 21.4 内存优化

- **流式处理**：逐层处理，不一次性加载所有层
- **数据压缩**：对体素数据使用RLE或八叉树压缩
- **内存池**：预分配内存，避免频繁分配/释放

---

## 22. 推荐学习路径与资源

### 22.1 学习路径

```
阶段1：基础（1-2周）
├── 学习STL文件格式和解析
├── 理解三角形-平面求交数学
├── 实现基础等距切片
└── 输出简单G-code

阶段2：核心算法（2-3周）
├── 实现线段连接成多边形
├── 实现直线填充算法
├── 实现多边形偏移（使用Clipper）
├── 实现外壳生成
└── 完成完整切片流水线

阶段3：进阶（2-3周）
├── 实现多种填充模式
├── 实现支撑生成
├── 实现自适应切片
├── 路径优化（TSP近似）
└── 接缝优化

阶段4：颜色管理（2-3周）
├── 学习颜色空间和转换
├── 实现Delta E色差计算
├── 实现颜色校准流程
├── 实现抖动算法
└── 体素级颜色映射

阶段5：高级（持续）
├── 阅读开源切片器源码
├── GPU加速
├── 全彩3D打印
└── AI辅助切片
```

### 22.2 推荐阅读

**学术论文：**

1. "Adaptive Slicing for 3D Printing" - 自适应切片
2. "Support Generation for FDM 3D Printing" - 支撑生成
3. "Color Management for 3D Printing" - 颜色管理
4. "Voxel-based Color 3D Printing" - 体素级全彩打印
5. "Neugebauer Model for 3D Color Printing" - 颜色混合模型

**书籍：**

1. 《Additive Manufacturing Technologies》- Ian Gibson
2. 《3D Printing and Additive Manufacturing: Principles and Applications》
3. 《Color Management: A Comprehensive Guide for Graphic Designers》
4. 《Digital Color Management: Encoding Solutions》- Edward J. Giorgianni

**标准规范：**

1. ISO/ASTM 52900 - 3D打印术语标准
2. ISO 15076-1 / ICC.1:2010 - ICC颜色管理标准
3. ASTM F3091/F3091M - 3D打印文件格式标准
4. 3MF Specification - 3D制造格式规范（支持颜色）

### 22.3 开源项目参考

| 项目 | 语言 | 特点 | 适合学习 |
|------|------|------|----------|
| CuraEngine | C++ | 工业级，模块化 | 架构设计 |
| PrusaSlicer | C++ | 功能全面 | 完整实现 |
| Slic3r | C++/Perl | 社区驱动 | 算法理解 |
| OrcaSlicer | C++ | 多色增强 | 颜色管理 |
| IceSL | Lua/C++ | 实时切片 | GPU加速 |
| Clipper2 | C++ | 多边形运算 | 几何算法 |
| OpenColorIO | C++ | 颜色管理 | ICC/LUT |

### 22.4 关键算法总结表

| 算法 | 复杂度 | 核心思想 | 应用场景 |
|------|--------|----------|----------|
| 三角形-平面求交 | O(n) | 线性插值 | 切片核心 |
| 线段连接 | O(n) | 哈希表匹配 | 轮廓提取 |
| 扫描线填充 | O(n·k) | 扫描线求交 | 直线填充 |
| 多边形偏移 | O(n log n) | Clipper库 | 外壳生成 |
| Douglas-Peucker | O(n log n) | 递归简化 | 轮廓简化 |
| Floyd-Steinberg | O(n) | 误差扩散 | 颜色抖动 |
| Neugebauer | O(2^k) | 面积加权 | 颜色混合 |
| Delta E 2000 | O(1) | Lab色差 | 颜色匹配 |
| 3D LUT插值 | O(1) | 三线性插值 | 颜色校准 |
| BVH求交 | O(log n) | 层次包围盒 | 加速切片 |

### 22.5 常用工具与库

| 工具/库 | 用途 | 语言 |
|---------|------|------|
| Clipper/Clipper2 | 多边形布尔运算和偏移 | C++ |
| CGAL | 计算几何算法库 | C++ |
| OpenMesh | 三角网格处理 | C++ |
| libigl | 几何处理 | C++ |
| OpenColorIO | 颜色管理和LUT | C++ |
| LittleCMS (lcms2) | ICC配置文件处理 | C |
| Eigen | 线性代数 | C++ |
| VTK | 体素化和可视化 | C++ |
| scikit-image | 图像处理(含Marching Squares) | Python |
| colour-science | 颜色科学 | Python |

---

## 附录：常用公式速查

### 几何

| 公式 | 描述 |
|------|------|
| $P_{\text{intersect}} = P_0 + t(P_1 - P_0)$ | 线段与平面交点 |
| $t = (Z_0 - z_0) / (z_1 - z_0)$ | 参数t的计算 |
| $A = \frac{1}{2}\sum(x_i y_{i+1} - x_{i+1} y_i)$ | 多边形有符号面积 |
| $h_{\text{opt}} = \Delta_{\text{max}} / \tan(\theta)$ | 自适应层高 |

### 颜色

| 公式 | 描述 |
|------|------|
| $XYZ = M \cdot RGB_{\text{linear}}$ | sRGB到XYZ |
| $L = 116f(Y/Y_n) - 16$ | XYZ到Lab |
| $\Delta E_{76} = \sqrt{\Delta L^2 + \Delta a^2 + \Delta b^2}$ | CIE1976色差 |
| $E = d \cdot h \cdot w / (\pi r^2)$ | 挤出量计算 |

---

> **文档版本**：v1.0  
> **最后更新**：2026-09-09  
> **适用范围**：3D打印切片算法与颜色管理算法学习参考
