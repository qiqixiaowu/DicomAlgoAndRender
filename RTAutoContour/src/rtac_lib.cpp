// Placeholder to ensure the static library has at least one translation unit.
// All core algorithms are header-only in include/*.hpp.
namespace rtac { namespace detail { static int _rtac_lib_anchor = 0; } }
