# 肝部与肺部非刚体配准详细文档

> **项目路径**: `algo-fusion-app/src/McsfAlgoRegAppOncology/`  
> **核心算法**: Demons 算法族（CPU + GPU CUDA 加速）  
> **适用场景**: 肿瘤放疗配准——同一患者不同时期（CT-CT）、不同模态（CT-MR、MR-MR）的肝脏/肺部非刚体配准

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [核心数据结构](#2-核心数据结构)
3. [Demons 算法数学原理](#3-demons-算法数学原理)
4. [Diffeomorphic Demons（微分同胚 Demons）](#4-diffeomorphic-demons微分同胚-demons)
5. [多分辨率框架（MultiGrid）](#5-多分辨率框架multigrid)
6. [GPU 加速架构](#6-gpu-加速架构)
7. [全局非刚体配准](#7-全局非刚体配准)
8. [肝部局部非刚体配准](#8-肝部局部非刚体配准)
9. [肺部局部非刚体配准](#9-肺部局部非刚体配准)
10. [ROI 区域配准](#10-roi-区域配准)
11. [关键点定位系统](#11-关键点定位系统)
12. [辅助函数详解](#12-辅助函数详解)
13. [参数配置总表](#13-参数配置总表)
14. [使用示例](#14-使用示例)
15. [面试问答](#15-面试问答)

---

## 1. 系统架构总览

### 1.1 两级配准架构

系统采用 **全局粗配准 + 局部精配准** 的两级架构：

```
┌─────────────────────────────────────────────────────────────────────┐
│                        输入: 参考图 + 浮动图 + 刚体矩阵               │
│                              (refImage, movImage, rigidMat[16])      │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  第一级: 全局非刚体配准 (Global Non-rigid Registration)              │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 1. 刚体矩阵重采样 → 对齐到参考图坐标系                        │    │
│  │ 2. 降采样到粗分辨率 (如 [2,2,4]mm)                            │    │
│  │ 3. 灰度值裁剪 (HU clip / 百分位 clip)                         │    │
│  │ 4. MultiGridDemonsGeneralG (3级金字塔, depth=3)               │    │
│  │    └─ BaseMIDemonsG (Mutual Information 力)                   │    │
│  │ 5. 形变场上采样回原始分辨率                                     │    │
│  │ 6. 重采样输出全局配准结果                                      │    │
│  └─────────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  第二级: 局部器官配准 (Local Organ-specific Registration)            │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 1. 关键点定位 → 确定器官 Z 轴范围 (肝顶/肝底/肺尖)             │    │
│  │ 2. ExtendEdge → 扩展边界 (1/4 比例)                           │    │
│  │ 3. ExtractLocalForZAxis → 提取局部子体积                      │    │
│  │ 4. 局部 Demons 配准 (更精细的参数)                             │    │
│  │    ├─ MRMR: BaseMIDemonsG (3级, sigma=3/7/10)                │    │
│  │    ├─ CTMR: DiffeomorphicMIDemonsG + WeightField (2级)       │    │
│  │    └─ CTCT: BaseMIDemonsG (2级, sigma=10/10)                 │    │
│  │ 5. DisEdgeSmooth → 边缘平滑过渡                                │    │
│  │ 6. LocalDis2Global → 局部形变场融合到全局场                    │    │
│  │ 7. 重采样输出最终配准结果                                      │    │
│  └─────────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
                    输出: 配准后图像 + 形变场
                    (pOutData, pDis_src_tar)
```

### 1.2 模块依赖关系

```
algo-fusion-app (应用层)
  ├── McsfAlgoRegAppOncoGlobalReg  ← 全局非刚体配准
  ├── McsfAlgoRegAppOncoLocalReg   ← 局部器官配准 (肝/肺/ROI)
  └── McsfAlgoRegAppOncoCommon     ← 公共数据结构 (OncologyWarpDis等)
       │
       ├── algo-registrationgpu (GPU Demons)
       │    ├── McsfAlgoBaseDemonsG      ← BaseDemonsG / DiffeomorphicDemonsG
       │    ├── McsfAlgoMultiGridDemonsG ← MultiGridDemonsGeneralG
       │    └── CUDA kernels             ← CalculateOpticalForce.cu 等
       │
       ├── algo-registration (CPU Demons)
       │    ├── McsfAlgoBaseDemons       ← BaseDemons / LCCDemons
       │    └── McsfAlgoMultiGridDemons  ← MultiGridDemonsGeneral
       │
       └── algo-localization
            └── McsfAlgoRegKeyptsLocalize ← 解剖关键点定位
```

### 1.3 支持的配准组合

| 模态组合 | 全局配准 | 肝部局部 | 肺部局部 | ROI局部 |
|---------|---------|---------|---------|---------|
| CT-CT   | ✅ | ✅ | ✅ | ✅ |
| MR-MR   | ✅ | ✅ | ❌ | ✅ |
| CT-MR   | ✅ | ✅ | ❌ | ✅ |

---

## 2. 核心数据结构

### 2.1 OncologyWarpDis — 形变场容器

**文件**: `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoCommon.h`

```cpp
class OncologyWarpDis
{
    std::unique_ptr<float[]> m_pDis;  // 形变场数据 (分量交错存储)
    unsigned int m_uiSize[3];          // 体素尺寸 [x, y, z]
    double m_dSpacing[3];              // 体素间距 [dx, dy, dz] (mm)

public:
    void Reset();                      // 清零形变场
    float* DisPtr();                   // 返回形变场指针
    unsigned long long DisLen();       // 返回形变场长度 = size[0]*size[1]*size[2]*3
    const unsigned int* size();        // 返回尺寸
    const double* spacing();           // 返回间距
};
```

**内存布局**: `m_pDis` 采用**分量交错存储**（component-interleaved）：

```
m_pDis[0] = dx_0     // 体素(0,0,0)的x方向位移
m_pDis[1] = dy_0     // 体素(0,0,0)的y方向位移
m_pDis[2] = dz_0     // 体素(0,0,0)的z方向位移
m_pDis[3] = dx_1     // 体素(1,0,0)的x方向位移
...
m_pDis[3*N-3] = dx_{N-1}
m_pDis[3*N-2] = dy_{N-1}
m_pDis[3*N-1] = dz_{N-1}
```

其中 `N = size[0] * size[1] * size[2]`，总长度为 `3*N`。

> **注意**: CPU Demons 内部使用**分量分离存储**（planar layout）：`dis[0..N-1]` 为 x 分量，`dis[N..2N-1]` 为 y 分量，`dis[2N..3N-1]` 为 z 分量。在接口层进行转换。

### 2.2 CVolumeDataInfo — 体数据信息

```cpp
struct CVolumeDataInfo
{
    short* pImage;              // 图像像素数据 (short 类型)
    unsigned int uiSize[3];     // 体素尺寸
    double dSpacing[3];         // 体素间距 (mm)
    double dOrigin[3];          // 原点坐标
    double RescaleSlope;        // HU 斜率 (DICOM RescaleSlope)
    double RescaleIntercept;    // HU 截距 (DICOM RescaleIntercept)
};
```

### 2.3 Demons 参数结构

| 参数 | 含义 | 典型值 |
|------|------|--------|
| `alpha` | 正则化参数，控制力的大小 | 1.0 |
| `sigma` | 高斯平滑标准差，控制形变场光滑度 | 3~10 |
| `step` | 步长因子，控制每次迭代位移大小 | 0.125~0.25 |
| `stopCriteria` | 收敛阈值（误差变化率） | 0.00001 |
| `itt` | 最大迭代次数 | 10~20 |
| `multi` | 是否多线程 | true |

---

## 3. Demons 算法数学原理

### 3.1 Thirion Demons 力公式

Demons 算法的核心是将配准问题转化为光流问题。对于参考图像 $I_R$ 和浮动图像 $I_M$，在体素位置 $\mathbf{p}$ 处的 Demons 力定义为：

$$
\mathbf{F}(\mathbf{p}) = \frac{-(I_M(\mathbf{p}) - I_R(\mathbf{p})) \cdot \nabla I_R(\mathbf{p})}{|\nabla I_R(\mathbf{p})|^2 + \alpha^2 (I_M(\mathbf{p}) - I_R(\mathbf{p}))^2}
$$

其中：
- $\nabla I_R(\mathbf{p})$ 是参考图像的梯度，使用**中心差分**计算：

$$
\frac{\partial I_R}{\partial x}\bigg|_i = \frac{1}{2}(I_R(i+1) - I_R(i-1))
$$

- $\alpha$ 是正则化参数，防止分母为零并控制力的幅度
- 分母中的 $\alpha^2 (I_M - I_R)^2$ 项确保在图像差异大时力不会发散

**源码实现** (`algo-registration/src/McsfAlgoDemons/src/OpticalForce.cpp`):

```cpp
// 中心差分计算梯度
float gradx = 0.5f * (RefImg[i+1] - RefImg[i-1]);
float grady = 0.5f * (RefImg[i+dimx] - RefImg[i-dimx]);
float gradz = 0.5f * (RefImg[i+dimxy] - RefImg[i-dimxy]);

// 图像差异
float diff = MovImg[i] - RefImg[i];

// Demons 力
float sum = gradx*gradx + grady*grady + gradz*gradz + alpha*alpha*diff*diff;
force_x = -gradx * diff / sum;
force_y = -grady * diff / sum;
force_z = -gradz * diff / sum;

// 累计误差
fError += fabs(diff);
```

### 3.2 力的变体

| 力类型 | 类名 | 适用场景 | 公式特点 |
|--------|------|---------|---------|
| Optical/SSD | `OpticalForce` / `DemonsOpticalForce` | 同模态 (CT-CT, MR-MR) | Thirion 原始公式 |
| Joint Histogram (JH) | `JHForce` | 跨模态 | 基于联合直方图熵的梯度 |
| Mutual Information (MI) | `MIForce` / `MIForce` | 跨模态 (CT-MR) | 基于互信息梯度 |
| Normalized MI (NMI) | `NMIForce` | 跨模态 | 归一化互信息，更鲁棒 |
| Local Cross Correlation (LCC) | `LCC_Force` | 局部相关性 | 局部窗口内互相关 |

### 3.3 运动场更新

力的计算完成后，通过以下步骤更新形变场：

**步骤 1: 高斯平滑力场**

$$
\mathbf{F}_{smooth} = G_\sigma * \mathbf{F}
$$

其中 $G_\sigma$ 是标准差为 $\sigma$ 的高斯核。高斯平滑确保形变场的空间连续性。

**步骤 2: 归一化并缩放**

$$
\mathbf{F}_{scaled} = \frac{step}{\max(|\mathbf{F}_{smooth}|)} \cdot \mathbf{F}_{smooth}
$$

找到力场中的最大值 `maxForce`，计算缩放因子 `fstep = step / maxForce`，确保位移不会过大。

**步骤 3: 累加到位移场**

$$
\mathbf{u}_{new} = \mathbf{u}_{old} + \mathbf{F}_{scaled}
$$

**源码实现** (`MovingField.cpp`):

```cpp
void DemonsMovingField::CalculateMovingField(float* force, float* dis, 
    int* dim, float step, ...)
{
    // 1. 高斯平滑力场 (3个分量分别平滑)
    GaussSmoothField(force, dim, sigma, threads);
    
    // 2. 找最大力
    float maxForce = 0;
    for (int i = 0; i < len*3; ++i)
        maxForce = max(maxForce, fabs(force[i]));
    
    // 3. 缩放
    float fstep = step / maxForce;
    MulC(fstep, force, len*3);
    
    // 4. 累加到位移场
    Add(force, dis, len*3);
}
```

### 3.4 图像变形（ApplyMovingField）

给定浮动图像 $I_M$ 和形变场 $\mathbf{u}$，变形后的图像通过**三线性插值**获得：

$$
I_{warped}(\mathbf{p}) = I_M(\mathbf{p} + \mathbf{u}(\mathbf{p}))
$$

**源码实现** (`ApplyMovingField.cu` / `Resampling.cpp`):

```cpp
// 对每个体素
float x_pos = i + dis[index];           // x + ux
float y_pos = j + dis[index + len];     // y + uy
float z_pos = k + dis[index + 2*len];   // z + uz

// 边界裁剪
x_pos = clamp(x_pos, 0, dimx - 1.001f);
y_pos = clamp(y_pos, 0, dimy - 1.001f);
z_pos = clamp(z_pos, 0, dimz - 1.001f);

// 三线性插值 (8个邻近体素的加权平均)
int x0 = floor(x_pos), x1 = x0 + 1;
int y0 = floor(y_pos), y1 = y0 + 1;
int z0 = floor(z_pos), z1 = z0 + 1;
float fx = x_pos - x0, fy = y_pos - y0, fz = z_pos - z0;

MovImgNew[index] = (1-fx)*(1-fy)*(1-fz) * MovImg[x0+y0*dimx+z0*dimxy]
                 + (1-fx)*fy*(1-fz)     * MovImg[x0+y1*dimx+z0*dimxy]
                 + fx*(1-fy)*(1-fz)     * MovImg[x1+y0*dimx+z0*dimxy]
                 + fx*fy*(1-fz)         * MovImg[x1+y1*dimx+z0*dimxy]
                 + (1-fx)*(1-fy)*fz     * MovImg[x0+y0*dimx+z1*dimxy]
                 + (1-fx)*fy*fz         * MovImg[x0+y1*dimx+z1*dimxy]
                 + fx*(1-fy)*fz         * MovImg[x1+y0*dimx+z1*dimxy]
                 + fx*fy*fz             * MovImg[x1+y1*dimx+z1*dimxy];
```

### 3.5 收敛判定

```cpp
// 迭代循环
for (int i = 0; i < iterNum; ++i) {
    CalculateForce(refImg, movImgNew, dim, alpha, force, error, temp);
    CalculateMovingField(force, dim, step, temp, dis, deltaDis, kerLen);
    ApplyMovingField(movImg, dis, dim, movImgNew);
    
    if (i != 0) {
        ratio = abs(error0 - error) / error0;
        error0 = error;
        if (ratio < stopCriteria && i > iMin)  // iMin=10 (CPU) 或 0 (GPU Diffeomorphic)
            break;
    } else {
        error0 = error;
    }
}
```

- `error` = 所有体素灰度差异绝对值之和 $\sum |I_M(\mathbf{p}+\mathbf{u}) - I_R(\mathbf{p})|$
- 收敛条件：误差变化率 $\frac{|error_0 - error|}{error_0} < stopCriteria$
- 最小迭代次数：CPU 版本 `iMin=10`，GPU Diffeomorphic 版本 `iMin=0`

### 3.6 完整迭代流程图

```
┌──────────────────────────────────────────────────┐
│              Demons 迭代主循环                     │
│                                                  │
│  ┌─────────────┐                                │
│  │ ApplyMoving │ ← 用当前形变场变形浮动图像        │
│  │   Field     │                                │
│  └──────┬──────┘                                │
│         │                                        │
│  ┌──────▼──────┐                                │
│  │ Calculate   │ ← 计算参考图与变形图之间的力      │
│  │   Force     │   (Optical/MI/NMI/LCC)         │
│  └──────┬──────┘                                │
│         │                                        │
│  ┌──────▼──────┐                                │
│  │ Calculate   │ ← 高斯平滑力 → 归一化 → 累加     │
│  │ MovingField │   到形变场                      │
│  └──────┬──────┘                                │
│         │                                        │
│  ┌──────▼──────┐                                │
│  │  收敛判定    │ ← error变化率 < stopCriteria?  │
│  └──────┬──────┘                                │
│         │                                        │
│    是 ──┼── 否 → 继续迭代                        │
│         │                                        │
│  ┌──────▼──────┐                                │
│  │  输出结果    │ ← 形变场 + 变形图像             │
│  └─────────────┘                                │
└──────────────────────────────────────────────────┘
```

---

## 4. Diffeomorphic Demons（微分同胚 Demons）

### 4.1 动机

标准 Demons 算法不保证形变场的可逆性（即 $\phi^{-1}$ 可能不存在），这会导致：
- 形变场可能出现折叠（folding）
- 无法保证拓扑保持性
- 多次配准的复合可能产生不合理结果

**Diffeomorphic Demons** 通过将形变场参数化为微分同胚映射来解决这些问题。

### 4.2 Scaling and Squaring（缩放-平方法）

微分同胚映射通过指数映射获得。给定速度场 $\mathbf{v}$，形变场 $\phi = \exp(\mathbf{v})$。

指数映射通过 **scaling and squaring** 近似计算：

$$
\phi = \exp(\mathbf{v}) = \left(\exp\left(\frac{\mathbf{v}}{2^n}\right)\right)^{2^n}
$$

**算法步骤**:

1. **Scaling**: 将速度场缩小 $2^n$ 倍：$\mathbf{v}_{scaled} = \mathbf{v} / 2^n$
   - $n$ 的选择使得 $|\mathbf{v}_{scaled}|$ 足够小（近似一阶泰勒展开有效）
   - 自动确定：$n = \lceil 2 + \log_2(\max|\mathbf{v}|) \rceil$

2. **Squaring**: 重复平方 $n$ 次：
   ```
   φ = v_scaled          // 初始：小形变场
   for i = 1 to n:
       φ = φ ∘ φ         // 复合：φ_new(p) = φ(φ(p))
   ```

**源码实现** (`BaseDemons.cpp` 中的 `Integrate` 函数):

```cpp
void BaseDemons::Integrate(float* dis, int* dim, int nsteps)
{
    // Scaling: 缩小 2^nsteps 倍
    float scale = 1.0f / pow(2.0f, nsteps);
    MulC(scale, dis, len * 3);
    
    // Squaring: 重复复合 nsteps 次
    float* dis_temp = new float[len * 3];
    for (int i = 0; i < nsteps; ++i) {
        memcpy(dis_temp, dis, sizeof(float) * len * 3);
        // φ_new(p) = φ(p) + u(φ(p))  — 用当前形变场变形自身
        ComposeDisplacement(dis, dis_temp, dim);
    }
    delete[] dis_temp;
}
```

### 4.3 对称力（Symmetric Force）

标准 Demons 只使用参考图像的梯度。对称力版本同时计算两个方向的力并取平均：

$$
\mathbf{F}_{sym} = \frac{1}{2}\left(\frac{-(I_M - I_R) \cdot \nabla I_R}{|\nabla I_R|^2 + \alpha^2(I_M-I_R)^2} + \frac{-(I_M - I_R) \cdot \nabla I_M}{|\nabla I_M|^2 + \alpha^2(I_M-I_R)^2}\right)
$$

```cpp
if (m_use_symmetric_force) {
    m_force->CalculateForce(d_refImg, d_movImgNew, ...);  // 正向力
    m_force->CalculateForce(d_movImgNew, d_refImg, ...);  // 反向力
    CalculateSymmetricForce(d_force, d_force_mov, img_dim); // 取平均
}
```

### 4.4 权重场（Weight Field）

在 CT-MR 配准中，使用**权重场**来控制不同区域的配准强度。权重场通常来自器官掩膜（mask），在器官内部权重为 1，外部为 0：

```cpp
// 在力计算后、运动场更新前，对力进行加权
m_movingFiled->WeightForce(d_weight, img_dim, d_force);
```

`WeightForceField` CUDA 函数将力场逐体素乘以权重值：

$$
\mathbf{F}_{weighted}(\mathbf{p}) = w(\mathbf{p}) \cdot \mathbf{F}(\mathbf{p})
$$

### 4.5 DiffeomorphicDemonsG 类

**文件**: `algo-registrationgpu/include/algo-registrationgpu/McsfAlgoBaseDemonsG.h`

```cpp
class DiffeomorphicDemonsG : public BaseDemonsG
{
    bool m_use_symmetric_force;  // 是否使用对称力

public:
    DiffeomorphicDemonsG(float alpha, float sigma, float step, 
                         float stopCriteria, unsigned iterNum, bool multiThread,
                         double* img_spacing, bool use_firstorder_exp);
    
    void SetUseFirstOrderExp(bool use_firstorder_exp);
    void SetExpMaximumUpdateStepLength(float imposed_maxup_step);
    void SetNormalizer(float normalizer);
    void SetUseSymmetricForce(bool use_symmetric_force);
    
    // 标准配准
    bool DemonsRegistration(...);
    
    // 带权重场的配准
    bool DemonsRegistration_WeightField(const short* h_refImg, 
        const short* h_movImg, const float* h_WeightField, ...);
    
    // 带权重场 + 指定多分辨率层级
    bool DemonsRegistration_WeightField_SpecificGrid(const short* h_refImg,
        const short* h_movImg, const float* h_WeightField, 
        unsigned int MGD_uiDeepth, ...);
};
```

**关键配置**:
- `m_curve = true`: 启用 `deltaDis`（增量形变场），用于 Diffeomorphic 复合
- `m_use_firstorder_exp`: 是否使用一阶指数近似（更快但精度略低）
- `m_use_autonumiter_exp = true`: 自动确定 scaling-squaring 的迭代次数
- `m_maxnumiter_exp = 20`: 最大指数迭代次数
- `m_normalizer = -1.0f`: 自动归一化
- `m_sqr_minspacing`: 最小体素间距的平方，用于归一化

### 4.6 DiffeomorphicMIDemonsG

```cpp
class DiffeomorphicMIDemonsG : public DiffeomorphicDemonsG
{
    // 构造函数中替换力计算器为 MIForce
    DiffeomorphicMIDemonsG(...) : DiffeomorphicDemonsG(...)
    {
        delete m_force;
        m_force = new MIForce;  // 使用互信息力
    }
};
```

**用于 CT-MR 肝部配准**，因为 CT 和 MR 的灰度对应关系不是线性的，需要基于统计相关性的力。

---

## 5. 多分辨率框架（MultiGrid）

### 5.1 动机

单分辨率 Demons 容易陷入局部最优，且对大形变收敛慢。多分辨率（金字塔）策略：
1. 在粗分辨率下捕获大形变
2. 逐级细化到高分辨率
3. 提高收敛速度和鲁棒性

### 5.2 MultiGridDemonsGeneralG

**文件**: `algo-registrationgpu/include/algo-registrationgpu/McsfAlgoMultiGridDemonsG.h`

```cpp
class MultiGridDemonsGeneralG : public MultiGridRegistrationInterfaceG
{
    unsigned m_deepth;       // 金字塔顶层深度 (如 3 表示最粗层缩小 2^2=4 倍)
    unsigned m_deepEnd;      // 金字塔底层深度 (通常为 1，即原始分辨率)
    BaseDemonsG** m_pDemons; // 每层使用不同的 Demons 实例 (可不同参数)
    
    bool DoRegistration(const short* h_refImg, const short* h_movImg, 
        const int img_dim[3], float* h_dis, ...);
    
    // GPU 缓冲区复用版本 (减少显存分配/释放开销)
    bool DoRegistration_withBuffer(const short* h_refImg, const short* h_movImg, 
        const int img_dim[3], const int reg_dim[3], float* h_dis, ...);
};
```

### 5.3 多分辨率流程

```
深度 deepth=3, deepEnd=1:

Level 3 (最粗):  dim / 4  →  Demons[2] 配准  →  形变场上采样
       ↓
Level 2 (中间):  dim / 2  →  Demons[1] 配准  →  形变场上采样
       ↓
Level 1 (原始):  dim / 1  →  Demons[0] 配准  →  最终形变场
```

**每层的操作**:

```cpp
for (uideepth = deepth; uideepth >= deepEnd; --uideepth) {
    // 1. 计算缩放因子
    unsigned uiScale = pow(2, uideepth - 1);  // Level 3 → scale=4, Level 2 → scale=2, Level 1 → scale=1
    
    // 2. 计算粗分辨率尺寸
    iDimLocal[0] = max(dim[0] / uiScale, 1);
    iDimLocal[1] = max(dim[1] / uiScale, 1);
    iDimLocal[2] = max(dim[2] / uiScale, 1);
    
    // 3. 降采样图像和形变场
    ImageResize(RefImg, RefLocal, dim, iDimLocal, scale);
    ImageResize(MovImg, MovLocal, dim, iDimLocal, scale);
    for (n = 0..2) {
        ImageResize(&Disp[n*lenOri], &DispLocal[n*len], dim, iDimLocal, scale);
        MulC(scale[n], DispLocal[n*len], len);  // 形变场也要缩放
    }
    
    // 4. 在粗分辨率下执行 Demons 配准
    Demons[uideepth - deepEnd]->DemonsRegistration(RefLocal, MovLocal, iDimLocal, DispLocal);
    
    // 5. 形变场上采样回原分辨率
    for (n = 0..2) {
        MulC(1.0/scale[n], DispLocal[n*len], len);  // 反向缩放形变场
        ImageResize(&DispLocal[n*len], &Disp[n*lenOri], iDimLocal, dim, 1.0/scale);
    }
}
```

### 5.4 DoRegistration_withBuffer（GPU 优化版）

标准 `DoRegistration` 在每层都重新分配/释放 GPU 显存。`DoRegistration_withBuffer` 通过以下优化减少开销：

1. **一次性分配所有 GPU 缓冲区**：使用 `IdxPool` 管理偏移量
2. **GPU 端图像缩放**：使用 `demonsImageResizeG` 替代 CPU 的 `ImageResize`
3. **支持自定义配准分辨率**：`reg_dim` 可以与 `img_dim` 不同（用于统一间距处理）

```cpp
// 缓冲区布局
IdxPool idx_pool;
idx_pool.PushBack("pOrgRef",    org_len * sizeof(short));       // 原始分辨率参考图
idx_pool.PushBack("pOrgMov",    org_len * sizeof(short));       // 原始分辨率浮动图
idx_pool.PushBack("pOrgDis",    org_len * sizeof(float) * 3);   // 原始分辨率形变场
idx_pool.PushBack("pRegRef",    reg_len * sizeof(short));       // 配准分辨率参考图
idx_pool.PushBack("pRegMov",    reg_len * sizeof(short));       // 配准分辨率浮动图
idx_pool.PushBack("pRegMovNew", reg_len * sizeof(short));       // 变形后图像
idx_pool.PushBack("pRegDis",    reg_len * sizeof(float) * 3);   // 配准分辨率形变场
idx_pool.PushBack("pRegForce",  reg_len * sizeof(float) * 3);   // 力场
idx_pool.PushBack("pRegTemp",   reg_len * sizeof(float));       // 临时缓冲区

cudaMalloc(&m_pBuffer_d, idx_pool.GetLen());  // 一次分配
```

---

## 6. GPU 加速架构

### 6.1 GPU Demons 类层次

```
ForceInterface (接口)
  ├── OpticalForce      → CalculateOpticalForce.cu
  ├── JHForce           → JointEntropyForce.cu
  ├── MIForce           → MutualInformationForce.cu
  └── NMIForce          → NMutualInformationForce.cu

MovingFiledInterface (接口)
  ├── BaseMovingFiled           → CalculateMovingField.cu
  └── DiffeomorphicMovingField  → CalculateDiffeomorphicMovingField.cu

BaseDemonsG (基类)
  ├── m_force: ForceInterface*
  ├── m_movingFiled: MovingFiledInterface*
  ├── m_alpha, m_sigma, m_step, m_stopCriteria, m_iterNum
  ├── m_Gausskernal, m_GaussKerlen
  │
  ├── BaseJHDemonsG       (force = JHForce)
  ├── BaseMIDemonsG       (force = MIForce)       ← 最常用
  ├── BaseNMIDemonsG      (force = NMIForce)
  │
  └── DiffeomorphicDemonsG (movingFiled = DiffeomorphicMovingField, m_curve=true)
       ├── DiffeomorphicMIDemonsG  (force = MIForce)     ← CT-MR 肝部用
       └── DiffeomorphicNMIDemonsG (force = NMIForce)
```

### 6.2 CUDA Kernel 文件

| CUDA 文件 | 功能 |
|-----------|------|
| `CalculateOpticalForce.cu` | 光流力计算（Thirion Demons 公式） |
| `CalculateMovingField.cu` | 标准运动场更新（高斯平滑 + 缩放 + 累加） |
| `CalculateDiffeomorphicMovingField.cu` | 微分同胚运动场更新（含 scaling-squaring） |
| `ApplyMovingField.cu` | 图像变形（三线性插值） |
| `CalculateDiff.cu` | 图像差异计算 |
| `Histogram.cu` | 灰度直方图计算 |
| `JointHistogram.cu` | 联合直方图计算 |
| `Entropy.cu` | 熵和互信息计算 |
| `JointEntropyForce.cu` | 联合熵力计算 |
| `MutualInformationForce.cu` | 互信息力计算 |
| `NMutualInformationForce.cu` | 归一化互信息力计算 |

### 6.3 GPU 内存管理

**显存估算** (`BaseDemonsG::GetGPUPeakMemUsed`):

```cpp
double totalBytes = 0;
// d_refImg + d_movImg + d_movImgNew + cudaArray(d_movImg) + d_movImg(备份)
totalBytes += 5 * imgPixNum * sizeof(short);
// d_dis + d_force
totalBytes += 2 * imgPixNum * 3 * sizeof(float);
// d_temp + cudaArray(d_dis_x)
totalBytes += 2 * imgPixNum * sizeof(float);
```

对于 $256 \times 256 \times 200$ 的体数据：
- 像素数 = 13,107,200
- 显存 ≈ $5 \times 13M \times 2 + 2 \times 13M \times 3 \times 4 + 2 \times 13M \times 4$ = 130MB + 314MB + 105MB ≈ **549MB**

**GPU 内存计数器** (`DemonsGPUMemCounter`):

```cpp
class DemonsGPUMemCounter {
    double m_peakBytes;  // 峰值显存
    double m_curBytes;   // 当前显存
    void Push(double b); // 分配显存
    void Pop(double b);  // 释放显存
    double GetPeakBytes();
};
```

### 6.4 GPU 迭代主循环

```cpp
bool BaseDemonsG::DemonsRegistration(...)
{
    // 1. 分配 GPU 内存
    cudaMalloc(&d_refImg, ...);
    cudaMalloc(&d_movImg, ...);
    cudaMalloc(&d_movImgNew, ...);
    cudaMalloc(&d_dis, ...);      // 形变场 (3*len*sizeof(float))
    cudaMalloc(&d_force, ...);    // 力场 (3*len*sizeof(float))
    cudaMalloc(&d_temp, ...);     // 临时缓冲区
    
    // 2. 拷贝数据到 GPU
    cudaMemcpy(d_refImg, h_refImg, ..., HostToDevice);
    cudaMemcpy(d_movImg, h_movImg, ..., HostToDevice);
    cudaMemcpy(d_dis, h_dis, ..., HostToDevice);
    
    // 3. 初始化高斯核
    InitGuassKernel(m_Gausskernal, ...);
    
    // 4. 迭代主循环
    ApplyMovingField(d_movImg, d_dis, img_dim, d_movImgNew);  // 初始变形
    for (int i = 0; i < m_iterNum; ++i) {
        m_force->CalculateForce(d_refImg, d_movImgNew, ...);      // GPU 计算力
        m_movingFiled->CalculateMovingFiled(d_force, ...);        // GPU 更新形变场
        ApplyMovingField(d_movImg, d_dis, img_dim, d_movImgNew);  // GPU 变形图像
        
        // 收敛判定
        ratio = abs(dError0 - dError) / dError0;
        if (ratio < m_stopCriteria && i > iMin) break;
    }
    
    // 5. 拷贝结果回 CPU
    cudaMemcpy(h_dis, d_dis, ..., DeviceToHost);
    
    // 6. 释放 GPU 内存
    cudaFree(d_refImg); cudaFree(d_movImg); ...
}
```

### 6.5 DemonsRegistration_d（设备端接口）

`DemonsRegistration_d` 接受已在 GPU 上的指针，避免重复的 Host↔Device 数据传输。用于 `DoRegistration_withBuffer` 中多层级配准的内部调用：

```cpp
bool BaseDemonsG::DemonsRegistration_d(
    const short* d_refImg,      // 已在 GPU 上
    const short* d_movImg,      // 已在 GPU 上
    short* d_movImgNew,         // 已在 GPU 上
    const int img_dim[3],
    float* d_dis,               // 已在 GPU 上
    float* d_force,             // 已在 GPU 上 (外部分配)
    float* d_tmp,               // 已在 GPU 上 (外部分配)
    ...);
```

---

## 7. 全局非刚体配准

### 7.1 API 接口

**文件**: `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoGlobalReg.h`

```cpp
// CT-CT 全局非刚体配准
bool OncologyNonrigidCTCT_Global(
    const CVolumeDataInfo& refImage,
    const CVolumeDataInfo& movImage,
    const double rigidMat[16],
    short* pOutData,
    OncologyWarpDis* pDis_src_tar,
    Mcsf::IProgress* pProgress = nullptr);

// MR-MR 全局非刚体配准
bool OncologyNonrigidMRMR_Global(...);

// CT-MR 全局非刚体配准
bool OncologyNonrigidCTMR_Global(...);
```

### 7.2 CT-CT 全局配准流程

**文件**: `algo-fusion-app/src/McsfAlgoRegAppOncology/src/McsfAlgoRegAppOncoGlobalReg.cpp`

```
输入: refImage (CT), movImage (CT), rigidMat[16]

Step 1: 刚体重采样
  └─ OncologyResampleCacade(refImage, movImage, rigidMat)
     → 将浮动图通过刚体矩阵变换到参考图坐标系
     → 输出: resampledMov (对齐后的浮动图)

Step 2: 降采样
  └─ 降采样到间距 [2, 2, 4] mm
     → 减少计算量，加速配准

Step 3: 灰度裁剪
  └─ HU 值裁剪到 [-140, 260]
     → 去除无关组织（骨骼、空气），聚焦软组织

Step 4: 3级 MultiGrid 配准
  └─ MultiGridDemonsGeneralG(depth=3, deepEnd=1)
     ├─ Level 3: BaseMIDemonsG(alpha=1, sigma=10, step=0.25, criteria=0.00001, itt=20, multi=true)
     ├─ Level 2: BaseMIDemonsG(alpha=1, sigma=10, step=0.25, criteria=0.00001, itt=20, multi=true)
     └─ Level 1: BaseMIDemonsG(alpha=1, sigma=10, step=0.25, criteria=0.00001, itt=20, multi=true)
     → 3个层级使用相同参数的 Demons 实例

Step 5: 形变场上采样
  └─ 将粗分辨率的形变场 resize 回原始分辨率

Step 6: 重采样输出
  └─ 用形变场变形原始分辨率浮动图
  → 输出: pOutData (配准后图像), pDis_src_tar (形变场)
```

### 7.3 MR-MR 全局配准流程

```
Step 1: 刚体重采样
Step 2: 降采样到间距 [1, 1, 2] mm  (MR 分辨率通常高于 CT)
Step 3: 无灰度裁剪 (MR 无 HU 值)
Step 4: 2级 MultiGrid 配准
  └─ MultiGridDemonsGeneralG(depth=3, deepEnd=2)
     ├─ Level 3: BaseMIDemonsG(sigma=10, step=0.125, itt=20)
     └─ Level 2: BaseMIDemonsG(sigma=10, step=0.25, itt=20)
     → 注意: deepEnd=2，不在原始分辨率配准（节省时间）
Step 5-6: 同 CT-CT
```

### 7.4 CT-MR 全局配准流程

```
Step 1: 刚体重采样
Step 2: 降采样到间距 [1, 1, 1] mm  (统一到各向同性)
Step 3: 无灰度裁剪 (跨模态，HU 裁剪无意义)
Step 4: 2级 MultiGrid 配准
  └─ MultiGridDemonsGeneralG(depth=3, deepEnd=2)
     ├─ Level 3: BaseMIDemonsG(sigma=3, step=0.125, itt=20)
     └─ Level 2: BaseMIDemonsG(sigma=7, step=0.25, itt=20)
     → 注意: sigma 从 3→7 递增，粗层用小 sigma 保留细节，细层用大 sigma 增强平滑
     → 使用 MI 力（互信息），因为 CT-MR 灰度非线性对应
Step 5-6: 同 CT-CT
```

### 7.5 三种全局配准参数对比

| 参数 | CT-CT | MR-MR | CT-MR |
|------|-------|-------|-------|
| 降采样间距 | [2,2,4] | [1,1,2] | [1,1,1] |
| 灰度裁剪 | [-140,260] HU | 无 | 无 |
| 金字塔深度 | 3→1 (3级) | 3→2 (2级) | 3→2 (2级) |
| 力类型 | MI | MI | MI |
| sigma | 10/10/10 | 10/10 | 3/7 |
| step | 0.25/0.25/0.25 | 0.125/0.25 | 0.125/0.25 |
| itt | 20/20/20 | 20/20 | 20/20 |
| alpha | 1 | 1 | 1 |
| stopCriteria | 0.00001 | 0.00001 | 0.00001 |

> **注意**: 全局配准统一使用 `BaseMIDemonsG`（互信息力），即使 CT-CT 同模态也用 MI，因为 MI 对灰度差异更鲁棒。

---

## 8. 肝部局部非刚体配准

### 8.1 API 接口

**文件**: `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoLocalReg.h`

```cpp
// CT-CT 肝部配准
bool OncologyNonrigidCTCT_Liver(
    const CVolumeDataInfo& refImage,
    const CVolumeDataInfo& movImage,
    const double rigidMat[16],
    short* pOutData,
    OncologyWarpDis* pDis_src_tar,
    Mcsf::IProgress* pProgress = nullptr);

// MR-MR 肝部配准
bool OncologyNonrigidMRMR_Liver(...);

// CT-MR 肝部配准
bool OncologyNonrigidCTMR_Liver(...);

// GPU 显存估算 (CT-MR / MR-MR)
double OncologyNonrigidCTMR_Liver_GetMaxGPUMem(const unsigned int uiSize[3], 
    const double dSpacing[3], const double rigidMat[16]);
double OncologyNonrigidMRMR_Liver_GetMaxGPUMem(...);
```

### 8.2 MR-MR 肝部配准详细流程

**函数**: `OncologyNonrigidMRMR_Liver` (L1514)

```
输入: refImage (MR), movImage (MR), rigidMat[16], pDis_src_tar (可能已有全局形变场)

Step 1: 检查是否有先验形变场
  ├─ 如果 pDis_src_tar 为空 → 执行全局非刚体配准 OncologyNonrigidMRMR_Global
  └─ 如果已有 → 直接使用 (两级配准的入口)

Step 2: 肝部 Z 轴范围定位
  └─ GetMRLiverRange(refImage, movImage, organZRange)
     ├─ 对参考图和浮动图分别调用 McsfAlgoRegKeyptsLocalize::GetMRKeyPts_v2
     ├─ 获取关键点标签 refLabel[14] / movLabel[14] 和坐标 refPts[42] / movPts[42]
     ├─ 肝顶 Z = refPts[label_7 * 3 + 2]  (label 7 = 肝顶)
     ├─ 肝底 Z = refPts[label_14 * 3 + 2] (label 14 = 肝底)
     ├─ 取参考图和浮动图肝脏范围的并集
     └─ 输出: organZRange[0] (Z最小), organZRange[1] (Z最大)

Step 3: 边界扩展
  └─ ExtendEdge(organZRange, refImage.uiSize[2])
     → Z范围向上下各扩展 1/4 的当前范围长度
     → 确保配准区域覆盖完整肝脏及周围过渡区

Step 4: 提取局部子体积
  └─ ExtractLocalForZAxis(refImage, organZRange, refLocal)
  └─ ExtractLocalForZAxis(movImage, organZRange, movLocal)
     → 沿 Z 轴裁剪出肝脏区域的子体积
     → 大幅减少计算量 (只配准肝脏区域而非整个体数据)

Step 5: 局部非刚体配准
  └─ MRMRLocalNonRigidReg(refLocal, movLocal, localDis)
     ├─ 降采样到间距 [1, 1, 1] mm
     ├─ 3级 MultiGrid 配准:
     │  MultiGridDemonsGeneralG(depth=3, deepEnd=1)
     │  ├─ Level 3: BaseMIDemonsG(sigma=3,  step=0.125, itt=20)  ← 小 sigma 保留细节
     │  ├─ Level 2: BaseMIDemonsG(sigma=7,  step=0.25,  itt=20)  ← 中等 sigma
     │  └─ Level 1: BaseMIDemonsG(sigma=10, step=0.25,  itt=20)  ← 大 sigma 平滑
     └─ 形变场上采样回原始局部分辨率

Step 6: 边缘平滑
  └─ DisEdgeSmooth(localDis, localDim, ...)
     → 在局部子体积边缘处，将形变场按距离边界的比例缩放至 0
     → 确保局部形变场与全局形变场在边界处平滑过渡，无突变

Step 7: 局部形变场融合到全局
  └─ LocalDis2Global(pDis_src_tar, localDis, organZRange, ...)
     → 将局部形变场添加到全局形变场的对应位置
     → offset = organZRange[0] (Z轴偏移量)

Step 8: 重采样输出
  └─ 用融合后的全局形变场变形原始浮动图
  → 输出: pOutData (最终配准图像)
```

### 8.3 CT-MR 肝部配准详细流程

**函数**: `OncologyNonrigidCTMR_Liver` (L1631)

这是最复杂的配准场景，因为：
1. CT 和 MR 灰度非线性对应
2. 需要处理 CT 的 HU 值和 MR 的百分位值
3. 使用 Diffeomorphic Demons 保证拓扑保持
4. 使用权重场（mask）限制配准区域

```
输入: refImage (CT), movImage (MR), rigidMat[16]

Step 1: CT 预处理
  ├─ HU 值转换: pixel = raw * RescaleSlope + RescaleIntercept
  ├─ CT 床体去除: 使用阈值分割去除检查床
  └─ 如果无先验形变场 → 执行全局非刚体配准 OncologyNonrigidCTMR_Global

Step 2: 肝部 Z 轴范围定位 (CTMRLocalRange)
  ├─ 对 CT 调用 GetCTKeyPts_v2 → 获取 CT 关键点
  ├─ 对 MR 调用 GetMRKeyPts_v2 → 获取 MR 关键点
  ├─ CT 肝顶: label[7], 肝底: label[14]
  ├─ MR 肝顶: label[7], 肝底: label[14]
  ├─ 计算 LiverTopDistance 先验:
  │   = CT肝顶Z - MR肝顶Z (体素单位)
  │   → 用于初始化 Z 方向位移，加速收敛
  └─ 取 CT 和 MR 肝脏范围的并集

Step 3: 边界扩展 + 提取局部子体积
  └─ 同 MR-MR，但需要同时处理 CT 和 MR 的局部子体积

Step 4: 灰度预处理
  ├─ CT: HU 裁剪到 [-250, 250]
  │   → 聚焦肝脏软组织范围
  └─ MR: 百分位裁剪 [0.01, 0.99]
      └─ GetWinValue(movLocal, backRatio=0.01, frontRatio=0.99)
          → 计算灰度直方图，取 1%~99% 分位数作为窗宽窗位
          → 去除极端噪声像素

Step 5: 初始化 Z 方向位移
  └─ 将 LiverTopDistance 设置为局部形变场的初始 Z 位移
     → 为 Demons 提供更好的初始猜测

Step 6: 生成权重场 (Weight Field)
  └─ 从 CT 局部子体积生成肝脏 mask
     ├─ 阈值分割: -250 ~ 250 HU 范围内的体素
     ├─ 形态学操作: 去除小连通域
     └─ 权重场 = mask (肝脏内=1.0, 外=0.0)

Step 7: 局部 Diffeomorphic Demons 配准
  └─ CTMRLocalNonRigidReg(refLocal, movLocal, weightField, localDis)
     ├─ 降采样到间距 [1, 1, 1] mm
     ├─ 2级配准:
     │  DiffeomorphicMIDemonsG:
     │  ├─ Stage 1: sigma=3, step=0.125, itt=20
     │  │   └─ DemonsRegistration_WeightField_SpecificGrid(deepth=3)
     │  │       → 在 depth=3 的粗分辨率下配准，使用权重场
     │  └─ Stage 2: sigma=7, step=0.25, itt=20
     │      └─ DemonsRegistration_WeightField_SpecificGrid(deepth=3)
     │          → 再次在 depth=3 配准，更大步长和 sigma
     ├─ Diffeomorphic 保证形变场可逆（拓扑保持）
     ├─ MI 力处理 CT-MR 跨模态
     └─ 权重场限制配准在肝脏区域

Step 8: 边缘平滑 (ROI 变体)
  └─ DisEdgeSmooth(localDis, localDim, roiRange, ...)
     → 使用 3D ROI 距离场进行平滑（而非简单的 Z 轴距离）
     → 更精确地控制肝脏边界的过渡

Step 9: 局部形变场融合到全局
  └─ LocalDis2Global + Add to global displacement

Step 10: 重采样输出
```

### 8.4 CT-CT 肝部配准详细流程

**函数**: `OncologyNonrigidCTCT_Liver` (L1816)

```
输入: refImage (CT), movImage (CT), rigidMat[16]

Step 1: 全局非刚体配准 (如果无先验)
  └─ OncologyNonrigidCTCT_Global

Step 2: 肝部 Z 轴范围定位
  └─ GetCTLiverRange(refImage, movImage, organZRange)
     ├─ GetCTKeyPts_v2 → 关键点
     ├─ 肝顶: label[7], 肝底: label[14]
     ├─ Fallback: 如果 label[14] 缺失，使用 label[8] 或 label[9]
     └─ 取参考图和浮动图范围并集

Step 3: 边界扩展 + 提取局部子体积

Step 4: 局部非刚体配准
  └─ CTCTLocalNonRigidReg(refLocal, movLocal, localDis)
     ├─ 2级 MultiGrid 配准:
     │  MultiGridDemonsGeneralG(depth=3, deepEnd=2)
     │  ├─ Level 3: BaseMIDemonsG(sigma=10, step=0.125, itt=20)
     │  └─ Level 2: BaseMIDemonsG(sigma=10, step=0.125, itt=10)
     └─ 注意: deepEnd=2，不在原始分辨率配准

Step 5: 边缘平滑
Step 6: 局部形变场融合到全局
Step 7: 重采样输出
```

### 8.5 三种肝部配准参数对比

| 参数 | CT-CT | MR-MR | CT-MR |
|------|-------|-------|-------|
| **定位方法** | GetCTLiverRange | GetMRLiverRange | CTMRLocalRange |
| **Demons类型** | BaseMIDemonsG | BaseMIDemonsG | DiffeomorphicMIDemonsG |
| **金字塔层级** | 3→2 (2级) | 3→1 (3级) | 3→3 (SpecificGrid, 2阶段) |
| **sigma** | 10/10 | 3/7/10 | 3/7 |
| **step** | 0.125/0.125 | 0.125/0.25/0.25 | 0.125/0.25 |
| **itt** | 20/10 | 20/20/20 | 20/20 |
| **权重场** | 无 | 无 | 有 (肝脏 mask) |
| **Diffeomorphic** | 否 | 否 | 是 |
| **灰度预处理** | 无 (全局已处理) | 无 | CT: [-250,250] HU, MR: [1%,99%] |
| **初始位移** | 无 | 无 | LiverTopDistance (Z方向) |
| **边缘平滑** | Z轴距离 | Z轴距离 | 3D ROI 距离 |

---

## 9. 肺部局部非刚体配准

### 9.1 API 接口

```cpp
bool OncologyNonrigidCTCT_Lung(
    const CVolumeDataInfo& refImage,
    const CVolumeDataInfo& movImage,
    const double rigidMat[16],
    short* pOutData,
    OncologyWarpDis* pDis_src_tar,
    Mcsf::IProgress* pProgress = nullptr);
```

> **注意**: 肺部配准目前只支持 CT-CT，因为肺在 MR 上信号极低，难以可靠定位和配准。

### 9.2 肺部 Z 轴范围定位

**函数**: `GetCTLungRange` (L555)

肺部范围定位比肝脏复杂，因为肺尖（apex）的定位需要特殊处理：

```
GetCTLungRange(refImg, movImg, organZRange):

Step 1: 关键点定位
  ├─ refImg → GetCTKeyPts_v2 → refLabel[14], refPts[42]
  └─ movImg → GetCTKeyPts_v2 → movLabel[14], movPts[42]

Step 2: 肺尖 Z 坐标 (Z最大值，即最上方)
  └─ 肺尖 = max(refPts[5*3+2], refPts[6*3+2])
     → label[5] 和 label[6] 对应左右肺尖
     → 取两者中 Z 坐标更大的 (更靠上)
     → organZRange[1] = 肺尖 Z

Step 3: 肺底 Z 坐标 (Z最小值)
  ├─ 优先方案: (肝顶Z + 肝底Z) / 2
  │   → label[7] (肝顶) 和 label[14] (肝底) 的中点
  │   → 解剖学上肺底约在肝脏中部高度
  ├─ Fallback 1: (肝顶Z + 肾上极Z) / 2
  │   → label[7] 和 label[8]
  ├─ Fallback 2: (肝顶Z + 肾下极Z) / 2
  │   → label[7] 和 label[9]
  └─ Fallback 3: 肺尖Z - 320mm/spacingZ
      → 如果所有关键点都缺失，假设肺高约 320mm

Step 4: 取参考图和浮动图范围并集
  ├─ organZRange[1] = max(refZRange[1], movZRange[1])  // 肺尖取最高
  └─ organZRange[0] = min(refZRange[0], movZRange[0])  // 肺底取最低
```

### 9.3 肺部配准完整流程

**函数**: `OncologyNonrigidCTCT_Lung` (L1957)

```
输入: refImage (CT), movImage (CT), rigidMat[16]

Step 1: 全局非刚体配准 (如果无先验)
  └─ OncologyNonrigidCTCT_Global

Step 2: 肺部 Z 轴范围定位
  └─ GetCTLungRange(refImage, movImage, organZRange)
     ├─ 肺尖: label[5/6] (左右肺尖)
     └─ 肺底: 肝脏关键点中点 或 320mm 估计

Step 3: 边界扩展
  └─ ExtendEdge(organZRange, refImage.uiSize[2])
     → 向上下各扩展 1/4 范围

Step 4: 提取局部子体积
  └─ ExtractLocalForZAxis(refImage, organZRange, refLocal)
  └─ ExtractLocalForZAxis(movImage, organZRange, movLocal)

Step 5: 局部非刚体配准
  └─ CTCTLocalNonRigidReg(refLocal, movLocal, localDis)
     ├─ 2级 MultiGrid 配准:
     │  MultiGridDemonsGeneralG(depth=3, deepEnd=2)
     │  ├─ Level 3: BaseMIDemonsG(sigma=10, step=0.125, itt=20)
     │  └─ Level 2: BaseMIDemonsG(sigma=10, step=0.125, itt=10)
     └─ 与肝部 CT-CT 使用相同的配准引擎

Step 6: 边缘平滑
  └─ DisEdgeSmooth(localDis, localDim, ...)

Step 7: 局部形变场融合到全局
  └─ LocalDis2Global(pDis_src_tar, localDis, organZRange, ...)

Step 8: 重采样输出

Fallback: 如果 GetCTLungRange 失败 (关键点定位失败)
  └─ 直接使用全局配准结果重采样输出 (不做局部配准)
```

### 9.4 肺部配准的特殊考虑

1. **呼吸运动**: 肺部受呼吸影响大，形变幅度可达 10-20mm，需要较大的 `step` 和足够的迭代次数
2. **肺尖定位**: 肺尖在 CT 上清晰可见（低密度空气），关键点定位可靠
3. **肺底估计**: 肺底与膈肌/肝脏交界，直接定位困难，通过肝脏关键点间接估计
4. **CT only**: MR 上肺组织信号极低（含空气），无法可靠配准

---

## 10. ROI 区域配准

### 10.1 API 接口

```cpp
// CT-CT ROI 配准
bool OncologyNonrigidCTCT_ROIReg(
    const CVolumeDataInfo& refImage,
    const CVolumeDataInfo& movImage,
    const double rigidMat[16],
    const double roiRange[6],     // [x1, y1, z1, x2, y2, z2] (体素坐标)
    short* pOutData,
    OncologyWarpDis* pDis_src_tar,
    Mcsf::IProgress* pProgress = nullptr);

// MR-MR ROI 配准
bool OncologyNonrigidMRMR_ROIReg(...);

// CT-MR ROI 配准
bool OncologyNonrigidCTMR_ROIReg(...);
```

### 10.2 ROI 配准流程

ROI 配准与器官配准的区别在于：**用户手动指定 3D ROI 区域**，而非通过关键点自动定位。

```
Step 1: 全局非刚体配准 (如果无先验)

Step 2: ROI 边界扩展
  └─ ExtendEdge_ROI(roiRange, distance)
     → 在 3D 空间中向 6 个方向扩展指定距离

Step 3: 提取 3D 局部子体积
  └─ ExtractLocalImage(refImage, roiRange, refLocal)
     → 提取 [x1..x2, y1..y2, z1..z2] 范围的子体积
     → (与器官配准的 ExtractLocalForZAxis 不同，这里是 3D 裁剪)

Step 4: 局部非刚体配准
  ├─ CT-CT: CTCTLocalNonRigidReg
  ├─ MR-MR: MRMRLocalNonRigidReg
  └─ CT-MR: CTMRLocalNonRigidReg (带权重场)

Step 5: 边缘平滑 (3D ROI 变体)
  └─ DisEdgeSmooth(localDis, localDim, roiRange, ...)

Step 6: 局部形变场融合到全局
  └─ LocalDis2Global(pDis_src_tar, localDis, roiOffset, ...)

Step 7: 重采样输出
```

---

## 11. 关键点定位系统

### 11.1 McsfAlgoRegKeyptsLocalize

**文件**: `algo-localization/McsfAlgoRegKeyptsLocalize/McsfAlgoRegKeyptsLocalize.h`

```cpp
class McsfAlgoRegKeyptsLocalize
{
public:
    // CT 关键点定位 (v2 版本)
    void GetCTKeyPts_v2(
        const short* pImage,
        const unsigned int uiSize[3],
        const double dSpacing[3],
        double rescaleSlope,
        double rescaleIntercept,
        std::vector<char>& label,    // 输出: 14个标签 (0=未找到, 1=找到)
        std::vector<unsigned>& pts   // 输出: 14*3 坐标 (x,y,z 交替)
    );
    
    // MR 关键点定位 (v2 版本)
    void GetMRKeyPts_v2(
        const short* pImage,
        const unsigned int uiSize[3],
        const double dSpacing[3],
        std::vector<char>& label,
        std::vector<unsigned>& pts
    );
};
```

### 11.2 关键点标签定义

| 标签索引 | 解剖结构 | 用途 |
|---------|---------|------|
| 0-4 | 脑部结构 (脑室等) | 脑部配准 |
| **5** | **左肺尖** | **肺部 Z 范围上界** |
| **6** | **右肺尖** | **肺部 Z 范围上界** |
| **7** | **肝顶** | **肝脏 Z 范围上界, 肺底估计** |
| **8** | **肾上极** | **肺底估计 (fallback)** |
| **9** | **肾下极** | **肺底估计 (fallback)** |
| 10-13 | 其他腹部结构 | — |
| **14** | **肝底** | **肝脏 Z 范围下界, 肺底估计** |

### 11.3 关键点定位在配准中的作用

```
                    关键点定位
                        │
         ┌──────────────┼──────────────┐
         │              │              │
    肝脏范围定位     肺部范围定位    初始位移估计
         │              │              │
    ┌────┴────┐    ┌────┴────┐         │
    │ 肝顶Z   │    │ 肺尖Z   │    LiverTopDistance
    │ label[7]│    │label[5/6]│    = CT肝顶Z - MR肝顶Z
    │         │    │         │    → 初始化 Z 方向位移
    │ 肝底Z   │    │ 肺底Z   │
    │label[14]│    │(肝顶+肝底)/2│
    └─────────┘    └─────────┘
```

---

## 12. 辅助函数详解

### 12.1 GetWinValue — 百分位窗宽窗位

```cpp
void GetWinValue(const short* pImage, unsigned len, 
                 double backRatio, double frontRatio,
                 short& winMin, short& winMax);
```

- 计算图像灰度直方图
- `winMin` = `backRatio` (如 0.01) 分位数
- `winMax` = `frontRatio` (如 0.99) 分位数
- 用于 MR 图像的灰度归一化

### 12.2 ConvertTransformToMatrixOffset — 矩阵转四元数

```cpp
void ConvertTransformToMatrixOffset(
    const double pMatrix4x4[16],
    double pMatrix3x3[9],    // 旋转部分 → 四元数
    double pOffset3x1[3]);   // 平移部分
```

将 4×4 变换矩阵转换为 ITK 风格的 versor 四元数 + 偏移量表示。

### 12.3 ExtractLocalForZAxis — Z 轴子体积提取

```cpp
bool ExtractLocalForZAxis(
    const CVolumeDataInfo& srcImg,
    const unsigned organZRange[2],  // [zStart, zEnd]
    CVolumeDataInfo& localImg);     // 输出子体积
```

- 沿 Z 轴裁剪，X/Y 方向保留完整
- 复制图像数据到新的 `CVolumeDataInfo`
- 更新尺寸和原点

### 12.4 ExtractLocalImage — 3D 子体积提取

```cpp
bool ExtractLocalImage(
    const CVolumeDataInfo& srcImg,
    const double roiRange[6],       // [x1,y1,z1,x2,y2,z2]
    CVolumeDataInfo& localImg,
    unsigned char* mask = nullptr); // 可选 mask
```

- 3D 裁剪（X/Y/Z 三个方向）
- 用于 ROI 配准

### 12.5 LocalDis2Global — 局部形变场融合

```cpp
void LocalDis2Global(
    OncologyWarpDis* pGlobalDis,    // 全局形变场
    const float* pLocalDis,         // 局部形变场
    const unsigned localSize[3],    // 局部尺寸
    const unsigned offset[3],       // 在全局中的偏移 [offsetX, offsetY, offsetZ]
    const unsigned globalSize[3]);  // 全局尺寸
```

- 将局部形变场**累加**到全局形变场的对应位置
- 偏移量由器官 Z 范围决定

```
全局形变场:
┌─────────────────────────────────────┐
│                                     │
│    ┌─────────────────┐              │
│    │  局部形变场      │ ← offset     │
│    │  (累加到全局)    │              │
│    └─────────────────┘              │
│                                     │
└─────────────────────────────────────┘
```

### 12.6 DisEdgeSmooth — 边缘平滑过渡

**变体 1: Z 轴距离平滑** (用于肝部 CT-CT/MR-MR)

```cpp
void DisEdgeSmooth(
    float* pDis,              // 形变场
    const unsigned int uiSize[3],
    unsigned int zStart,      // 局部 Z 起始
    unsigned int zEnd);       // 局部 Z 结束
```

- 在 Z 轴边界处，将形变场按距离边界的比例缩放
- 边界处: scale = 0 (无形变)
- 中心处: scale = 1 (完整形变)
- 过渡区: scale = distance / edgeWidth

```
Z轴位置:  zStart          zEnd
           │                │
           ▼                ▼
scale:  0 ────── 1 ────── 0
           ↑                ↑
         边界             边界
         (平滑过渡到0)    (平滑过渡到0)
```

**变体 2: 3D ROI 距离平滑** (用于 CT-MR 肝部和 ROI 配准)

```cpp
void DisEdgeSmooth(
    float* pDis,
    const unsigned int uiSize[3],
    const double roiRange[6]);  // 3D ROI 范围
```

- 在 3D 空间中计算到 ROI 边界的距离
- 按距离比例缩放形变场
- 更精确地控制 3D 边界过渡

### 12.7 ExtendEdge — 边界扩展

```cpp
void ExtendEdge(unsigned organZRange[2], unsigned totalZ);
```

```cpp
unsigned range = organZRange[1] - organZRange[0];
unsigned extend = range / 4;  // 扩展 1/4
organZRange[0] = max(0, organZRange[0] - extend);
organZRange[1] = min(totalZ - 1, organZRange[1] + extend);
```

### 12.8 ExtendEdge_ROI — 3D ROI 扩展

```cpp
void ExtendEdge_ROI(double roiRange[6], double distance);
```

- 在 6 个方向（±X, ±Y, ±Z）各扩展 `distance` mm

### 12.9 OncologyResampleCacade — 级联重采样

```cpp
bool OncologyResampleCacade(
    const CVolumeDataInfo& refImage,
    const CVolumeDataInfo& movImage,
    const double rigidMat[16],
    short* pOutData,
    OncologyWarpDis* pDis = nullptr);
```

- 应用刚体矩阵 + 非刚体形变场的级联变换
- 先刚体变换，再非刚体变形
- 使用三线性插值

### 12.10 OncologyResample2SpecificSpacing — 目标间距重采样

```cpp
bool OncologyResample2SpecificSpacing(
    const CVolumeDataInfo& srcImage,
    const double targetSpacing[3],
    short* pOutData);          // short 版本

bool OncologyResample2SpecificSpacing(
    const CVolumeDataInfo& srcImage,
    const double targetSpacing[3],
    unsigned char* pOutData);  // unsigned char 版本
```

- 将图像重采样到指定的体素间距
- 用于配准前的降采样和配准后的上采样

---

## 13. 参数配置总表

### 13.1 全局配准参数

| 参数 | CT-CT Global | MR-MR Global | CT-MR Global |
|------|-------------|-------------|-------------|
| 降采样间距 | [2,2,4] | [1,1,2] | [1,1,1] |
| HU 裁剪 | [-140,260] | — | — |
| 金字塔 depth | 3 | 3 | 3 |
| 金字塔 deepEnd | 1 | 2 | 2 |
| Demons 类型 | BaseMIDemonsG | BaseMIDemonsG | BaseMIDemonsG |
| alpha | 1 | 1 | 1 |
| sigma (各层) | 10/10/10 | 10/10 | 3/7 |
| step (各层) | 0.25/0.25/0.25 | 0.125/0.25 | 0.125/0.25 |
| itt (各层) | 20/20/20 | 20/20 | 20/20 |
| stopCriteria | 0.00001 | 0.00001 | 0.00001 |

### 13.2 肝部局部配准参数

| 参数 | CT-CT Liver | MR-MR Liver | CT-MR Liver |
|------|------------|------------|------------|
| 降采样间距 | 原始 | [1,1,1] | [1,1,1] |
| 金字塔 depth | 3 | 3 | 3 |
| 金字塔 deepEnd | 2 | 1 | — (SpecificGrid) |
| Demons 类型 | BaseMIDemonsG | BaseMIDemonsG | DiffeomorphicMIDemonsG |
| alpha | 1 | 1 | 1 |
| sigma (各层) | 10/10 | 3/7/10 | 3/7 |
| step (各层) | 0.125/0.125 | 0.125/0.25/0.25 | 0.125/0.25 |
| itt (各层) | 20/10 | 20/20/20 | 20/20 |
| stopCriteria | 0.00001 | 0.00001 | 0.00001 |
| 权重场 | 无 | 无 | 有 |
| Diffeomorphic | 否 | 否 | 是 |
| CT 灰度裁剪 | — | — | [-250,250] HU |
| MR 灰度裁剪 | — | — | [1%,99%] |
| 初始 Z 位移 | 无 | 无 | LiverTopDistance |
| 边缘平滑 | Z轴 | Z轴 | 3D ROI |

### 13.3 肺部局部配准参数

| 参数 | CT-CT Lung |
|------|-----------|
| 降采样间距 | 原始 |
| 金字塔 depth | 3 |
| 金字塔 deepEnd | 2 |
| Demons 类型 | BaseMIDemonsG |
| alpha | 1 |
| sigma (各层) | 10/10 |
| step (各层) | 0.125/0.125 |
| itt (各层) | 20/10 |
| stopCriteria | 0.00001 |
| 边缘平滑 | Z轴 |

---

## 14. 使用示例

### 14.1 CT-CT 肝部配准

```cpp
#include "algo-fusion-app/McsfAlgoRegAppOncoLocalReg.h"
#include "algo-fusion-app/McsfAlgoRegAppOncoCommon.h"

// 1. 准备输入数据
CVolumeDataInfo refImage;  // 参考 CT
CVolumeDataInfo movImage;  // 浮动 CT
// ... 填充图像数据、尺寸、间距、HU参数 ...

double rigidMat[16];       // 刚体配准矩阵 (4x4)
// ... 从刚体配准结果获取 ...

// 2. 创建输出缓冲
short* pOutData = new short[refImage.uiSize[0] * refImage.uiSize[1] * refImage.uiSize[2]];
OncologyWarpDis warpDis;
warpDis.Reset(refImage.uiSize, refImage.dSpacing);

// 3. 执行肝部非刚体配准
Mcsf::IProgress progress;
bool success = Mcsf::OncologyNonrigidCTCT_Liver(
    refImage, movImage, rigidMat,
    pOutData, &warpDis, &progress);

if (success) {
    // pOutData: 配准后的图像
    // warpDis:  源→目标的形变场
    float* disField = warpDis.DisPtr();
    // ... 使用结果 ...
}

delete[] pOutData;
```

### 14.2 CT-MR 肝部配准（含全局+局部两级）

```cpp
// 1. 全局非刚体配准
OncologyWarpDis globalDis;
globalDis.Reset(refImage.uiSize, refImage.dSpacing);

Msf::OncologyNonrigidCTMR_Global(
    refImage, movImage, rigidMat,
    pGlobalOut, &globalDis, &progress);

// 2. 局部肝部配准 (传入全局形变场作为先验)
short* pFinalOut = new short[/* ... */];
Mcsf::OncologyNonrigidCTMR_Liver(
    refImage, movImage, rigidMat,
    pFinalOut, &globalDis, &progress);
// → globalDis 现在包含全局+局部的融合形变场

// 3. GPU 显存预估 (可选)
double gpuMem = Mcsf::OncologyNonrigidCTMR_Liver_GetMaxGPUMem(
    refImage.uiSize, refImage.dSpacing, rigidMat);
std::cout << "Peak GPU memory: " << gpuMem / 1024.0 / 1024.0 << " MB" << std::endl;
```

### 14.3 CT-CT 肺部配准

```cpp
OncologyWarpDis warpDis;
warpDis.Reset(refImage.uiSize, refImage.dSpacing);

Mcsf::OncologyNonrigidCTCT_Lung(
    refImage, movImage, rigidMat,
    pOutData, &warpDis, &progress);
```

### 14.4 ROI 配准

```cpp
// 用户指定 ROI [x1, y1, z1, x2, y2, z2] (体素坐标)
double roiRange[6] = {100, 100, 50, 300, 300, 200};

Mcsf::OncologyNonrigidCTCT_ROIReg(
    refImage, movImage, rigidMat, roiRange,
    pOutData, &warpDis, &progress);
```

---

## 15. 面试问答

### Q1: Demons 算法的核心思想是什么？

**A**: Demons 算法将图像配准转化为光流问题。核心思想是：对于参考图像和浮动图像之间的每个体素，计算一个"力"（force），这个力驱动浮动图像向参考图像对齐。力的大小与图像灰度差异成正比，与图像梯度成反比。通过高斯平滑力场并累加到位移场，迭代收敛到最终形变场。

公式：$\mathbf{F} = \frac{-(I_M - I_R) \cdot \nabla I_R}{|\nabla I_R|^2 + \alpha^2(I_M - I_R)^2}$

### Q2: 为什么 CT-MR 配准使用互信息（MI）力而不是光流力？

**A**: 光流力假设参考图和浮动图的灰度呈线性对应关系（同模态）。CT 和 MR 的灰度对应是非线性的——同一组织在 CT 上的 HU 值与 MR 上的信号强度没有简单的函数关系。互信息度量的是两幅图像的统计相关性，不依赖灰度的线性对应，因此适合跨模态配准。

### Q3: Diffeomorphic Demons 相比标准 Demons 有什么优势？

**A**: 
1. **拓扑保持**：微分同胚映射保证形变场可逆，不会产生折叠
2. **物理合理性**：形变场对应一个连续的变换过程，更符合生物组织的变形特性
3. **可逆性**：可以计算逆变换，用于双向配准
4. 通过 scaling-and-squaring 方法高效近似指数映射

### Q4: 多分辨率（MultiGrid）策略有什么好处？

**A**: 
1. **捕获大形变**：粗分辨率下，图像细节被平滑，大形变更容易被捕获
2. **避免局部最优**：从粗到细的策略减少了陷入局部最优的风险
3. **加速收敛**：粗分辨率计算量小，快速逼近大致形变；细分辨率精修
4. **提高鲁棒性**：对噪声和局部差异更不敏感

### Q5: 两级配准架构（全局+局部）的设计动机是什么？

**A**: 
1. **全局配准**处理整体对齐（如腹部整体位移），使用粗分辨率和较大 sigma
2. **局部配准**专注于器官级别的精细对准（如肝脏内部形变），使用更高分辨率和更精细参数
3. **计算效率**：局部配准只处理器官区域（通过关键点定位），而非整个体数据
4. **边缘平滑**确保局部形变场与全局场无缝融合

### Q6: 关键点定位系统如何工作？

**A**: 使用 `McsfAlgoRegKeyptsLocalize` 自动检测 14 个解剖关键点（肺尖、肝顶/底、肾脏等）。对于 CT，使用 HU 值特征和形态学方法定位；对于 MR，使用信号强度和结构特征。关键点用于：
1. 确定器官 Z 轴范围（提取局部子体积）
2. 计算初始位移估计（LiverTopDistance）
3. 如果定位失败，有 fallback 策略（如肺底用 320mm 估计）

### Q7: 权重场（Weight Field）在 CT-MR 配准中的作用？

**A**: 权重场来自肝脏 mask（肝脏内=1，外=0）。在 Demons 力计算后，将力乘以权重场，使得：
1. 肝脏内部的力保持完整，驱动精确配准
2. 肝脏外部的力被抑制，避免无关组织的干扰
3. 边界处力自然过渡，配合 DisEdgeSmooth 实现平滑融合

### Q8: DisEdgeSmooth 为什么重要？

**A**: 局部配准只在器官区域产生形变场。如果直接将局部形变场叠加到全局场，在边界处会产生突变（不连续），导致重采样图像出现伪影。DisEdgeSmooth 将形变场在边界处按距离比例缩放到 0，确保：
1. 局部形变在器官内部完整保留
2. 边界处平滑过渡到 0
3. 与全局形变场无缝融合

### Q9: GPU 加速带来了什么性能提升？

**A**: 
1. **并行力计算**：每个体素的力计算独立，GPU 并行处理
2. **并行图像变形**：三线性插值并行化
3. **GPU 端高斯平滑**：避免 CPU↔GPU 数据传输
4. **缓冲区复用**：`DoRegistration_withBuffer` 一次性分配所有 GPU 内存，减少 `cudaMalloc/cudaFree` 开销
5. 典型体数据（256³）配准时间从 CPU 的数十分钟降低到 GPU 的 1-2 分钟

### Q10: 收敛条件是如何设计的？

**A**: 使用误差变化率作为收敛指标：
$$ratio = \frac{|error_0 - error|}{error_0}$$

其中 `error` 是所有体素灰度差异绝对值之和。当 `ratio < stopCriteria`（如 0.00001）且迭代次数 > 最小值（CPU: 10, GPU Diffeomorphic: 0）时收敛。这确保：
1. 误差不再显著下降时停止（避免无效迭代）
2. 最小迭代次数保证基本收敛
3. `stopCriteria=0.00001` 是较严格的阈值，确保充分收敛

### Q11: 为什么 CT-CT 肺部配准的 deepEnd=2 而不是 1？

**A**: `deepEnd=2` 意味着不在原始分辨率（Level 1）执行配准，只在 Level 3 和 Level 2 配准。原因：
1. 肺部 CT 通常分辨率较高，原始分辨率配准计算量大
2. 肺部结构在中等分辨率下已经能很好地对齐
3. Level 2 的形变场上采样到原始分辨率已足够精确
4. 节省计算时间，同时保证配准质量

### Q12: scaling-and-squaring 的迭代次数如何确定？

**A**: 
- **自动模式** (`m_use_autonumiter_exp=true`)：根据最大位移自动计算
  - $n = \lceil 2 + \log_2(\max|\mathbf{v}|) \rceil$
  - 确保 $\max|\mathbf{v}/2^n| < 1$，使一阶泰勒近似有效
- **手动模式**：`SetExpMaximumUpdateStepLength` 指定最大更新步长，反推迭代次数
- 默认最大 20 次迭代

---

## 附录: 源文件索引

| 文件 | 路径 | 内容 |
|------|------|------|
| 局部配准 API | `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoLocalReg.h` | 肝/肺/ROI 配准接口声明 |
| 全局配准 API | `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoGlobalReg.h` | 全局配准接口声明 |
| 公共数据结构 | `algo-fusion-app/include/algo-fusion-app/McsfAlgoRegAppOncoCommon.h` | OncologyWarpDis 等 |
| 局部配准实现 | `algo-fusion-app/src/McsfAlgoRegAppOncology/src/McsfAlgoRegAppOncoLocalReg.cpp` | 所有局部配准实现 (~2100行) |
| 全局配准实现 | `algo-fusion-app/src/McsfAlgoRegAppOncology/src/McsfAlgoRegAppOncoGlobalReg.cpp` | 全局配准实现 |
| CPU Demons 头文件 | `algo-registration/include/algo-registration/McsfAlgoBaseDemons.h` | BaseDemons 类声明 |
| CPU Demons 实现 | `algo-registration/src/McsfAlgoDemons/src/BaseDemons.cpp` | 迭代循环、积分等 |
| CPU 光流力 | `algo-registration/src/McsfAlgoDemons/src/OpticalForce.cpp` | Thirion Demons 力 |
| CPU 运动场 | `algo-registration/src/McsfAlgoDemons/src/MovingField.cpp` | 高斯平滑、位移更新 |
| CPU MultiGrid | `algo-registration/src/McsfAlgoDemons/src/MultiGridDemons.cpp` | 多分辨率框架 |
| CPU 重采样 | `algo-registration/src/McsfAlgoDemons/src/Resampling.cpp` | 三线性插值变形 |
| GPU Demons 头文件 | `algo-registrationgpu/include/algo-registrationgpu/McsfAlgoBaseDemonsG.h` | GPU Demons 类层次 |
| GPU MultiGrid 头文件 | `algo-registrationgpu/include/algo-registrationgpu/McsfAlgoMultiGridDemonsG.h` | GPU 多分辨率框架 |
| GPU Demons 实现 | `algo-registrationgpu/src/McsfAlgoDemonsG/src/McsfAlgoBaseDemonsG.cpp` | GPU 迭代循环 |
| GPU MultiGrid 实现 | `algo-registrationgpu/src/McsfAlgoDemonsG/src/McsfAlgoMultiGridDemonsG.cpp` | GPU 多分辨率实现 |
| GPU 力接口 | `algo-registrationgpu/src/McsfAlgoDemonsG/include/Force.h` | ForceInterface 层次 |
| GPU 运动场接口 | `algo-registrationgpu/src/McsfAlgoDemonsG/include/MovingFiled.h` | MovingFiledInterface |
| CUDA 接口 | `algo-registrationgpu/src/McsfAlgoDemonsG/include/cuInterface.h` | CUDA 函数声明 |
| 关键点定位 | `algo-localization/McsfAlgoRegKeyptsLocalize/McsfAlgoRegKeyptsLocalize.h` | 解剖关键点检测 |
| 测试用例 | `algo-fusion-app/src/McsfAlgoRegAppOncology_UT/src/McsfAlgoRegAppOncology_UT.cpp` | 使用示例 |
